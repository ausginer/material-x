{
  "name": "@ausginer/traits-abstract",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsdown --config tsdown.config.ts",
    "clean:build": "git clean -fx -e .vite -e node_modules -- index.js index.js.map index.d.ts index.d.ts.map",
    "typecheck": "tsgo -p tsconfig.json --noEmit",
    "lint": "oxlint --type-aware . && eslint --flag unstable_native_nodejs_ts_config -c ../../eslint.config.ts .",
    "test": "node --eval \"\""
  },
  "dependencies": {
    "type-fest": "5.5.0"
  }
}
