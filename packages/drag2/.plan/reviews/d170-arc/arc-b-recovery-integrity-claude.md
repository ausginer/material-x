# Arc B recovery — integrity pass

**Read at:** `8c1b20042ecf74ddeef7c9bc65ceb6ff46e2fa3e`. Verified before reading source: branch `drag2/fin-review` at that commit, `git status --porcelain` empty, `git diff 8c1b20042 HEAD -- packages/drag2/src packages/drag2/tests packages/drag2/bench` empty, and `git rev-parse 8c1b20042:packages/drag2/src` = `cc11493373519deb2fa7df6e17da2e8eb7b9265c`, matching the stated tree object. No transient mutation is present.

**Production range:** `5f7a901a4..8c1b20042` resolves to exactly two commits touching `packages/drag2`: `0b45c0088` ("deliver the construction window per member and delete the identity conjunct" — the substantive change, touching `src/kernel/{kernel,seams,spec}.ts`, `bench/size/measure.ts`, contract docs, and `tests/kernel/{construction,kernel.browser,seams,vocabulary}`) and `8c1b20042` (a one-line tense correction in `02-kernel-behavior-contract.md`). This is a first, independent pass — I did not read any prior review of this range (the five named exclusions, and no others under `.plan/reviews/d170-arc/`) and derived every claim below from the tree and from running the suite myself.

**Lens:** package coherence outside the change — neighbouring flows, public surface, architectural invariants, integration effects. Feature-vs-plan correctness is out of scope by design; I only cross-checked plan/contract prose against source where the prose makes a falsifiable claim about the current tree (which is itself an integrity property: does the record the package relies on still agree with the code it describes).

## What I examined and found coherent (stated clean results)

1. **Public surface.** `#pinned`, `#begin()` and the identity conjunct are all `#private` — no exported type, class or function changes shape. `SeamDriver`'s constructor drops from four collaborators to three (`frames, preparationValid, fail, notify`); grep over `packages/drag2/src` finds exactly one construction site (`kernel.ts:2495`), consistent with the new signature. `tests/exports.node.test.ts`, `packaging.node.test.ts`, `entry.node.test.ts` and `consumer.node.test.ts` all pass (see run below), so the package's declared and shipped surface is unaffected.

2. **`#begin()` removal is total and consistent.** Every one of the five surviving call sites (`kernel.ts:341, 1596, 1817, 1863, 2057`) now opens the pair directly via `this.#frames.begin()`; `SeamDriver`'s own internal use (`seams.ts:275`) matches. `grep -rn "pinned|OperationIdentity"` across `src/`, `tests/` and `.plan/` turns up only the unrelated, still-live `OperationIdentity` type (`frames.ts:17`, used for per-operation identities elsewhere) and prose uses of the English word "pinned" — no stray reference to the deleted field or method survives.

3. **`fail()` and `#cancelWith`'s new `#spec` guards are correctly ordered.** `fail()` tests `if (this.#spec)` before dereferencing `#driver`; `#cancelWith` tests `!this.#spec` before dereferencing `#frames.current.operation`. Both ride the existing invariant that `#spec` is published only after `#driver`/`#frames` are assigned, so neither adds a new nullable field. This matches contract doc `00-index.md` D-184's specific claim, which I verified against the code rather than trusting.

4. **`arm()`'s construction-window unwinding matches its own documented order.** Read the full method (`kernel.ts:2423-2551`): static `actionTags`/`command.types` validation runs first and unconditionally; the terminal latch (`!this.#bracket.closed`) is tested before each of the two `createFramePart()` calls and again before the entity (`FrameTransaction`/`SeamDriver`/`#spec`) is constructed; every exit that does not reach the `return` inside the latch-open branch — including a `destroy()` that closed the bracket with no throw — falls through to the shared `#unwindArm` helper, which retires the behavior, resets whichever frame(s) exist, aborts ingress and clears `#spec`. This is exactly what `00-index.md` D-184 and the `spec.ts` docblock describe.

5. **`BehaviorContext`'s construction-window contract (`spec.ts`) is accurate against the current two behaviors.** Neither `sortableFramePart`/`freeDragFramePart` (the actual `createFramePart` implementations wired at `sortable/spec.ts:1891` and `free-drag/spec.ts:1076`) reads or writes anything on the kernel — they take no parameter at all. So the newly-documented per-member construction-window behavior (dispatch dropped silently, `fail`/`cancel` demoted, `destroy` still tears down) is exercised only by the dedicated instrument (`tests/kernel/construction.node.test.ts`, six rows) and is inert in both shipped behaviors today — a correct and unsurprising state, not a gap: neither behavior's frame-part factory reaches back into the context.

6. **Bench-budget control suspension is consistent across the three sites that state it.** `bench/size/measure.ts` currently declares `control:` on exactly two rows — `vocabulary root - drag.js` (142) and `baseline B - shipped @ydinjs/drag sortable.js` (6889) — and on no others. `.plan/obligations.md` O-13 names exactly those two as the ones that "keep theirs throughout" and names the other five (`free drag minimal`, `free drag + bounds`, `free drag + landing`, `free drag complete`, `kernel root - kernel.js`) as suspended for the D-170 arc series; `.plan/measurements/arc-b-remediation.md` §The controls states the same two figures as the ones "both held it exactly." All three sites agree.

7. **The `02-kernel-behavior-contract.md` tense correction (`8c1b20042`) is accurate.** It changes a present-tense claim ("03 §Internal to the ordinary tier **lists** `SeamOutcome` and `ArmOutcome` as kernel-tier published; that **is** wrong...") to past tense, because `0b45c0088` had already edited `03-feature-composition.md`'s live list to drop `ArmOutcome` from it. Read `03-feature-composition.md`'s current text: it now states "Two names were on this list and are struck: `SeamOutcome` and `ArmOutcome`..." — so the past-tense correction in `02` is required and correct, and `grep -rn "ArmOutcome" packages/drag2/src packages/drag2/tests` returns nothing, confirming the name really is dead in the current tree.

8. **Historical ledger prose that still names `ArmOutcome`/`SeamContext` as if live is correctly out of scope, not missed drift.** `05-lifecycle-invariants.md` ("Resolved by `ArmOutcome` (`ARM_ARMED`/...)") and `00-index.md`'s canonical F-35 entry read, on first pass, like present-tense claims about dead machinery. They are not: both are **decision/finding-ledger rows** describing what fixed a defect _at the time it was fixed_, and the package's own `references.node.test.ts` documents this exact carve-out ("a decision-ledger row is a dated act whose reasoning stands as it stood... Ledger rows are therefore skipped by shape"). `00-index.md` F-382 — landed inside this very range — makes the same point explicitly: "the two historical resolution records that name the deleted mechanism are left alone: F-35's disposition is a statement about what resolved it at a date, not a claim about the tree." I ran `references.node.test.ts` and it passes, consistent with this being intentional rather than an oversight. Not a finding.

9. **`packages/material-x` has no dependency on `drag2`.** No import of `@ydinjs/drag2` anywhere under `packages/material-x/src`, and `drag` does not appear in its `package.json`. The Material-X-component check this lens is asked to apply (files.json vs. runtime entrypoints) does not apply: this range adds or removes no Material X component, and the two packages do not share a build or runtime boundary.

10. **Full package suite is green.** `npx vitest run -c vitest.config.ts` from `packages/drag2`: **72 test files, 1301 passed, 0 failed, 60 skipped** (all 60 are the perf/measurement suites — `m1`, `m1-prime`, `m2`, `m2-prime`, `m5`, `m6`, `p01-write-cost`, `p02-shrink`, `q7` — which are opt-in measurement harnesses, gated the same way regardless of this change, and unrelated to the construction-window/identity-conjunct diff). This includes the node-tier record-integrity instruments (`decisions.node.test.ts`, `references.node.test.ts`, `docs.node.test.ts`, `coverage.node.test.ts`), the size/budget instrument (`bench/size.node.test.ts`, which builds with `tsdown` and re-measures every composition), and every browser-tier kernel/sortable/free-drag suite. Nothing in the range regressed a neighbouring flow this suite would see.

## Noted but explicitly not a finding of this pass

`.agents/docs/architecture.md` §"Drag: the authored-presentation barrier" documents `canReleasePresentation`, `kernel/protocol.ts`, `kernel/presentation-ready.ts`, `PRESENTATION_READY_TIMEOUT` and `ResolutionCurrency`. Those symbols exist in `packages/drag` (the older, currently-shipped `@ydinjs/drag`, confirmed by `package.json`'s `"name": "@ydinjs/drag2"` for this package plus `bench/size/measure.ts`'s "baseline B — shipped `@ydinjs/drag` sortable.js" row) but not in `packages/drag2/src` (`grep` for all five names against `packages/drag2/src` returns nothing). This is a real question about which package the architecture doc's Drag section describes, but the diff under review does not touch `kernel/protocol.ts`, `kernel/presentation.ts`, or anything the presentation-ready barrier depends on, and `architecture.md` was not touched by either commit in range. I checked it only because this lens is told to consult `architecture.md` for suspected drift, and having checked, the suspected drift is orthogonal to this change and predates it — reporting it here would attribute pre-existing repository state to this range. Left for whoever owns a general architecture-doc pass.

## Scope not reached, and why

- **`packages/drag` (the old, shipped package) itself.** Out of this lens's boundary: it is not part of the stated production range and this range does not touch it.
- **Runtime behavior beyond what the suite exercises** (manual/exploratory drag interaction, visual output). The lens is package coherence, not feature correctness, and the full suite (including 208 browser-tier kernel tests and the sortable/free-drag interaction suites) already exercises the changed machinery end-to-end; I relied on that rather than re-deriving it by hand.
- **The five excluded prior-round documents** and any other `.plan/reviews/d170-arc/*` record not already cited above (in particular I did not open `arc-b-recovery-cleanup-claude.md`, a sibling pass from what appears to be this same recovery round, to avoid biasing this pass's independence beyond the letter of the exclusion list).
- **A byte-for-byte re-verification of every budget number** in `measure.ts` beyond the two `control:` rows and the five-name cross-check above. `tests/bench/size.node.test.ts` (part of the green full-suite run) already asserts every row's budget and module graph against a real build; re-deriving those same numbers by hand would not add independent evidence beyond what that instrument already provides.

## Findings

None. Every property checked above holds at the read commit.