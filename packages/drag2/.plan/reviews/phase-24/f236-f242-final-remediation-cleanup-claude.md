# F-236–F-242 final remediation — code discipline (cleanup)

Commit read at `f3adb3fc` (confirmed via `git log --oneline 3d968dfa..f3adb3fc` to be exactly one commit). Diff range `3d968dfa..f3adb3fc`.

## Scope

Covered, against `CONTRIBUTING.md` (Part I style, Part II size-and-ownership policy) and `.agents/docs/documentation.md` §5 (comments/JSDoc):

- `packages/drag2/tests/packaging.node.test.ts` — the D-85 guard rewrite: deleted `slotConstants()`/`declaresSlot`/two-directory walk, reuse of the pre-existing `sources()` helper, dead-import and dead-local check.
- `packages/drag2/.plan/contract/02-kernel-behavior-contract.md` — the strikethrough additions, proportion and readability.
- `packages/drag2/.plan/contract/00-index.md` and `packages/drag2/.plan/plan.md` — the six new/edited ledger rows and the new plan entry, checked for duplication against established format and against the prior round's already-validated ledger/narrative split.
- `packages/drag2/src/kernel/spec.ts` (published JSDoc on `ActivationScope.visual`) and `packages/drag2/tests/COVERAGE.md` — checked incidentally for the same proportion/duplication/JSDoc-hygiene questions.

Not covered: correctness of the D-165 domain model itself (item/visual/box semantics), lifecycle/failure-semantics questions, and whether F-239's chosen disposition (delete vs. widen) was the right call — that is `f239-f240-disposition-claude.md`'s and the architect's territory, not a code-discipline question, and I did not re-litigate it.

## Findings

**cleanup-1 (no defect).** `packaging.node.test.ts`'s D-85 guard rewrite is a net simplification with no new machinery. Evidence: the diff (`git diff 3d968dfa..f3adb3fc -- packages/drag2/tests/packaging.node.test.ts`) deletes the ~24-line `slotConstants()` helper plus its JSDoc, the `declaresSlot` regex, and the two-directory (`free-drag`/`sortable`) manual `readdir`/`readFile` walk with its `offenders` accumulator, replacing all of it with a single `Promise.all` over the file's own pre-existing `sources(SRC)` walker (already used at line 117 for an unrelated assertion), filtering for `from '@ydinjs/box-quad'` and asserting the result equals `['kernel/presentation.ts']`. Net: −49/+11 lines in this hunk, no new imports, no new local abstraction — `sources()` is reused, not duplicated.

- Dead-import check: `readFile`, `readdir` and `resolve` (all imported at the top) remain used elsewhere in the file — `readdir` at lines 26 and 87 (inside `sources()`) and 338; `readFile` at lines 44, 71, 118, 281, 371, 422, 494; `resolve` at line 22 (`ROOT`) and line 76. None became dead.
- Dead-local check: the old `files = ['free-drag', 'sortable']` array is deleted along with the loop that used it; no replacement local of that shape was introduced. `SRC` (module-level `const`) remains used at ten other sites in the file (lines 41, 58, 117, 164, 188, 198, 211, 215, 234, 242, 252, 281, 321, 338), so it did not become an unused local either.
- The rewritten `it` still names its unit under test consistently with the file's `describe`/`it` convention (CONTRIBUTING.md §Unit tests): `it('should import the geometry package in exactly one module', ...)`.

**cleanup-2 (no defect).** The strikethrough markup added to `02-kernel-behavior-contract.md` is proportionate and matches the document's own long-established convention; it is not new machinery introduced by this remediation. Evidence: `git show 3d968dfa:.../02-kernel-behavior-contract.md | grep -c '~~'` → 40 occurrences already present before this commit; `git show f3adb3fc:... | grep -c '~~'` → 48 after, i.e. this diff added roughly 8 strike markers against a file that already used the pattern pervasively (JSDoc blocks, prose paragraphs, table cells, and even inside a fenced `ts` example — see line 60's pre-existing `~~Unused by the sortable behavior...~~ **Required by D-39...**`, which predates this diff). `.agents/docs/documentation.md` §1's "no strikethrough" rule targets _current-state_ documents (Convention/Policy/Operation); the documentation model's own table (§8) places `packages/*/.plan/` under **Record**, and §6 explicitly sanctions amending a record entry in place with "its supersession named rather than rewritten" — which is what the struck-and-replaced prose does here. Each new strike in this diff corresponds exactly to a sentence whose factual content D-165 superseded (the two-role `AdmissionSubject` description, the "optionally paired" `admit` return description, the single-field `inheritedSpace` declaration and its "derived from the measurement" rationale); adjacent unstruck text (e.g. "Both admission members return this type — see §Admission returns a subject...", the D-59 "three reasons" list) is left alone because it still holds. Nothing reads as over-struck or under-struck relative to what actually changed.

**cleanup-3 (no defect).** The `00-index.md` and `plan.md` additions for F-236–F-242 do not introduce new duplication beyond the pattern this round's own `cleanup` pass already validated. Evidence: `packages/drag2/.plan/reviews/phase-24/bq6-bq9-d165-remediation-cleanup-claude.md` (this round's earlier cleanup report) explicitly checked and accepted "the triple recording of F-232–F-235 across `00-index.md`/`plan.md`/`COVERAGE.md`" as "the pre-existing pattern this package already uses for every other closed finding... each document answers a different question (ledger status vs. narrative chronology vs. what a test file asserts)." The six new rows (F-236–F-242, one row per finding, F-239 revised) follow the same one-row-per-finding ledger format as F-230–F-235 (bold **Closed, date** lead, evidence, **The general shape** closer). The new `plan.md` entry (`### 2026-08-31 — the remediation round's remaining five findings...`) is chronological narrative — it explains _why_ each fix was made and what was rejected (e.g. the F-239 "delete vs widen" rationale, the "F-238 and F-241 are one correction read twice" framing) — content the terse ledger rows do not carry, consistent with the ledger/narrative split. No section of the plan entry merely restates a ledger row verbatim.

**cleanup-4 (no defect).** No other diff hunk reads as machinery beyond what closing these six findings requires.

- `packages/drag2/src/kernel/spec.ts`: the `ActivationScope.visual` doc-comment edit (F-236) brings it to exactly the same shape and length as the sibling `box` member's doc comment three lines below (visible in the file at `src/kernel/spec.ts` lines ~157–169), which is the minimum edit for "give `visual` the shape `box` already has." It stays within §5.1's published-JSDoc rules: present tense, no `D-`/`F-` pointers, no strikethrough, no history narration.
- `packages/drag2/tests/COVERAGE.md`: a single-line row update to match the renamed `it` description — proportionate, no new rows, no duplication of the ledger's own content.
- No stale references survive the `packaging.node.test.ts` rewrite: `grep -rn "slotConstants|declaresSlot|BOX_ANCESTOR_"` across `packages/drag2/tests/` and `packages/drag2/src/` returns nothing.

## Summary

Clean pass. All four scoped questions checked and found without defect: the test rewrite is a genuine simplification with no dead code; the strikethrough usage is proportionate and consistent with the document's pre-existing, precedented convention (Record-classified under `.agents/docs/documentation.md` §8/§6, not a current-state rulebook under §1); the ledger/plan additions match the format and split this round's own cleanup pass already validated; and no other hunk introduces machinery, duplication or dead surface beyond what closing F-236–F-242 requires.

LSP plugin - unavailable (not probed; this review used Read/Bash/grep over a documentation- and test-file diff, where LSP's code-symbol operations — definitions, references, call hierarchy — would not have added evidence beyond the grep-based dead-import/dead-local checks already performed).