# D-168 — decision elimination review

**Files read at `d51d59c6`** (the sole commit in `7f6d1851..d51d59c6`, "drag2: name the kernel's per-operation state as two records"), the review range's tip. Decision text read from `npx just decisions` (D-168 `active`) and from `.plan/reviews/phase-24/f273-operation-record-claude.md` in full. `npx just decisions --retired` run and grepped for `168`, `273`, `clearOperationState` — no rows, confirmed below.

## Scope

**Question asked**: does D-168's stated justification — that initialization and retirement become auditable _by construction_, not by "someone remembering to extend a nine-assignment helper 250 lines from the declarations" — actually hold of the shipped code, and does anything survive the change without a live reason?

**Backward** (current machinery → decision → still alive?): every construction site of `OperationRecord`/`ActivationRecord`, every mutation site, both retirement call sites, the four excluded fields' current write/read sites, `ActivationScope`'s construction, and the type declarations themselves (checked for a reintroduced discriminant).

**Forward** (retired content → orphaned survivor?): `npx just decisions --retired` for D-168/F-273-adjacent rows (none — D-168 is a fresh landing with no retired clause of its own: its status changed `Unimplemented → Implemented` and it gained a witness paragraph, it retracted nothing it had asserted). Traced whether any _earlier_ decision's retired text (the pre-D-168 flat-binding shape, never itself the subject of a decision) left orphaned prose in the contract tier — checked `.plan/contract/00-index.md`, `01-construction-ownership.md`, `02-kernel-behavior-contract.md`, `03-feature-composition.md` for `clearOperationState` and per-operation-state prose asserting the old closure-`let` shape as current. **Result: nothing survives.** The one place the old fourteen-field enumeration appears (`01-construction-ownership.md:270`) is explicitly historical narrative about a pre-Checkpoint-C probe, correctly past-tensed already.

**Verification performed**: `npx just typecheck` (clean); `npx vitest run tests/kernel/kernel.browser.test.ts` (171/171 passed, including the new "the operation record" describe block, run before an unrelated transient working-tree mutation from a concurrent process appeared and later cleared on its own — noted under Method); `git show d51d59c6:...` used for all source citations to stay independent of that transient state; grep census of every `OperationRecord`/`ActivationRecord`/`operation`/`activation` read and write site in `src/kernel/kernel.ts`.

**Not covered in depth**: `bench/size/measure.ts`'s control-row updates and `.plan/measurements/budget-rebases.md`'s size table (spot-checked, not re-measured — the size gate is D-106's standing instrument and re-running it is outside a DER's remit); the free-drag/sortable spec files (`AdmissionSubject`'s three-member shape is unchanged by this commit, per the diff); `01`, `03`–`07` contract files beyond the greps listed above.

## Findings

**None.** Every property D-168 required is verified true of the shipped code, and no field, helper, comment or branch was found surviving without a live justification.

## Checked and found sound (null results)

- **Both records are complete at their one constructing literal, with no field left implicitly relying on a later assignment.** `OperationRecord` is built whole at `mintOperation` (`operation = { visual, box, item, lifetimes: createOperationLifetimes(notify), cancelRequest: null }`, `src/kernel/kernel.ts:1006-1012`) — including `lifetimes`, constructed _inside_ the literal rather than assigned afterward, which is what makes it non-nullable in the type. `ActivationRecord` is built whole at `acquireActivation` (`activation = { originRect: rect, lift: session, visualSpace }`, line 1279). A full-file grep for `operation =` / `activation =` (excluding the two `null` retirement sites) turns up exactly these two literals — no partial construction, no second write completing a record started elsewhere. This is the specific defect F-273 named ("constructed partial at admission and completed 250 lines away") and it does not recur.
- **`clearOperationState` is fully deleted, with no shadow obligation reintroduced.** `grep -rn clearOperationState src/ tests/` is empty. No new helper collects a per-field list to maintain: the only other nulling sites are the two three-line retirement blocks (`operation = null; activation = null; pinned = null;`), which null two record references and one identity-comparison field — not a per-field census that must be extended when a new field is added inside either record (the type now enforces that; TypeScript rejects an unfilled `readonly` field in the literal).
- **The one documented exception (`cancelRequest`) has exactly two mutation sites, matching a latch and nothing more.** `operation!.cancelRequest = { reason, origin }` at `cancelWith` (line 773) and `live.cancelRequest = null` at `handleCancel` (line 2010) — set once per operation, consumed once, no third site.
- **Retirement ordering — the one property with a live test — holds and is exercised.** Both retirement paths (`retireOperation` line ~550, `runPhysicalTeardown` line ~630) null `operation`/`activation`/`pinned` as the _last_ statement, strictly after `scrub(current); scrub(draft)`. `tests/kernel/kernel.browser.test.ts`'s new `"should still hold the operation while retirement runs"` re-enters through `host.cancel()` from inside `spec.retire` and asserts no report and a completed `finalized` call — a mutation that reordered the nulling ahead of the scrubs would fail this test outright (confirmed by running it against the committed tree: 171/171 pass).
- **`handleFailed`'s mid-operation `retireAttempts` caller — spot-checked directly.** Line 2134 calls `retireAttempts()` (clearing `resolution`/`settlement`/`settlementInput`) with the operation fully alive — `operation` is not touched — then line 2161 installs a fresh `settlement = { targetX: null, targetY: 0 }` for the same still-live operation, exactly as F-273's finding required: two settlements in sequence inside one `OperationRecord`'s lifetime, with the failure (not the second settlement) as the operation's actual terminal event.
- **`pinned`'s self-reference exclusion — spot-checked directly.** `pinned` remains a bare `let pinned: OperationIdentity | null` (line 296), written by every `begin()` (line 440: `pinned = current.operation`) and compared at `preparationValid` (line 486: `current.operation === pinned`). It was not folded into `OperationRecord` — doing so would compare a value against itself, exactly the reason the decision gives.
- **No discriminant duplicating `phase` was added.** `OperationRecord` and `ActivationRecord`'s type declarations (lines 181, 222) carry only the eight original data fields between them (five + `cancelRequest`, three) — no `kind`/`state`/`stage` tag. Nullability of the `let operation`/`activation` bindings is the only discriminant, exactly as §2 of the decision record specifies and explicitly rejects the alternative.
- **No defensive null-check beyond what the prior flat-binding code already needed.** The three sites using `operation?.cancelRequest` (`preparationValid` line 486, `handleFailed`'s stale-checkpoint guard line 2097) are optional-chained because those call sites are reachable when no operation exists at all — exactly mirroring the old plain nullable `let cancelRequest`, which was equally `null` outside an operation. The sites using `operation!.cancelRequest` (`cancelWith` line 764, `failOperation` line 816, `handleStartCommitted` line 2065) do so only after a preceding `current.operation`/`identity` check already establishes an operation is live, consistent with the stated invariant `current.operation !== null ⟹ operation !== null`. Neither pattern is new caution; both are load-bearing translations of the pre-existing nullable-`let` guards.
- **No compatibility shim for the old flat-binding shape.** No adapter, no re-exported alias, no dual-write path. The eight `let` bindings (`visual`, `box`, `item`, `lifetimes`, `cancelRequest`, `originRect`, `lift`, `visualSpace`) are gone outright — `grep` for each as a bare `let` declaration in `src/kernel/kernel.ts` returns nothing.
- **`ActivationScope` is still built field by field, and the kernel does not invert into reading its own geometry back from it.** Lines ~1284-1301: `visual: live.visual, originRect: activation.originRect, box: live.box, boxPre, visualSpace: activation.visualSpace, itemSpace, lift: session, motion: live.lifetimes.motion, presentation: live.lifetimes.presentation` — every field sourced from the kernel's own locals/record, none read back from the object being constructed. Matches §5's required property exactly.
- **Neither record appears in any signature, queue argument, or captured callback.** `grep -n "OperationRecord\|ActivationRecord"` finds only the two type declarations and the two `let` bindings — no function parameter, no dispatched-action payload, no `FailureCheckpoint` field. `OperationIdentity` (unchanged, one field) remains the only thing carried across a queue boundary, as the decision's §4 requires.
- **The factory-vs-class question (D-167/F-231/F-274) is untouched by this implementation and this DER does not reopen it.** Both records are plain object literals constructed inside the closure `createKernel` already returns from; they cross no tier boundary and are not threaded arguments, so nothing about this change alters the evidence D-167 or a future F-274 reassessment would read. No opinion offered beyond that observation.
- **Forward pass: no retired normative content left orphaned machinery.** D-168 carries no retired clause of its own (`npx just decisions --retired` has no D-168/F-273 row); the pre-decision flat-binding shape it replaced was never itself the subject of a separate decision to check for stragglers, and the one place its old field census is narrated (`01-construction-ownership.md:270`) is already correctly framed as a historical probe finding, not a current-state claim.
- **Size gate.** `.plan/measurements/budget-rebases.md`'s new section (+13 to +57 B Brotli, all fifteen rows, tightest slack 93 B against a 150 B budget) and `bench/size/measure.ts`'s five updated control baselines are internally consistent with the diff's own shape (64 identifier occurrences becoming qualified property reads) and were not re-measured independently — this is D-106's standing instrument, not something a DER pass re-derives, and no claim in the decision record depends on a number this pass could not otherwise check.

## Method

`npx just decisions` / `--retired` from the package directory, grepped for D-168/F-273. `git diff 7f6d1851..d51d59c6` per file (`.plan/contract/00-index.md`, `.plan/measurements/budget-rebases.md`, `.plan/plan.md`, `bench/size/measure.ts`, `src/kernel/kernel.ts`, `tests/kernel/kernel.browser.test.ts`) rather than the whole tree. `git show d51d59c6:packages/drag2/src/kernel/kernel.ts` used for source citations (rather than the live working tree) after a transient, non-self-inflicted modification appeared in the working copy mid-session — matching the shape of a mutation-testing probe against the retirement-ordering property described in `.plan/plan.md`'s 2026-09-02 entry ("a test asserting it would have passed against a record that was never dropped ... was written, run against that mutation, and deleted") — and cleared again before this report was finalized; `git status --porcelain` confirmed clean at the end of this session, and no change in this report's diff citations originates from that window. `npx just typecheck` and `npx vitest run tests/kernel/kernel.browser.test.ts` (171/171, including the new ordering witness) were run against the tree before that transient modification appeared. Full-file greps for `OperationRecord`, `ActivationRecord`, `clearOperationState`, `cancelRequest`, `pinned`, `retireAttempts`, `settlementInput`, every bare `let` name from the old flat-binding shape, and `operation =`/`activation =` assignment sites. LSP plugin — unavailable; not used. This pass's evidence is write/read-site enumeration by grep and direct reading against a specific two-record diff already known by line range from the decision record itself (§1, §2, §3, §9) — a bounded, already-identified file set for which symbol-level tooling would not have added discriminating power over what the decision record's own method note (§10) already established for the pre-image.