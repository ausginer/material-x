# D-168 integrity: no drift found across the kernel's tier, guard and budget boundaries

**Read at `d51d59c6`**, branch `drag2/fin-review`, baseline `7f6d1851`. Package coherence only — D-168's correctness against its own plan is another pass's subject and is not re-litigated here except where a claim in the decision record or `plan.md` is itself the thing under test (it is, per the task brief, for §4's truth-table, §5's reconstruction-fidelity and §7's budget figure).

**No production source was written.** Two verification builds were made in throwaway `git worktree`s at `d51d59c6` and `7f6d1851` (removed before finishing) so the byte and module-graph figures below are pinned to committed blobs, not the live working tree — which, during this pass, briefly carried another agent's in-flight mutation probe in `runPhysicalTeardown` (self-reverted; `git status` is clean now apart from that agent's own untracked report). Every citation in this report that touches exact source content was re-verified against `git show d51d59c6:./src/kernel/kernel.ts` or the pinned worktree after that was flagged, not against the live file alone.

## Scope

Checked, against current `src/kernel/kernel.ts` at `d51d59c6`: tier crossing of `OperationRecord`/`ActivationRecord` (task item 1), `phase` as sole discriminant (item 2), every `operation`→`identity` rename site (item 3), the three `if (lifetimes)`→`if (operation)` sites (item 4), `ActivationScope` reconstruction (item 5), decision/contract/coverage/size-control agreement (item 6), and the module graph (item 7). Out of scope per the brief: F-274, F-231, the class/factory question, the two pre-existing `prefer-destructuring` lint errors — none of D-168's diff touches their evidence.

## Findings

**None.** Every property the task asked me to verify directly against source holds, and every figure I re-measured from a pinned commit matches what `budget-rebases.md` and `plan.md` claim. This is a null result, not an absence of effort — the evidence trail is below so the consolidator can see what was actually checked rather than take the absence on faith.

## Verification trail

**1. Tier crossing — clean.** `OperationRecord` (kernel.ts:181) and `ActivationRecord` (kernel.ts:222) are unexported `type` aliases; `grep -rn "OperationRecord|ActivationRecord" src/ tests/` returns only their declaration and the two `let operation/activation` bindings, all in `kernel.ts`. `Kernel<Part, Activation>` (the exported type, line 250) and every `handle*`/`retireOperation`/`failOperation` signature take `OperationIdentity` (a pre-existing one-field frozen type from `frames.ts`) or nothing — never a record. `FailureCheckpoint.operation` (line 243) is typed `OperationIdentity`; `dispatchKernel(CANCEL, current.operation)` (line 774) passes the frame's identity field, not the record. No queue payload, signature or captured callback carries either record.

**2. `phase` sole discriminant — clean.** Neither record declares a `phase`-shaped field; the `ActivationRecord` doc comment states the reasoning explicitly ("Nullable rather than discriminated: `phase` on the frame is the lifecycle discriminant... one authority for a lifecycle position is the whole point"), and nothing in the diff adds a second one.

**3. `operation`→`identity` rename — every site checked, semantics unchanged.** Diffed each renamed guard against `7f6d1851`: `retireOperation` (`if (identity && current.operation !== identity)` — same comparison as the old `if (operation && current.operation !== operation)`, only the parameter name changed), `handleRelease`, `handleActivate`, `handleCancel`, `handleStartCommitted`, `handleErrorReported` (destructures `checkpoint.operation` as `identity`, guards `current.operation !== identity`, and its own `if (stage !== FAILURE_TERMINAL_CALLBACK && operation)` correctly refers to the closure-level record, not the renamed local — confirmed this is exactly the third `if (operation)` site, see item 4) and `failOperation` (`const identity = current.operation;` replacing a destructured `const { operation } = current;`, with `!operation` → `!identity` and the closure-level `cancelRequest` becoming `operation!.cancelRequest`). Every guard compares the same value against the same value; only the label changed, which is the stated point of the rename ("the file now spells the operation's _state_ and the operation's _token_ differently" — `plan.md`, 2026-09-02).

**4. `if (operation)` ≡ former `if (lifetimes)` at all three sites, including the un-named third — verified.** Old `if (lifetimes)` occurred at exactly two sites in `7f6d1851` (`git show 7f6d1851:./src/kernel/kernel.ts | grep -n "if (lifetimes)"` → lines 519, 592, both in the two retirement paths). The third site the decision alludes to but doesn't number is a compound guard, not a bare `if`: `if (stage !== FAILURE_TERMINAL_CALLBACK && lifetimes)` at old line 2208, inside `handleErrorReported`. All three are now `if (operation)` (current lines 553, 633) and the compound form at current line 2255. Truth-table equivalence: `mintOperation` (line 1011) constructs `operation` as one object literal with `lifetimes: createOperationLifetimes(notify)` inline and no field assigned outside that literal (confirmed by reading the full function, lines 993–1050); the assignment is atomic (either the whole literal lands or, on a thrown `createOperationLifetimes`, `operation` is untouched and stays whatever it was, i.e. `null` after a prior retirement). So a non-null `operation` with an absent `.lifetimes` is unrepresentable — TypeScript's non-optional `readonly lifetimes: OperationLifetimes` on the type is backed by an actual construction-order guarantee in the one function that assigns the variable, not merely by the type annotation.

**5. `ActivationScope` reconstruction — values and timing unchanged.** Diffed `acquireActivation` (old ~1193–1274, new 1244–1323) line by line: `boxPre` measured from the same source (`live.box` / old `source`), `acquireLift` called with the same five arguments in the same order, `activation = { originRect: rect, lift: session, visualSpace }` lands at the exact point the old three flat assignments (`originRect = rect; lift = session; visualSpace = above;`) did — immediately after `acquireLift` returns, before `presentation.use(session.dispose)`. The returned `ActivationScope` object literal is unchanged field-for-field; only `originRect` and `visualSpace` changed from bare-identifier reads to `activation.originRect` / `activation.visualSpace`, exactly as D-168 §5 and `plan.md` claim.

**6. Decisions/contracts/coverage/size controls — agree.** `.plan/contract/00-index.md`: D-168's row now reads `Implemented, 2026-09-02 (Remediation)` (was `Unimplemented (Remediation)`); the "Decisions not yet implemented" table's D-168 row is removed and replaced by a closing cycle-summary paragraph, consistent with the repository's witness-retirement convention used for D-166/D-155 before it. `bench/size/measure.ts`: only the `control` field changed on the five reachable rows (kernel root and the four free-drag compositions); every `budget` field is byte-identical to `7f6d1851` — confirmed by `git diff 7f6d1851..d51d59c6 -- bench/size/measure.ts`, which touches no `budget:` line. `tests/COVERAGE.md` is untouched by this diff and correctly so: it maps `05-lifecycle-invariants.md`'s numbered test-matrix rows to tests, and D-168 changes internal representation without adding, removing or renumbering a contract invariant (`phase` stays the one discriminant, per item 2) — the new retirement-ordering test (`tests/kernel/kernel.browser.test.ts`, "should still hold the operation while retirement runs") is a construction/type-establishment probe for an unnumbered property, not a new `I-nn` row, so its absence from `COVERAGE.md` is not a gap. `.plan/obligations.md` carries no D-168-tied `SC-*` row and needs none — D-168 owed a measurement, not a standing obligation, and the measurement is in `budget-rebases.md`.

**7. Module graph — unmoved, confirmed independently.** `git diff 7f6d1851..d51d59c6 --stat` shows the diff is confined to `kernel.ts`, `.plan/**` and `bench/size/measure.ts`'s `control` fields plus one test file — no new import statement anywhere. Module counts from a pinned-worktree measurement at `d51d59c6` (31/30/32/31/33/34/25/26/27/28/45/2/14/29/26 across the fifteen compositions) match `budget-rebases.md`'s claim exactly and match the count at `7f6d1851` row for row.

**Budget check (task's explicit ask).** Re-measured from clean pinned worktrees, immune to the parallel agent's live-tree mutation: at `7f6d1851`, `kernel.js` = 5,827 B Brotli against a 5,977 B budget → **150 B slack**, confirming the task brief's figure independently of both the decision's stale 246 B and the brief's assertion. At `d51d59c6`, `kernel.js` = 5,861 B Brotli, same 5,977 B budget → **116 B slack**, i.e. **+34 B spent**, matching `budget-rebases.md`'s table exactly. All fifteen `budget:` values in `bench/size/measure.ts` are byte-identical across the range (verified by diff, item 6 above) — only `control:` moved, on the five rows a kernel change reaches — so the ceiling was not loosened to make D-168 fit; the slack shrink is entirely the D-166 rebase plus D-168's own +34 B.

## Method

Direct source diffing (`git diff`/`git show` against both endpoints) for every claim about code content and ordering; two throwaway `git worktree` builds (`d51d59c6`, `7f6d1851`) run through `bench/size/measure.ts`'s own `measureAll()` for the size and module-graph figures, to avoid citing a live working tree that briefly carried another pass's instrumentation; `npx just test tests/kernel/kernel.browser.test.ts` run once for a sanity check (171/171 passing, including the new retirement-ordering witness) — not relied on for any finding, since this pass verifies coherence rather than the feature's own correctness.

LSP plugin - unavailable; not used: `ToolSearch` found no LSP tool in this session. Every check here was "does this exact text at this exact line agree with that exact text at that other line" across two commits, which `git diff`/`git show`/`grep`/`Read` answer directly.