# F-244–F-248 closure — code discipline review

**Commit read at:** `2919b975` (`drag2: finish the D-165 migration across the kernel behavior contract`), the sole commit in diff range `55a20d2f..2919b975` — confirmed with `git log --oneline 55a20d2f..2919b975` (one entry). Files touched: `packages/drag2/.plan/contract/00-index.md`, `packages/drag2/.plan/contract/02-kernel-behavior-contract.md`, `packages/drag2/.plan/plan.md`, `packages/drag2/src/sortable/spec.ts`.

**Scope.** Covered: (1) the `src/sortable/spec.ts` comment edit; (2) every strike-through, rewrite and new prose block in `02-kernel-behavior-contract.md`'s delta, checked against what F-244–F-248 (per `f236-f242-final-remediation-summary.md`) actually required, and against the file's own established strike/replace convention; (3) `00-index.md`'s five new ledger rows and the one-line D-85 row edit; (4) `plan.md`'s new entry, checked for duplication against the three preceding plan entries on the same recurrence (`bq6-bq9-d165-remediation-summary.md`'s entry at plan.md:2239, `f239-f240-disposition-claude.md`'s at plan.md:2255, `f236-f242-final-remediation-summary.md`'s at plan.md:2269). Not covered: correctness of the underlying D-165/D-85 technical claims (feature-proof/integrity territory), and the `00-index.md` row content beyond discipline (whether the claims are true is out of lens here).

This is a documentation-only commit (one source comment aside), so CONTRIBUTING.md Part II (runtime machinery) mostly does not engage; the applicable rules are Part I's "Comments and JSDoc" (→ `.agents/docs/documentation.md` §5) for the `spec.ts` line, and this repository's own `.plan/contract` strike/record conventions (referenced throughout `documentation.md` §5.3 and the prior rounds' findings) for the markdown files.

---

## 1. `src/sortable/spec.ts` — clean

The only change is line 424: `"the second half of the admission subject"` → `"a named member of the admission subject"`, inside an existing internal comment block. Confirmed comment-only: `git diff 55a20d2f..2919b975 -- packages/drag2/src/sortable/spec.ts` shows a single `-`/`+` pair with no other hunk in the file. No logic, type or structural change nearby (the surrounding block, `admit`'s box-resolution reasoning, is otherwise byte-identical). Proportionate to F-247's claim (the same wording class F-236 retired in `src/kernel/` survived one directory over) and consistent with §5.2's "argue for what is" register — the new wording states the current three-role shape without narrating that it used to say "half". No finding.

## 2. `02-kernel-behavior-contract.md` — one finding, otherwise clean

Traced every hunk against F-244/F-245/F-246/F-248's specific claims:

- `AdmissionSubject`'s doc comment, the D-59 verdict table, `ActivationScope.box`'s doc comment, the `visualSpace` doc-comment split, and the D-52 diagram's post-fence note are each a direct instance of the convention `.plan/contract/*.md` already uses elsewhere (strike inside prose or inside a doc comment, replacement text following) — matching F-248's specific repair (no more bare `// ~~declaration~~` fenced-block strikes) and F-245/F-246's specific repair (the table cell struck on the file's "six precedent instances" per the prior round's own finding; the previously-unstruck `admit` cell and `box` doc comment now struck like their neighbors). The volume of new strike-through markup matches the five sites F-244–F-248 named — no additional site was struck that wasn't already flagged, and no flagged site was left unstruck.
- The rewritten D-85 placement paragraph (**"What E-01 found was two policies, not two reads..."**) replaces a paragraph that was not merely stale but affirmatively false against the shipped tree (F-244's specific finding: `presentation.ts` documents a second ancestry walk while the old paragraph claimed "no second read left to disagree with"). The replacement is comparable in length to what it replaces, and every clause in it does discrimination work required to correct the false claim (why D-165 doesn't reopen E-01: the split E-01 found was a **policy** difference, not a **read-count** difference) — it does not restate reasoning already standing two paragraphs above it. Proportionate; not over-explained relative to the section's existing register, which already argues at this density elsewhere in the same subsection (three-count list, "live trap" framing, etc.).

**cleanup-1 (tier C).** The new sentence **"Read _the pair_ above as _the object form_ from here on"** (§Admission returns a subject, immediately after the D-165 paragraph) is a live inconsistency with the convention this same commit applies everywhere else in the file. Three uses of "the pair" stand unstruck in the bullets immediately above it — "the pair is the only way to say 'different'", "The pair with `box === visual` is legal, inert and pointless" — describing what is now a three-member object form. Rather than striking/replacing those uses (the mechanism this commit uses for every other terminology drift in the same file, including the adjacent D-59 verdict-table cell four lines above), the commit adds a forward-looking instruction telling the reader to silently reinterpret "the pair" as "the object form" from that point on. This is the same defect class the prior round closed as F-246 — a correction applied by judgement rather than by convention, landing unevenly within one commit — reproduced once more inside the very commit meant to finish the convention sweep. It is not narrating history (so §5.3's "someone must be told" test doesn't clearly indict it), and it does not misstate anything, so I record it as a discipline/consistency finding rather than a correctness one: a reader who stops at "the pair is the only way to say 'different'" reads a two-role claim with no local signal that the term has drifted, until three sentences later. Evidence: `packages/drag2/.plan/contract/02-kernel-behavior-contract.md`, §Admission returns a subject, the "One spelling per meaning" bullet and its neighbors versus the following D-165 paragraph.

No other prose additions in this file read as over-explained, narrating history, or duplicating standing reasoning.

## 3. `00-index.md` — clean

The five new ledger rows (F-244–F-248) match this table's own established density (compare the immediately preceding row, F-243, and F-238–F-242) — every row in this ledger carries a claim, a resolution and a "general shape" closing sentence; F-244–F-248 are not longer or more elaborate than their neighbors. The D-85 row's one-line edit (`§Why \`inheritedSpace\` is on the scope`→`§Why the inherited space is on the scope`) is a mechanical repointing to the renamed heading F-244 required, nothing more. No finding.

## 4. `plan.md` — one finding

**cleanup-2 (tier C).** The new entry's closing paragraph —

> **The recurrence is the finding worth keeping.** Three rounds, three sweeps, each scoped to where the previous finding's evidence happened to point: F-232 named two sites and left F-236 one member away; F-237 named three and left F-244, F-245 and F-246 on the same page; F-236 swept `src/kernel/` and left F-247 in `src/sortable/`. A defect that is a _class of wording_ has no failing state and no instrument, so its boundary has to be drawn from where the wording can live — the whole surface, the whole package — rather than from the citations that surfaced it.

— restates a point this same plan already made twice. The third preceding entry (`plan.md:2269`, "the remediation round's remaining five findings...") states, verbatim in substance, under its own F-236 paragraph: _"A finding's evidence names the sites its reporter read; a remediation scoped to that list passes over identical instances, so a defect that is a class of wording is swept by surface rather than by citation."_ And within this same commit, `00-index.md`'s new F-247 row closes with: _"a sweep bounded by where the last finding's evidence happened to point rather than by where the wording can live"_ — near-identical wording to the new plan.md sentence, landing twice in one commit. What is genuinely new in the paragraph is the specific three-round site enumeration (F-232→F-236, F-237→F-244/245/246, F-236→F-247), which is load-bearing synthesis not present in any prior entry — that part earns its place. The generic "class of wording has no failing state / instrument" framing does not; it is the third or fourth restatement of the same observation across this round's records, which is closer to narrative bloat than to the "recurrence is the finding worth keeping" the sentence itself claims to be isolating. Evidence: `packages/drag2/.plan/plan.md` lines 2273–2277 (F-236's prior statement) and 2286–2302 (the new entry), and `packages/drag2/.plan/contract/00-index.md`'s F-247 row (same commit).

**cleanup-3 (tier C, mechanical).** The new `plan.md` section is preceded by two consecutive blank lines before its `---` separator, the only such occurrence in the file — every other section boundary (checked across the whole file) uses exactly one blank line before `---`. Evidence: `sed -n '2278,2288p' packages/drag2/.plan/plan.md`; grep across the file confirms all other `---` separators are preceded by a single blank line. Cosmetic; not a discipline violation of any numbered rule, noted for completeness since it is a mechanical artifact of this commit's edit and would be a one-character fix.

---

## Summary

Two substantive tier-C discipline findings (`cleanup-1`, `cleanup-2`) and one mechanical note (`cleanup-3`); nothing tier A or B. The `spec.ts` comment change, the bulk of the `02-kernel-behavior-contract.md` rewrite, and all of `00-index.md`'s additions are proportionate to F-244–F-248 and consistent with this repository's `.plan/contract` conventions — checked explicitly, not assumed clean by default. The two substantive findings are both instances of the same underlying pattern this closure round exists to eliminate — a correction applied unevenly, and a synthesis restated rather than pointed to — recurring in miniature inside the commit meant to finish that elimination.