# `drag2` — probe 2: private kernel, private behavior runtime, contributed features

## Status

**Consolidated for an executable implementation probe.** This is the frozen construction model for `drag2`, revised after [review 1](../reviews/contract-probe-2/contract-probe-2-review-1.md) (answered in [`challenge-response.md`](challenge-response.md)), the React placeholder probe, [review 2](../reviews/contract-probe-2/contract-probe-2-review-2.md), [review 3](../reviews/contract-probe-2/contract-probe-2-review-3.md) and [review 4](../reviews/contract-probe-2/contract-probe-2-review-4.md), [review 5](../reviews/contract-probe-2/contract-probe-2-review-5.md) and [review 6](../reviews/contract-probe-2/contract-probe-2-review-6.md) — three pre-implementation consistency, correctness and cost passes.

**The signatures are compiled.** [`packages/drag/docs/contract-probe-2/contract.ts`](../../../../packages/drag/docs/contract-probe-2/contract.ts) holds the representative contracts, a reference sortable behavior implementing every seam, a driver for every seam (admission, activation, `moved`, action, release, settlement, arm, join), and `@ts-expect-error` assertions for **each tier-A claim that is expressible as a type error** — several, such as sealing and hold linearity, are sequencing rules no type can state. It is covered by `npx just typecheck` from `packages/drag`.

**It is a _type_ fixture, not an executable lifecycle reference.** It has no queue, no cancellation state machine, no failure checkpoint, no readiness watch and no landing completion dispatch; where a driver needs one it has an inert stub. At 2,300 lines it can look executable, so this is worth stating plainly: **typecheck cannot catch a lifecycle error there.** The executable cases live in the test matrix in [05](05-lifecycle-invariants.md) and belong to the implementation. **These documents are the source of truth; where the fixture disagrees, the fixture is the bug.**

[`../contract/`](../contract/) — probe 1 — remains the reference for **lifecycle invariants, edge cases and validated product requirements**. Its _construction_ model (a public `Kernel` object, one shared runtime the behavior extends in place, a behavior that calls `begin()`/`commit()` itself, `Pick<>` projections over an aggregate runtime type) is not carried forward.

`challenge-response.md` is **frozen provenance**. Its arguments are folded in here; where it and these documents differ, **these documents win**. It is amended only for factual errata, never to carry a contract change — otherwise it drifts into being a second live version of the contract. Documents 00–06 are the only contract set.

### Normative precedence and freeze

The set is read with this precedence:

1. **00–04 are normative contracts**: decisions, ownership, lifecycle SPI, feature composition and frame rules.
2. In **05**, the invariant table, open questions, measurements owed and test matrix are normative. The chronological finding narratives are rationale; an older finding paragraph cannot override a later decision or the contracts in 00–04.
3. **07 is a normative behavior contract** — free drag's public surface and decomposition (Phase 18). It is subordinate to 00–04: where it and 02 disagree about a **seam**, 02 wins; where it and 03 disagree about **composition**, 03 wins. What is free-drag-specific is decided there and nowhere else.
4. **06 is an illustrative execution trace.** It must agree with 02, but never overrides 02 when wording drifts.
5. `contract.ts` is a type fixture only. Review files and `challenge-response.md` are provenance only.

The architecture decisions are frozen for the implementation probe. A further contract change requires a failing executable lifecycle case that the frozen SPI cannot express; a prose-only review finding is not sufficient. Performance and bundle choices explicitly listed as measurements remain open until measured.

**Revised once and re-frozen — Phase 14, 2026-08-04.** The rule above worked as designed: three probes ([`probes/`](../probes/README.md)) produced failing executable cases, and the contract was revised **once** against all three together rather than patched three times. The revision is **D-32…D-35**, and the reasoning for treating it as one change — including two decisions that would have been made differently in isolation — is §[02](02-kernel-behavior-contract.md) §Where the four changes touch each other. Nothing in the revision retracts a Part I result; what it changes is four places the SPI could not express something, three of which were the sortable's shape written into the kernel.

Re-freezing restores this rule unchanged. It does not close the contract: a later phase that produces a failing executable case reopens it, and that is the system working.

Checkpoint C reviewed the revision **five times, ran one follow-up round, and is closed. The SPI is re-frozen, and no decision was reopened after pass 1.** The first two passes changed D-33's mechanism (a kernel-minted token became controller acknowledgement by request identity) and the honesty of what I-35 claims. The third ([`checkpoint-c-3.md`](../reviews/checkpoint-c/checkpoint-c-3.md)) changed **no** decision: it resolved two places where the same event had two normative readings, and made both report paths visible in the fixture.

The fourth ([`checkpoint-c-4.md`](../reviews/checkpoint-c/checkpoint-c-4.md)) changed no decision either, and found two blockers anyway. Both are the same species: a decision that was right in the abstract and **unfinished in the seams that carry it.** D-32 said a command reuses the pointer lifecycle, and the normative sortable seams then overwrote the command's destination at activation and re-resolved it from a pointer sample at release — so the one thing D-32 puts in the draft was destroyed twice. D-35 said the lift session is the sole transform writer between acquisition and the join, which was wider than the property needed, false against this document's own landing contract, and rated a tier the API does not provide.

Both are corrected in place — §The command destination and §The landing origin — and neither reopened a decision. **The lesson is worth keeping: a revision is not finished when the SPI compiles.** Three passes of review looked at the four decisions and none of them looked at what the reference behavior had to do differently underneath them.

The correction pass then found two more of the same kind — `release.effect` had to branch as well, and the armed readiness window has an interior where two synchronous acknowledgements could each dispatch.

The fifth ([`checkpoint-c-5.md`](../reviews/checkpoint-c/checkpoint-c-5.md)) found the same pattern **inside pass 4's own corrections**, and this is the part worth keeping. Pass 4 narrowed D-35's interval and downgraded its tier — correctly — and left the behavior holding `dispose()` on the kernel-owned lift session, so I-34 stayed breakable through a first-class method rather than through a documented residue. Pass 4 established that nothing may dispatch `READINESS_SETTLED` without first claiming the once-only latch — correctly — and applied it to the live armed path but not to arm's own dispatch of the copied early latch, so the race it closed in one window stayed open across the boundary to the other.

**Both corrections fixed the prose and left the type or the ordering.** That is a sharper lesson than "a revision is not finished when the SPI compiles": a correction is not finished when its _sentence_ is true. Where a rule names a capability, check the capability's type; where a rule names an order, check every site that performs it.

Three follow-ups (**C6-01…C6-03**, recorded in the contract without a separate review file) closed the checkpoint by applying that lesson to pass 5's own corrections: the projected `write` is retained and stays effective, so the blanket late-use-safe claim was narrowed and a **temporal** tier-C rule stated in its place — deliberately without a hot-path guard (§The temporal rule on `write`); publishing `rt.placeholder` and `rt.lift` created a clearing obligation and a failure path the fixture had not discharged; and one matrix row named a table position rather than the arrival rule. **Add a rule, and check what the rule now obliges.**

The re-freeze stands on that basis. What remains is not contract work: `src/` still implements the **pre-revision** SPI, so revised-runtime evidence belongs to Phases 15, 16 and 19–20, exactly as `plan.md` assigns it.

**Revised a second time — Revision 2, the API redesign, 2026-08-13.** Three API review rounds and five probes produced a redesign of the **consumer-facing** surface, recorded as **D-36…D-60** and owned by [`../reviews/api-reviews/api-review-final-summary.md`](../reviews/api-reviews/api-review-final-summary.md).

The freeze rule is discharged the same way Phase 14 discharged it, and it is worth being explicit about which decisions carry executable evidence and which do not, because the rule says a prose-only finding is not sufficient:

| Probe | Failing executable case | Decisions |
| --- | --- | --- |
| **A** — transaction bracket ([`../probes/api-4-transaction-bracket.md`](../probes/api-4-transaction-bracket.md)) | 21 browser failures against the floor-preserving variant; 27 of 33 statement-level liveness readings retire; a manufactured `panic()` producer shows teardown leaving the throwing stack | D-36, D-37, D-38, D-39 |
| **C1** — the commit window ([`../probes/api-2-commit-window.md`](../probes/api-2-commit-window.md)) | 3 of 5 authored commits land at the viewport origin while reporting `onFinish` once and `onError` zero times; the placeholder runs 30 px too tall for a whole drag | D-42, D-43 |
| **E** — input policy ([`../probes/api-3-input-policy.md`](../probes/api-3-input-policy.md)) | 8 of 10 cases release-blocking; `ArrowRight` in a nested text input produces a complete accepted reorder from one keystroke | D-46 |
| **api-1** — layout ([`../probes/api-1-box-anchor.md`](../probes/api-1-box-anchor.md)) | measured: no single-window rule reproduces the removed footprint in both nested cases | D-43 |

**D-40, D-41, D-44, D-45, D-47, D-48, D-50, D-54, D-55, D-56, D-57 and D-58 carry no failing executable case, and are not claimed to.** They are owner decisions about the shape of a public API, or derivations forced by one, and the freeze rule does not govern them: it governs _the architecture decisions frozen for the implementation probe_ — the SPI. D-41 is the one that comes closest to the line, because it retracts D-33, which **was** a Phase-14 SPI revision; it is admitted here as a **deletion**, and a deletion needs no case that the SPI cannot express.

**Two decisions cross the frozen SPI, and neither must be buried.** **D-59** changes a seam signature — `admit`'s return widens to carry the box element — and is discharged by api-1 through the chain written in its row: no single-window footprint rule works, so D-43 needs a pre-lift box, and every carrier other than admission's return contradicts H-2. **D-53** adds a member to the frozen SPI — a logical-liveness reader on `KernelHost` — so the freeze rule governs it in full, and probe A discharges it: the three `presentation.signal.aborted` sites are the failing executable case, and they exist _precisely because_ the SPI could not express the reading D-38 requires. Every other row leaves the SPI alone. **D-49, D-51 and D-52 are repairs rather than additions** — each closes a hole that the other Revision 2 decisions opened in a rule that already existed, and each was found by a reviewer rather than authored here: D-51 in particular closes an exception D-37 could not survive without, since a finite domain with an unstated exception is not finite.

Revision 2 does not reopen Part I. What it changes is who the library is for: Phase 14 was four places the SPI could not express something, and this is the consumer surface that sits on top of it.

**Reconciled — Revision 2.1, 2026-08-13.** The owner read the reconciled contracts and found **five owner-decision drifts**: places where Revision 2 recorded a decision the owner had already made, or dropped one, and the contracts followed the record rather than the decision. They are **D-61…D-65**, and they are corrections to _this document's_ bookkeeping, not new architecture.

| # | Drift | Restores | Corrects |
| --- | --- | --- | --- |
| D-61 | The supported **middle tier** — sortable feature/config-utility authoring — was collapsed into the kernel tier | owner final §11 ladder; [`api-review-3-summary.md`](../reviews/api-reviews/api-review-3-summary.md) §1 | D-45's closing sentence, D-47's ladder, 03 §Fragments are public, installers are opaque |
| D-62 | The public terminal channel is **`onEnd`**; the contracts still carry `onFinish`/`onCancel` | owner final §4; review 3 §11 | D-40's own row, 02 §Callbacks and §Settlement, 03 §The schema |
| D-63 | The **public custom landing runner** was to be removed; `landing({ run })` survives | [`api-review-1-summary.md`](../reviews/api-reviews/api-review-1-summary.md) §5, review 2, review 3 §10 | D-56's surviving factory list, 03 §`landing()`, the preserved-from-probe-1 row |
| D-64 | **Coarse consumer error codes** with detailed stages internal; the 13 `FAILURE_*` constants are still ordinary-tier public | review 3 §12 | D-48's entry-topology _rationale_, 03 §The export topology, §The public/internal boundary |
| D-65 | **`placeholder`** is the consumer callback slot, not `createPlaceholder` + `placeholderClassName` | review 3 §2, §4 | D-56's flat-key split |

**The freeze rule does not govern any of the five, and none is claimed to carry a failing executable case.** Four restore standing owner decisions about a public API and one is a naming inconsistency; the SPI is untouched — D-53 and D-59 remain Revision 2's only two crossings. What D-63 removes from the _sortable_ surface it keeps at the _kernel_ tier, which review 3 §10 states explicitly, so no seam changes shape.

**Two further owner decisions closed what the reconciliation left open: D-66 resolves Q-14, and D-67 replaces the zero-argument landing thunk with a contextual `duration({ distance, from, to })`.** Both are recorded in the same table. D-66 is the one to read carefully — it retracts a rule 02 states normatively, and it narrows D-23.

**Three of the five cost something, and the cost is recorded in the row rather than absorbed:** D-61 puts the installer authoring types under a versioning promise, D-64 removes thirteen constants a consumer could switch on, and D-65 removes the class-only placeholder convenience D-56 argued for by name. **One reveals a contradiction that is not resolved here** — D-62 §The unresolved arm, below, and open question Q-14.

**The compiled fixture now exists** — [`docs/revision/revision-2.ts`](../../docs/revision/revision-2.ts), enforced by `npx just typecheck` — and it earned itself immediately. **Three of its sixteen assertions were false**, each a contract claim TypeScript does not actually enforce: F-51 (bivariant callback slots, repaired by named aliases), F-52 (the zero-argument thunk is not deletable), F-53 (`Promise<void>` flows into a `void` position). **These are the first defects in either revision found by a machine rather than by a reader**, and all three had survived a reconciliation, an owner pass and a review round in prose.

**One further claim was disproved by measurement rather than by the compiler**: F-54, the identity argument for keeping `drag.js`. See [`measurements/error-identity.md`](../measurements/error-identity.md).

**Revised a third time — Revision 2.2, the kernel vocabulary pass, 2026-08-14. One decision, D-68.** The fixture's rewiring to `src/` produced its second machine-found defect, and it is a **surface** defect rather than a type-level one: `kernel.js` publishes no value a behavior must produce, so the tier D-47 exists to publish **cannot be constructed from its own entry** — in any authoring style. See F-59. D-68 settles the vocabulary D-47 §11 queued and L-14 booked as owed, and it changes no seam, no signature and no entry count.

**What this episode is evidence of** is recorded rather than smoothed over: Revision 2 read the _final summary_ closely and the three review summaries underneath it loosely, and four of the five drifts are decisions that live only in the earlier rounds. The final summary's §12 asks to reconcile "against the decisions above" — and "above" was never the whole owner record. A reconciliation pass has to be run against the decision _history_, not against its most recent digest.

## The model

| # | Claim |
| --- | --- |
| H-1 | The kernel is an **internal executor**. It owns machine state, queue, frame pair, attempts, lifetimes, cancellation and teardown privately. It is not a public authoring object. |
| H-2 | A behavior is instantiated once per controller and owns a **completely private runtime** captured by its callbacks. The kernel does not know, store, extend or type it. |
| H-3 | The behavior supplies a **frame part** and a fixed set of **direct lifecycle callbacks**. The kernel selects transitions, opens and commits frames, and calls those callbacks. The behavior never calls `begin()` or `commit()`. |
| H-4 | A **feature is a function factory** that may create its own private runtime and returns a construction-time **contribution object** of named callbacks plus metadata. |
| H-5 | The behavior assembles contributions **once** into direct slots and fixed pipelines. No descriptor is interpreted on a runtime path. |
| H-6 | There is **one physical frame**, composed kernel-first from independently owned parts. **No participant declares a concrete whole-frame type.** (The kernel's own private generic _is_ `KernelFrame & Part`; the claim is about who authors a concrete shape, not about the absence of any intersection.) |

## Verdict

The model holds. Its load-bearing property is H-3: once the kernel owns `begin()`/`commit()`, the _prepare → commit → post-commit effect_ discipline stops being prose the behavior must obey and becomes the shape of the callback contract.

That guarantee must be stated precisely, in three tiers. Conflating them overstates what the API delivers.

| Tier | What it means | Examples |
| --- | --- | --- |
| **A — Frame publication safety** | The violation does not compile, or is unexpressible | `prepare` never receives `current`; `effect` receives `Readonly` (top-level slots); the behavior cannot write kernel frame fields; there is no behavior-callable `commit()`; a post-commit failure has no way to express a revert; release and settlement cannot discard |
| **B — Kernel-enforced sequencing** | The kernel orders it; the behavior has no opportunity to get it wrong | motion closes after the `RELEASING` commit and before the final measurement; revalidate-then-commit runs unconditionally; a settlement scope is sealed before any gate is armed; `dispose()` is projected off the lifetimes handed to the behavior; a seam throw is classified **and stops the seam's success continuation** |
| **C — Discipline** | A contract rule a participant must obey; the API permits violation | `prepare` performs no externally visible mutation; part factories are deterministic; `resetFramePart` is exhaustive; the shallow-copy contract and the immutability of frame _referents_; the ordering rule inside an `effect` |

Tier C is not a defect of this model — probe 1 had the same rules with none of A or B. But the honest claim is _"frame publication became structural"_, not _"preparation became safe"_. See [05](05-lifecycle-invariants.md) for the per-invariant classification.

Concrete consequences of the model, none of which is offered as a headline metric:

- The kernel's frame slice is **seven fields it exclusively owns**. Settlement gates left the frame entirely for a kernel-private attempt.
- The behavior authors only its own frame part; it cannot mis-initialise kernel state because it cannot name it.
- Probe 1's open question **Q-5** (where the geometry cache lives) is answered by construction: inside the axis feature — `y()` or `xy()`, each holding its own instance of the shared `rect-index` cache. (Written as `vertical()` until Phase 17 renamed the axis features; see [03](03-feature-composition.md) §The export topology this requires.)
- Probe 1's pressure point **P-2** (a free drag must clamp before writing) is resolved at **no** hot-path cost — the move path spends the same three post-`MOVE` indirect calls probe 1 spent. That boundary is deliberate and narrow; the end-to-end count is M-1's job. **Tested by a second behavior at last** (13c P-1): the shape survives; the number is Phase 21's.
- Probe 1's **P-1** (no live policy seam) is resolved: policy updates are behavior actions and the kernel supplies the transition envelope. **Exercised** by 13c P-6 for the policy half. The _controlled position_ half was not resolved by it and is D-35's.

What this model does **not** improve: per-boundary revalidation inside a `prepare` (unchanged from probe 1 — one check, at the end), and the number of callbacks a behavior author implements (comparable to probe 1, not fewer).

**No hot-path or bundle number here is measured yet.** The four owed measurements are M-1…M-4 in [05](05-lifecycle-invariants.md) §Measurements (landed 2026-08-02); until they land, the performance and tree-shaking statements in these documents are design intent, not evidence.

## Artifacts

| # | Artifact | What it fixes |
| --- | --- | --- |
| 1 | [`01-construction-ownership.md`](01-construction-ownership.md) | `draggable()`, the two-phase handshake, who owns what, the privacy boundary, teardown |
| 2 | [`02-kernel-behavior-contract.md`](02-kernel-behavior-contract.md) | `BehaviorSpec`, the tri-phase transition, capabilities, gates, the action envelope, the settlement/landing protocol |
| 3 | [`03-feature-composition.md`](03-feature-composition.md) | Contributions, assembly, private feature runtimes, consumer-declared views, tree-shaking |
| 4 | [`04-frame-slicing.md`](04-frame-slicing.md) | Part composition, shape authority, copy/reset ownership, write protection |
| 5 | [`05-lifecycle-invariants.md`](05-lifecycle-invariants.md) | Invariants by tier, findings, open questions, test matrix |
| 6 | [`06-vertical-sortable-trace.md`](06-vertical-sortable-trace.md) | One complete downward reorder under controlled React, with hot-path accounting |
| 7 | [`07-free-drag-contract.md`](07-free-drag-contract.md) | Free drag's public surface, its decomposition into config slots and capabilities, its seam mapping, the terminal-barrier enumeration and Phase 19's acceptance criteria (D-69…D-76) |
| — | [`contract.ts`](../../../../packages/drag/docs/contract-probe-2/contract.ts) | The compiled fixture: representative contracts, a reference behavior, the seam drivers, and `@ts-expect-error` assertions for every tier-A claim |
| — | [`challenge-response.md`](challenge-response.md) | Provenance: the review-1 answers these documents fold in |

Read 1 → 2 → 4 for the execution model; 3 for composition; 6 to see it run; 7 for the second behavior. Read `contract.ts` when a signature in the prose looks ambiguous — it is the version that has to compile.

## Preserved from probe 1

Inputs, not re-derivations.

| Finding | Discharged in |
| --- | --- |
| FIFO run-to-completion dispatch and defined reentrancy | [02](02-kernel-behavior-contract.md) §Queue |
| Explicit transition and commit boundaries | [02](02-kernel-behavior-contract.md) §The tri-phase transition — tier A/B, D-3 |
| Stale async continuation rejection (double validation) | [02](02-kernel-behavior-contract.md) §Attempts |
| ~~Synchronous and terminal~~ **Immediate and terminal** `destroy()` | [01](01-construction-ownership.md) §Teardown. **Revision 2, D-36:** terminal is preserved and strengthened — the logical latch is set on the closing statement rather than at the end of a seven-step sequence. Synchronous is preserved only outside a reentrant transaction. |
| ~~Independent readiness and landing gates~~ **One landing gate** | [02](02-kernel-behavior-contract.md) §The settlement gate — requested, sealed, then armed, D-7. **Revision 2, D-41:** independence was the property that let readiness and landing overlap; with readiness deleted there is nothing to be independent of. Probe 1's underlying requirement — that absent landing costs no fake asynchronous work — is unaffected, since it was always the _default-open_ rule, not the gate count. |
| No fake asynchronous work when landing is absent | Gates default open; absence of a `landing()` feature holds **no landing gate**. Readiness is unaffected, and same-drain finalization requires neither gate held (I-9). |
| Layout displacement is not a lifecycle gate | [03](03-feature-composition.md) — a displacement hook cannot reach `SettlementScope` |
| Release stabilizes motion before producing exactly one proposal | [02](02-kernel-behavior-contract.md) §Release — tier B, D-6 |
| Identity-based collection reconciliation | [03](03-feature-composition.md) §The collection model |
| Nearest-centre insertion with the placeholder as incumbent | [03](03-feature-composition.md) §`y()` and §`xy()` |
| Numeric spatial attempt identity where sufficient | [02](02-kernel-behavior-contract.md) §Attempts — behavior-private, D-11 |
| Correct cleanup during long-running landing | [05](05-lifecycle-invariants.md) I-7 |
| Optional feature code remains tree-shakeable | [03](03-feature-composition.md) §Tree-shaking |
| Custom spring landing runner without a kernel change | [02](02-kernel-behavior-contract.md) §Landing — the kernel never names a runner type. **Revision 2.1, D-63: preserved at the kernel tier only.** The property was always about the _kernel_ not needing to change, and it is untouched; what is withdrawn is the **sortable's public `landing({ run })` slot**. A spring is still authorable by a kernel-tier behavior, and review 3 §10 says so explicitly. |

## Decision ledger

`D-n` cross-references probe 1's `C-n` where the two models disagree.

| ID | Decision | Why | vs probe 1 |
| --- | --- | --- | --- |
| D-1 | `draggable(root, behavior)` uses a **two-phase handshake**: `behavior(host)` returns `{ spec, controller }`; the kernel then arms ingress. No `install()` side effect. | Makes "no input before install returns" unexpressible rather than a rule. | replaces C-1 |
| D-2 | The kernel is **not a value the behavior holds**. It hands out a small `KernelHost` at construction and per-seam arguments at call time. No exported `Kernel` or `KernelRuntime` type. | H-1. Six of probe 1's twelve `Kernel` members existed only because the behavior drove transitions. | replaces C-3 |
| D-3 | Every transactional seam is a **`Transition`**: `prepare(draft, capability) → Prepared \| null`, `effect(current, prepared, capability)`, optional `rollback(prepared)`. The kernel commits between prepare and effect and revalidates first. One shared **core returning a `SeamOutcome`**, and a **per-seam driver** that reads it: action discards normally, activation discard retires _but activation failure does not_, release and settlement cannot discard at all. | H-3. `Prepared` is the designated channel for staged state, so a discarded transition never has to have touched the private runtime. A `boolean` core conflated _discarded_ with _failed_, which let every seam continue success work after a classified failure (D-23). | new |
| D-4 | The behavior's runtime is **captured by closures**, not stored by the kernel. | H-2 verbatim. Alternative recorded as F-4. | replaces C-3 |
| D-5 | `admit(event, draft)` returns **the element to lift**, or `null`. | The kernel gets the one thing it needs from admission and nothing else. `item` is behavior state. | narrows C-4 |
| D-6 | Release is **two commits**: the kernel commits `RELEASING` and the release point, _then_ closes motion ingress, _then_ opens the transition in which the behavior resolves the final insertion and builds the proposal. | One extra frame copy per `pointerup`. Committing first means no irreversible physical action ever occurs while the committed frame describes a state that action has invalidated. | keeps C-4's ordering; the kernel now owns it |
| D-7 | Settlement gates default **open**, are **requested** during `settlement.effect`, and live on a **kernel-private settlement attempt** that is sealed when that callback returns and only then **armed**. Each may be requested at most once. **Narrowed to one gate by D-41 (Revision 2):** readiness is deleted, so the plan holds landing alone. Request-seal-arm survives unchanged — it is what makes a synchronous `done()` from a `duration: 0` runner safe, and that has nothing to do with readiness. | Encodes "no fake asynchronous work when landing is absent" as the default path, while keeping readiness independent of landing. Request-seal-arm is what makes a synchronous `done()` from a `duration: 0` runner safe. Gate state was never observable or transactional; putting it on the attempt makes staleness handling free. | replaces C-9's mechanism, keeps its semantics |
| D-8 | The **behavior** writes the lift transform, through `lift.write`. | Resolves P-2 at no cost: the same three post-`MOVE` indirect calls either way. **Extended by D-35 (Checkpoint C, C4-05):** `moved` is the hot path but is no longer the only site. A controlled position writes from an `action.effect` and the sortable's `release.effect` writes the final release sample — both through the same `lift.write`, which is why D-35 records inside the session rather than at one call site. Read "inside `moved`" as "through the session", or this row contradicts D-35. | replaces C-4 note |
| D-9 | ~~`Behavior<Controller, Part>`~~ — **superseded twice.** D-30 made the public value opaque `Behavior<Controller>`, and D-34 (Checkpoint C, C4-05) added `Activation`; **both `Part` and `Activation` are erased at the brand**. The three-parameter form survives on the **internal construction types** — `BehaviorFactory` and `BehaviorInstall` — neither of which a consumer names. (This said "only on `BehaviorFactory`" until Checkpoint C, C5-05; 01 defines both.) What this row still decides is the disjointness rule, unchanged. The kernel is internal, so a type parameter costs no public signature noise. **Disjointness from the kernel slice is enforced twice**: `FramePartOf<Part>` at the authoring boundary, `validateFramePart` in production at `arm()`. | Deletes probe 1's cast-at-the-boundary compromise. `Object.assign`'s `T & U` typing composes the frame with no cast. `Part extends object` alone does not forbid a mutable `phase`, so the type parameter cannot carry the guarantee by itself. | replaces C-3's compromise |
| D-10 | **The behavior owns the frame's domain shape.** A feature _may_ contribute an opaque frame part, but the prepare-phase seam it would need is **not specified and not implemented**. | The mechanism is recorded because prohibiting it was wrong; building it for no consumer would be the speculative generality the brief forbids. | new |
| D-11 | Attempt identity: object identity for resolution/readiness/settlement (kernel-private), a monotonic `number` for the coalesced spatial attempt (behavior-private). **D-33's extension is withdrawn with it (D-41, Revision 2): there is no readiness acknowledgement, so attempt identity is kernel-private again without qualification — which is what this row said before Phase 14.** ~~**Extended by D-33 (Checkpoint C, C4-05): the _readiness acknowledgement_ identity is not kernel-private.**~~ It is the `ReorderRequest` the behavior published, held by the consumer, and compared by the behavior in `controller.ready(request)`. The kernel deliberately does not know what a request is — `KernelHost.presentationCommitted()` carries none — so cross-operation safety on that path is a **behavior** obligation, not a kernel one (I-35). The _hold, deadline and settle latch_ stay kernel-private, which is what this row still decides. | keeps C-8 |
| D-12 | ~~A feature is `(context) => Contribution`.~~ — **superseded by D-45 (Revision 2).** A fragment is a plain partial config object; the merge is over config slots and the installers it names are invoked after it. What this row still decides survives verbatim: the result is read once at assembly and **dropped**, and it is validatable by key. What changes is that the object is authored by the consumer's own literal rather than returned from a branded factory. | H-4/H-5. Declarative, validatable by key. | replaces C-5; superseded by D-45 |
| D-13 | Features declare **their own structural view types**. No feature imports `Pick<SortableRuntime, …>`. | Inverts the dependency: the consumer states its requirement. | replaces C-3's projections |
| D-14 | The eight-phase vocabulary and the five resource lifetimes are kept verbatim. | Nothing in this model touched them. | keeps C-15 |
| D-15 | The physical frame is composed **kernel-first from parts**: the kernel writes its own literal, then folds the behavior's part. Two sources, no fold over feature parts (D-10). The behavior authors `createFramePart()` / `resetFramePart()` only. | The behavior can no longer mis-initialise kernel fields, because it cannot name them. Construction-time hidden-class transitions are paid twice per controller and are not a measured cost. | new |
| D-16 | ~~The landing target is **provisional while readiness is pending**~~ — **narrowed by D-41 (Revision 2) to: measured once, authoritatively, then pinned at the join.** The serial order guarantees the authored DOM is final before the measurement is taken, so there is no provisional interval, no `authoredReady`, and no readiness-specific retarget. Two clauses survive and are the ones that were load-bearing: **the kernel performs the final pin at the join** before releasing presentation, and **whether to re-anchor follows the recovery**. | Evidenced by the React probe. Correctness comes from the final pin, not from every runner being retargetable. Probe C1 then showed the provisional half was worse than unnecessary: `authoredReady` is false at `armSettlement` **by construction**, so the provisional target was stale in all five commit strategies including the two that otherwise hold. | new; narrowed by D-41 |
| D-17 | **Pointer capture is kernel-owned**, acquired on `root` at activation and released with the motion lifetime. | The kernel already owns pointer identity, ingress, the motion lifetime, release ordering and teardown. This makes `activation.prepare` externally inert for the reference behavior. | new |
| D-18 | The resolution choice is a **staged `ResolutionCommand`**, not a `ResolutionGate` with `open()`/`skip()`. | Makes "exactly one choice, exactly once" structural. A capability with two methods and no state machine left zero calls, duplicate calls and `open` -then- `skip` all undefined. | new |
| D-19 | **Geometry is a paired contribution** — `resolve` / `invalidate` / `retire` — flattened into direct slots at assembly. | The behavior owns the events that make geometry stale; the feature owns the cache. Contributing only `resolve` forced behavior code to reach into a private rect index, which could not compile. | new |
| D-20 | Feature seams take **frame state and runtime state as separate arguments**. The runtime argument is one `PresentationView` per operation. | A single view carrying a `current` frame property was not constructible without leaking the kernel's swappable references. Separating them also removes a duplicated `pointerY`. | narrows D-13 |
| D-21 | `LifetimeScope = Readonly<Pick<Lifetime, 'signal' \| 'use' \| 'useWhile'>>` is what activation hands the behavior. | A type-level projection costs nothing — the same physical object is passed — and it converts I-11 from "the behavior should not close motion" into "it cannot". | new |
| D-22 | The v1 authoring surface is **unsupported-but-possible**, not prevented. Seam, spec and host types are unexported; `SortableFeature` is exported because authoring has to typecheck at all. | Named because the brief asks for the boundary. TypeScript accepts a structurally matching literal regardless of whether a type name is exported, so "only built-ins may author" was never enforceable — and it is not a correctness boundary, since nothing in the kernel depends on provenance. | new |
| D-23 | The seam core returns a **`SeamOutcome`** (discarded / invalidated / prepare-failed / committed / effect-failed), and **a classified failure stops incompatible continuation** — no retirement, no consumer invocation, no gate arming, ~~no terminal callback~~ — **the last clause is retracted by D-66 (Revision 2.1)**: a terminal is **disposition**, not continuation, and a started operation publishes exactly one whatever happened. Everything else on this list stands. | Catching a throw is only half a failure model. The checkpoint is queued, so the window between the throw and the checkpoint is exactly where the old boolean let success work run. | new |
| D-24 | `SettlementInput` is **discriminated and exhaustive over all five cases** — fulfilled / rejected / skipped / canceled / failed — with a stated mapping to outcome, recovery, domain result and callback. | Five _open numeric_ statuses with no mapping was the defect; the number of cases was not. Removing canceled/failed made the kernel unable to produce behavior-owned terminal state at all (D-28). | new |
| D-25 | The collection action **never discards**: it stages `cancelReason` and `effect` publishes first, then cancels. | An invalid collection ends the current drag; it must not throw away the consumer's update, which is what `host.cancel()` + `null` did. | new |
| D-26 | **Post-callback revalidation** at every reentrancy-capable boundary, with stale-return disposal for anything the callback _returns_. | Reserve-before-call protects resources that already exist. A `LandingStart` that destroys the controller and then returns a live handle leaked it; an `admit` resolver that destroys let the listener mint an operation on a terminal controller. | new |
| D-27 | One canonical `movePlaceholder()`, anchored on `insertion.after` with an append fallback, **inert when the placeholder is already in position**, and **refusing an anchor outside the placeholder's own container**; `homeInsertion` carries real identity neighbours. | `before?.after(…)` is a silent no-op for a start gap. `Node.before()`/`append()` on an already-correct position is a remove-and-reinsert that resets CSS transitions and forces layout, and release invokes the helper unconditionally. `before()`/`after()` are relative to the _anchor_, so an item the consumer reparented mid-drag does not fail the write — it moves the placeholder into the other container, taking the drag's layout footprint out of the list. Every caller reaches the writer with an insertion built from a snapshot that may be older than the DOM (spatial move, release write, home recovery), so the refusal belongs in the writer and classifies at each caller's own stage — `PLACEHOLDER_MOVE`, `RELEASE`, `LANDING_TARGET`. Destination recovery does not use the writer and already tests sibling identity before re-anchoring. | new |
| D-28 | **Every classification entrypoint latches.** A kernel-private `seamFailureRequested` flag makes `host.fail` indistinguishable from a throw at the driver boundary; `armSettlement` returns `ARM_ARMED`/`ARM_STALE`/`ARM_FAILED`; landing completion is once-only. | D-23 covered throws only. The checkpoint is _queued_, so the window before it applies is precisely what has to be closed — and it was still open for explicit `host.fail`, for landing-create failure, and for a synchronous runner `fail()`. | extends D-23 |
| D-29 | **Teardown is total.** Each frame reset is individually wrapped and ingress abort runs from a `finally`. **Rescoped by D-36 (Revision 2):** totality is a property of the teardown _sequence_, wherever it runs, not of the stack that called `destroy()`. When teardown defers to the transaction boundary the same seven steps run with the same wrapping; what no longer holds is that they have run by the time `destroy()` returns. | `resetFramePart` is behavior code the API permits to throw; an unwrapped throw made `destroy()` non-terminal against I-6. | new |
| D-30 | ~~The **feature value is opaque** — an unexported `unique symbol` brand.~~ — **retracted by D-45 (Revision 2).** The second clause stands: `FailureStage` and `DOMRealm` are public because public types structurally depend on them. The brand does not. D-45 finds the coherent third state this row said did not exist — **a public stable schema whose slot values are opaque** — so opacity moves from the record to the installer inside it, and third-party authoring of a _config_ becomes ordinary object literal syntax while authoring an _installer_ still requires the kernel entrypoint (D-47). | "Public stable type whose full structure is internal and unstable" is not a coherent third state. Branding closes the world for real; exporting the genuinely-needed types stops the leak the other way. | replaces D-22; retracted by D-45 |
| D-31 | The public proposal and results are **narrowed unions with string discriminants**, carrying version, identity neighbours, rejection reason and cancellation stage. | Probe 1's preserved product contract. Discrimination must not require an internal outcome constant. | new |
| D-32 | Discrete input is a **second admission member**, `command: { types, admit(event, draft) }`, not an intent protocol. The kernel binds the declared types on `root` inside its own ingress abort; the member answers feasibility synchronously in the listener, writes its state into the open draft and returns the element to lift or `null`. A non-null return mints a **pointerless** operation — `pointerId === -1`, no pointer listeners, no capture — which the kernel activates and releases in one slot. `PENDING` is redefined as _activation not yet committed_. | The 13a gap was never "a behavior needs to request a transition"; it was "a behavior needs to be asked a question before anything is queued". A second admission is the whole requirement, and it adds **no** `KernelHost` member, no phase and no frame field. Carrying a staged `Prepared` from the listener — 13a's candidate — would have broken §The staged value never outlives its transaction and forced a second staged type parameter; the draft already carries it. | new |
| D-33 | **Retracted by D-41 (Revision 2). The whole protocol is deleted, not amended** — `ReorderResolution.accept({ presentation: true })`, `controller.ready(request)`, `KernelHost.presentationCommitted()`, `rt.pendingRequest`, the acknowledgement deadline and the four-row invalid-acknowledgement matrix all go. The reasoning below was sound and its premise is gone: D-33 solved _how a consumer acknowledges a render that happens after the resolution returns_, and the serial commit removes that window by having the resolution not return until the render is done. Kept in full because it is the best record of why per-operation identity on a controller method is hard, which D-47's kernel surface will meet again. ~~The authored presentation is **declared in the resolution and acknowledged through the controller**~~: `ReorderResolution.accept({ presentation: true })`, then `controller.ready(request)`, compared by **object identity** against the request the behavior published from `release.effect`. `PreparedSettlement` carries a boolean, `holdForReadiness()` takes nothing, and `KernelHost` gains `presentationCommitted()`. An acknowledgement arriving before the settlement exists is latched on the resolution attempt, and discarded at **seal** — with a report — if the gate plan turns out to hold no readiness. Every acknowledgement that contradicts its own resolution, early or late, is _reported and dropped_, never silently ignored. The declaration is **opt-in**, and omitting it is tier-C discipline rather than a caught error. | Probe 13b's candidate C-3, whose only objection was that a controller method has no per-operation identity — which the **request** already supplies: it is public, per-operation, and in the consumer's hand before the render it acknowledges, because it is what asked for the render. That ordering is why C-2's kernel-minted token failed: a capability created at arm time is younger than a synchronous commit. No settlement primitive crosses the public boundary, nothing is allocated on the path, and `effect` still returns `void`, so the render/landing overlap survives structurally. | replaces the `presentationReady` promise |
| D-34 | `BehaviorSpec<Part, Activation extends {} = true>` — the activation seam's staged type is the **behavior's** choice. The sortable behavior declares `HTMLElement`; a behavior that stages nothing returns `true`. | `Transition` already defaults `Prepared` to `true`; it was `BehaviorSpec` pinning it to `HTMLElement` that made a second behavior return an element it does not own, with `effect` obliged to ignore it. The kernel still treats the staged value as `{}` and drops it, so this is a one-parameter change with a wide re-verification surface and no runtime cost. | new |
| D-35 | `LandingContext.from` is **the delta the lift session last rendered**, read from the session the kernel owns — not `pointerX - originX`. **From acquisition until `from` is sampled**, conforming behavior rendering goes through `write(x, y)`; after that the landing runner is the deliberate writer until `destroy()`. `compose(x, y)` composes and records nothing. | The pointer form is true for exactly one behavior. Any behavior that constrains, clamps, snaps or externally drives its visual — and every pointerless operation under D-32 — starts its landing from a position the visual is not at. Recording inside a kernel-owned method beats the `renderedDelta(current)` seam it replaces: two scalar writes and no seam, writes that do not come from `moved` are still tracked, and no behavior has to implement anything. **Narrowed at Checkpoint C, C4-02**: the interval ends at the sample, not at the join, and "renders only through `write`" is tier-C discipline (I-34). | new |

### Revision 2 — the API redesign (D-36…D-60)

D-61…D-67 follow in §Revision 2.1 — five drift corrections and two owner decisions closing what the reconciliation surfaced.

| ID | Decision | Why | Supersedes |
| --- | --- | --- | --- |
| D-36 | **Logical closure and physical teardown are separate events.** `destroy()` closes the controller logically and immediately — every guard fails from that statement onward — and if synchronous library execution is in progress, **physical teardown runs at the boundary of the outermost library transaction**, not on the closing stack. Outside a reentrant transaction the two still coincide. `destroy()` returns `Promise<void>`, settling once, after physical teardown. **`panic()` follows the same model**, and the order becomes _close → report → teardown_. | Probe A: `panic` is reached from `drain`'s `catch` **after** the loop has exited, so the deferral window is one stack frame wide and its only statement is `report`, which touches no library state. Observed as teardown order flipping `['retire','report']` → `['report','retire']`. The bracket is what lets 27 of 33 statement-level liveness readings retire (D-37): a guard exists to stop work that would outlive the close, and work that is undone at the boundary has no consequence to stop. | narrows D-29's totality to the deferred boundary; reverses 01 §Teardown's "physical release completes before it returns" |
| D-37 | **The liveness domain is finite and API-shaped.** After logical closure the library must not (a) invoke a **declared consumer slot**, (b) **admit** another operation, or (c) **publish** another lifecycle or domain event. Internal work already inside the current transaction may finish, provided it survives the boundary in no observable form. The whole-program ceilings — "reads no further geometry", "no further consumer-reachable read" — are **withdrawn**, and the consumer reach/stretch analysis is **retired as a proof domain**. | The old quantifier ranged over _every overridable member of every consumer-owned node_, which 05 itself admits "cannot be enumerated" — an obligation no implementation can discharge and no review can check. Probe A shows why the replacement is not merely weaker: a bracket makes an act consequence-free by **undoing** it, and for a call the consequence _is_ the call, so exactly the declared-slot invocations survive as irreducible residue. The quantifier goes from undischargeable to a finite list the export table already enumerates. | retracts I-36's ceiling register and the 62-row stretch table; retracts README §Terminal barrier's published ceiling |
| D-38 | **No physical-teardown observation may answer a liveness question.** An aborted resource or presentation signal lags logical closure once teardown defers. Liveness is read from the logical latch or from transaction validity, never from `signal.aborted`, a disposed session, or a detached node. | Probe A: the only three failures the bracket caused _on its own_ were `presentation.signal.aborted` used as a liveness reading. Those sites were chosen deliberately because the signal is **strictly stronger** than the closed latch — it also catches a kernel-internal panic. Under deferral the same property makes it strictly **weaker**, because it now lags the close it is standing in for. A reading that was correct for a stated reason became wrong for that same reason. | new — recorded as I-37 |
| D-39 | **A prepared-but-unadopted placeholder must be rolled back.** The sortable's `activation.rollback` is **required**, not vacuous: it undoes library-authored attributes, styles and state written onto a consumer-owned element during `prepare`. | The residue does not dissolve under deferred teardown, which an earlier reading of this revision claimed. `prepare` completes but is never **adopted** — `preparationValid()` returns false and the seam reports `SEAM_INVALIDATED` — so the disposer registered in `effect` never runs and never becomes responsible. Deferral changes when teardown happens, not whether adoption occurred. This is a **local acquisition property** with an existing mechanism; it is not a reason to reinstate statement-level liveness. | reverses 06 §"`activation.rollback` is unnecessary"; corrects I-17's "vacuous for the sortable behavior" |
| D-40 | **One `canceled` terminal. No `aborted`.** **The `onEnd` union has four arms — `accepted`, `noop`, `rejected`, `canceled`** — and any five-arm `EndResult` carrying `aborted` in an earlier redesign draft is superseded here. **`onEnd` is the callback's name as well as the union's, and this row is where that was written down and then not carried into any other document — corrected by D-62.** The four arms are the existing `ReorderTransactionResult`, unchanged. **What this row did not settle is what the fifth arm was _for_: see §The unresolved arm and Q-14.** **Implementation consequence, implied by this row rather than stated by it:** consuming a late settlement safely requires the resolution attempt's `catch` to outlive the attempt's currency, so the abort guard cannot key off `completed` alone. Probe A verified the behavior; the lifetime change is what delivers it. `canceled` says _the drag operation_ was abandoned; it has never promised that consumer side effects were rolled back, and after release it must not be read as one. A settlement arriving from an abandoned resolver is **consumed safely** — no unhandled rejection, no second terminal, no revival. `onError` remains the orthogonal diagnostic channel. | A second terminal name would encode _provenance_, not a different consumer obligation: in both cases the consumer must not assume its own started work was undone, because the library never had a way to undo it. Probe A verified the abandonment path reaches one terminal and consumes the late rejection with a real `unhandledrejection` listener attached. Splitting the name would oblige every consumer to handle two results that call for the same code. | closes the C-2 proposal to add `aborted`; qualifies I-31 |
| D-41 | **The authored commit is serial, and the readiness gate is deleted.** The order is _release → freeze proposal → `onReorder` → authored commit → consumer resolution → restore library presentation invariants → authoritative landing measurement → landing → terminal_. `controller.ready(request)`, `ResolutionOptions`, `KernelHost.presentationCommitted()`, `rt.pendingRequest`, `authoredReady`, the acknowledgement deadline and the readiness-specific retarget lifecycle are **all removed**. Settlement holds **one** gate, landing. A framework commit barrier is integration code, not a drag protocol. | The serial order leaves the readiness hold **no producer**: a consumer that must render before the landing measurement awaits its own commit inside `onReorder`, which is what a Promise-returning resolver already expresses. D-33 existed to let the resolution return _before_ the render and be acknowledged after; deleting that window deletes the protocol, the identity comparison it needed, and the four-row invalid-acknowledgement matrix underneath it. It also dissolves probe C1's finding that the provisional landing target is stale in **all five** commit cases — with one measurement, taken authoritatively, there is no provisional target to go stale. | retracts D-33 and I-35; narrows D-7 to a single gate and D-16 to _measure once, pin at the join_; resolves F-46 and F-6's residue by deletion |
| D-42 | **Destructive rerenders during an active operation are out of contract, and the precondition is checked.** Supported authored commits move existing item nodes and leave the drag-owned DOM intact — an append loop, keyed or morphdom-style movement. `replaceChildren`, `innerHTML` rebuilds, and replacing the container are unsupported. At the authoritative landing measurement the library verifies **O(1)** that the placeholder is still connected and still a sibling of the item; a failed check **reports** through `onError` and lands from the unrepaired position. Nothing is recovered. | Probe C1: three of five commit strategies produce a landing target of `{-50,-100}` — a detached placeholder measures `0×0` at the viewport origin — and the row visibly travels to `(0,0)` over 12 frames before teleporting back. **All five report `onEnd` once and `onError` zero times** (probe C1 measured `onFinish`; D-62 renamed the channel), so the worst integration bug in the package is also its most silent. The check is two reads and is not renderer detection: it validates a precondition the landing measurement already depends on. Recovery is refused separately — C1 shows `ReorderRequest.before`/`after` survive in 4 of 5 cases, so recovery is _possible_, and buying it would mean relaxing D-27's cross-container refusal for a case now outside the contract. | new — F-49 |
| D-43 | **`box()` and `visual()` are separate, and the footprint needs two measurement windows.** `visual` is the node faithfully lifted; `box` is the geometry source; `box(item) = visual(item)` by default. The removed footprint's **height** is **`boxPre.height − boxPost`** when `box` and `visual` differ, measured before the lift and again after the visual leaves flow, and **`boxPre.height` alone when `box === visual`** — corrected during implementation (F-55), because a lifted element's own offset box does not change across `acquireLift`, so the subtraction yields `0` rather than its height in the default composition. Its **width is `boxPre.width` on every composition** — **corrected again at application review 1 (F-58)**: the subtraction measures a _collapse_, which is a scalar on the flow axis, and a box that surrenders no cross extent made it `width: 0px` on every composed `box`. `item.after(placeholder)` and the copied `slot` attribute both stay. Scope limits are stated positively: visual order must follow DOM order; rule-placed layouts are unsupported; a composed `box !== visual` is supported **with `y()` only**, and in grid `box === visual` is required. | api-1 measured it: with a sibling remaining in the box, `boxPre 62 − boxPost 32 = 30`, and the list collapsed by exactly 30 — while `box` (62) and `visual` (60) are both wrong, in different directions in different cases. Probe C1 reproduced it live and found the shipped `visual()` sizing the placeholder from `visual.offsetHeight`, running the list **30 px too tall for an entire drag**. A running translate corrupts the box's top by 60 px and its height by 0, so both windows must be offset-box reads. The timing is free: `acquireLift` and the activation seam are already 40 lines apart. | corrects 03 §`placeholder()`'s single-window "sized from the visual's offset box"; extends D-27's anchoring rationale |
| D-44 | **Collection delivery is a pull source plus a payload-free invalidation.** `sortable({ items: () => … })` supplies the current committed collection; `controller.invalidate()` says the committed presentation or data may have changed. `updateItems(payload)` is **removed**, and no `itemCount()`/`itemAt()` scanning protocol replaces it. **Array identity is the structural-change signal**: the same identity invalidates geometry only, a new identity triggers snapshot, reconcile and geometry invalidation. The library's shallow copy happens **only** on the structural branch. In-place mutation of the same array is outside the contract. | The thunk was called once at construction and every later change went through `updateItems`, so the package carried two collection channels and re-read neither. One source plus one signal collapses them, and array identity is a free structural test the consumer already produces — React, Vue and Svelte all return a new array when order changes. This puts the O(n) copy on structural change rather than on every warm invalidation, which is what a resize or zoom produces. The name collides with the feature-owned geometry `invalidate()` on purpose: the controller member is the **cause**, the geometry slot the **effect**. | retracts `updateItems()` and ledger L-1's "the thunk is called exactly once, at construction" |
| D-45 | **A fragment is a plain declarative partial config.** `y()` returns `{ axis: installYAxis }`; `layoutAnimation()` returns `{ plugins: [installLayoutAnimation()] }`. The call stays **variadic and the library merges** — `sortable(root, config, y(), landing())` — with merge semantics owned by the **config slot**, not by fragment provenance: scalar and capability slots last-wins, `plugins` appends in fragment order. Installers are invoked **after** the merge completes, so a losing capability is never constructed. Defaults are derived post-merge. The **opaque feature brand is withdrawn**; what stays opaque is the _installer value_, not the record that carries it. | D-30 branded the feature value because "public stable type whose full structure is internal and unstable" is not a coherent third state. A plain config record is the coherent state D-30 could not find: **the schema is public and stable, the installer values are opaque**, and a consumer can therefore spread a preset, override one slot, filter a plugin array, or lift `weirdThing().axis` out of a helper — none of which requires constructing an installer. ~~which still needs the kernel entrypoint (D-47).~~ **That clause is a drift and is corrected by D-61**: constructing an installer needs the **middle** tier, `sortable/feature.js`, not the kernel. Sending an installer author to the kernel entrypoint tells them to replace the sortable in order to extend it. Keeping the merge in the library is what keeps `plugins` appending; a consumer-merged single object would silently last-wins the one slot that must concatenate. | retracts D-30 and the `SortableFeature` brand; supersedes D-12's contribution objects and the C-5 strategy/plugin tag resolution |
| D-46 | **Two gaps closed at Phase R, both recorded in 02 §Input policy: the opt-out attribute is `data-drag-ignore`, and the command table is enumerated and is deliberately narrower than the pointer one** — an `a[href]` or a `button` owns no arrow key, so declining there would remove keyboard reordering from a focused control for nothing. **Ingress must not consume interaction it does not use.** `preventDefault()` at admission is withdrawn as an unconditional act: a press that never crosses the activation threshold must leave native focus, caret, selection and form-control behavior intact. Default admission **declines** on interactive and editable descendants, so the existing decline path — which probe E confirms leaves native behavior completely untouched — carries the policy. Keyboard command admission must ask what the event landed on: text inputs keep caret arrows, `contenteditable` keeps editing navigation, native controls keep their keys, and **`event.isComposing === true` never admits**. Plain-text selection inside a draggable non-interactive region is requested with a modifier rather than inferred. | Probe E: eight of ten cases release-blocking from two root causes, and six of those **never activated a drag at all** — the damage is done by the admission `preventDefault()` alone. The worst case is an `ArrowRight` in a nested text input leaving the caret frozen, `defaultPrevented`, and a complete accepted reorder `{from:2,to:3}` from one keystroke, because the command ingress never asks what the event landed on. This is a policy change, not new machinery: declining is already total, and only `pointerdown` was ever prevented, so click, `href` and ctrl-click already survive. `handle()` resolves seven of the ten but is **not** the accessibility answer — it removes keyboard reordering unless the consumer makes the grip focusable, which is now a stated obligation. | corrects 02 §"moving the call one frame outward produces the same observable result"; adds the interactive-descendant rule 02, 03 and 05 do not currently contain |
| D-59 | **The box element arrives through admission, in the kernel's own vocabulary.** `admit` returns the element to lift, optionally paired with a box element; the kernel stores `box` in **its own per-operation state** beside `originRect` — **held by the kernel, not placed on the transactional frame**, on the same argument that keeps gate state on the settlement attempt: written once at admission, read twice, never transactional. A literal frame field would take the published seven-field slice to eight for no transactional benefit. **The spelling is `HTMLElement | Readonly<{ visual, box }> | null`**, discriminated by `'visual' in subject`rather than`instanceof HTMLElement`, because `instanceof`is realm-sensitive and`DOMRealm`exists precisely because an element may come from another document. An optional`box` was rejected: it gives _"the box is the visual"_ two encodings, which this contract refuses everywhere else. It does not read a behavior-authored draft field. | D-52's first form had the behavior stash `box` in the draft and the kernel read it back, which contradicts **H-2** — _the kernel does not know, store, extend or type_ the behavior's part — and D-15, which exists so the kernel cannot name behavior fields. The contradiction is not cosmetic: an exception here is precisely the "kernel learns one sortable-shaped thing" defect Checkpoint C found four times. Widening the admission return keeps D-5's principle intact — _the kernel gets what it needs from admission and nothing else_ — and it now needs two things rather than one, which is a change in the count, not in the rule. Admission runs once per press and is not the hot path, so the pair costs nothing measurable. **This is an SPI seam-signature change — the second and last in Revision 2 — and the freeze rule governs it.** It is discharged by a chain, not by a single probe, and the chain is worth writing out because "it repairs another decision" is not on its own sufficient: **api-1 measured that no single-window rule reproduces the removed footprint in both nested cases** → D-43 needs two windows → the second window requires the box element to be known **before** `acquireLift` → the only two places that can carry it are the behavior's draft and admission's return → the draft form contradicts H-2 and D-15 → admission's return is what is left. The failing executable case is api-1's; D-59 is where it finally lands. | repairs D-52; preserves H-2 and D-15 |
| D-60 | **`onError` is orthogonal to the terminal, and a classified stage may have no recovery.** One operation may produce `onError` **and** `onEnd` (`onFinish` when this row was written — D-62). `FAILURE_LANDING_TARGET` under D-49 is the first stage that is _classified, non-consequential, and has no recovery_ — the settlement is not failed and the domain result stands. | The owner already decided the first half in §4 — _"diagnostics remain orthogonal … do not create a second terminal taxonomy merely to encode diagnostic provenance"_ — but the contract carried an **unstated biconditional**, `onError ⇒ consequential`, that nothing had ever needed to test. D-49 falsifies it, so it has to be written down rather than left as an assumption a reader will re-derive. The second half follows: §Failure classification lists every stage with a recovery, and this one has none, which the `FailureStage` → recovery mapping must be able to express instead of treating as a gap. **Consequence for the test suite, worth stating because probe C1's finding is phrased the other way:** the defect was _`onFinish` once, `onError` zero_, and the fixed behavior is _`onEnd` once, `onError` once_ — so any assertion of mutual exclusivity between the two channels is now false. **Read with D-64**: the assertion the test makes about `onError` is now about a `code`, not a stage, so C1's regression test asserts `onEnd` once and one `DraggableError` whose code is the presentation class. | follows from D-49; makes owner §4 normative |
| D-56 | **A fragment factory that names no installer is deleted.** `callbacks()`, `handle()`, `visual()` and `placeholder()` become plain config keys — `{ onReorder, threshold }`, `{ handle: fn }`, `{ visual: fn }`, `{ box: fn }`, `{ placeholder: fn }` (D-65) — written directly in the config object. Their subpaths go with them. ~~**`placeholder()` carried two things, and both survive as flat keys**: `createPlaceholder` and `placeholderClassName`.~~ **Both are deleted and replaced by one slot, `placeholder` (D-65).** The argument below was mine, not the owner's, and the owner has overruled it; it is kept because it states the cost D-65 accepts. ~~The second is not dead weight — it is the only way to keep the default element and still brand it, and without it that consumer has to supply a whole factory to add one class. It stays **flat rather than nested** because `SortableContribution` is deliberately one flat type with fixed key names, and a single nested slot would be the sole exception to that.~~ The flat-versus-nested argument survives D-65 and now selects a **single callback slot** rather than two keys, which is the shape `handle`, `visual` and `box` already have. The surviving fragment factories are exactly those that install something: **`y()`, `xy()`, `landing()`, `layoutAnimation()`** — and `landing()`'s options narrow to `{ duration, easing }` under D-63. | Under D-45 those four became identity wrappers returning a public config slot, which voids the stated reason their subpaths exist — _"a separate subpath per optional feature is what makes the measurement honest"_. A subpath carrying no runtime machinery measures nothing. Keeping them would leave four exports whose entire behavior is to build an object literal the consumer can write more clearly by hand, which is the ceremony D-45 exists to remove; the argument that retired the feature brand retires these for the same reason one row down. **Validation is not lost**: `callbacks()`'s construction-time `onReorder` requirement and `threshold` check move to the merge, where D-45 already puts the missing-axis check, and they now fire for a config supplied any way at all rather than only through a factory. | follows from D-45; removes four exports and four subpaths |
| D-57 | **Post-merge installation order is schema order; plugins install in array order; retirement is the reverse of installation.** | D-45 erases provenance, so "declaration order" — the order retirement was _defined_ against — no longer names anything, and nothing in the owner's decisions replaces it. Schema order is the only candidate that is deterministic, independent of how the consumer spread their fragments, and stable across a refactor that reorders arguments. The alternative, first-appearance order across the merged fragments, reintroduces exactly the provenance tracking D-45 deleted. | closes the ordering D-45 left undefined |
| D-58 | **Insertion candidates are measured as `box`, not `visual`.** This supersedes Checkpoint D's parity D2, which chose `visual` because no `box` concept existed. | D-43 defines `box` as _the sortable geometry source_ and `visual` as _the node faithfully lifted_, and insertion is a geometry question. Leaving candidates on `visual` while the placeholder occupies the box's removed footprint puts the two sides of the same comparison on different rects — in api-1's case A they differ by **30 px** — which is a hysteresis defect, not a rounding one: the incumbent placeholder is measured one way and its challengers another. Under the default `box === visual` nothing changes, so this costs the common case nothing and makes the uncommon case coherent. D2's _coherence_ argument is what selects `box` here; only its choice of node is superseded. | supersedes parity D2; reconciles D-43 with the axis rule |
| D-55 | **`Behavior<Controller>` and `brandBehavior` are retired from the public surface.** The construction types survive internally. The kernel tier publishes `BehaviorFactory` and the seam vocabulary; it does not publish a branded, packaged behavior value. | D-48 leaves the brand with **no producer**: `sortable()` and `freeDrag()` return controllers, and `draggable()` takes a factory. An exported opaque type that nothing constructs is the same defect D-45 removed one row up — a boundary marker with no boundary to mark. Retiring it also removes the last consumer of `unbrandBehavior` on the install path. If a later behavior genuinely needs to be _packaged without being installed_, that is a new capability with a new justification, not a survival of this one. | follows from D-48; parallels D-45's retraction of the feature brand |
| D-51 | **D-37 (a) admits relinquishing invocations, and they are a closed named list.** A call into a declared consumer slot after logical closure is permitted **only** when its sole purpose is to release a resource the library is holding and cannot release itself. Such a call performs no operation work, publishes nothing, ignores any return value, and is wrapped. **The list is currently one member: `LandingHandle.destroy()`.** Adding to it is a contract change. **D-63 makes the member tier-dependent, and the list survives because the exception is about ownership rather than about consumers.** With the sortable's runner library-owned, `LandingHandle.destroy()` at the ordinary tier is a call into the library's own WAAPI runner and needs no exception at all. At the **kernel** tier a third-party behavior still supplies the runner, and there the call is into code the library does not own — which is what D-37 (a) is actually about. Read "declared consumer slot" throughout D-37 and D-51 as **"a slot filled by code the library does not own"**, which includes a third-party behavior; the narrower reading would silently empty this list the moment the sortable stopped supplying runners, and leave I-20 and F-30 unguarded at the only tier where they can still be violated. Under D-36 a relinquishing call **reached after logical closure** is part of physical teardown and therefore **defers with it**. **Qualified before implementation:** a relinquishing call on the **normal path of a live controller** — the join's `landing.destroy()`, which sits between the authoritative measurement and the pin — is ordinary sequenced work and keeps its position. An earlier unqualified wording would have deferred it past the pin, inverting the one ordering §Landing states as normative. This clause is about _closure_, never about relinquishment as such. | Without this row D-37 is false as written, and falsely in the one place it must not be: the kernel _must_ destroy a landing handle returned by a runner that destroyed the controller, or it leaks a runner nothing owns (I-20, F-30). The old prose floor admitted this as a breach; D-37's finite domain leaves no room for a breach, which is worse — **a finite domain with an unstated exception is not finite**, and the whole value of D-37 is that the list can be checked. So the exception is stated, enumerated, and given a discriminating property: relinquishment _returns_ something to the consumer, it does not _ask_ anything of them. That is why it cannot be used to smuggle operation work past a close. | closes the hole 05 found in D-37; keeps I-20 and F-30 intact |
| D-52 | **The two measurement windows have distinct owners, and neither disturbs `originRect`.** The behavior stashes the `box` element in the admission draft; the **kernel** reads `boxPre` beside its existing `originRect` capture, before `acquireLift`; the **behavior** reads `box.offsetHeight` — **one extent, not a pair** (F-58), because the cross axis never subtracts — at the top of `activation.prepare` — **and skips that read entirely when `box === visual`**, which F-55 corrects: the lifted element's own offset box is unchanged across `acquireLift`, so window 2 is only meaningful when the box is a distinct element that stays in flow. The placeholder-sizing writes stay in `prepare` and are covered by D-39's rollback ledger. `originRect` is **not** derived from either window. | D-39 and D-43 legislate the same code and neither said which seam owns which write, so they could not both be implemented. The ordering makes it free: `acquireLift` precedes the activation seam, so `boxPost` is already available where `prepare` measures today, and the draft already carries behavior state, so stashing `box` needs no new SPI member (D-5). **`originRect` must stay the visual's grab rect**: it is the basis of the origin-relative landing space, and deriving it from a `box` chosen for geometry reasons would make the landing coordinate space a function of a `box()` the consumer picked for layout — which contradicts §One coordinate space. The three rects collapse to one read under the default `box === visual`, which is the common case. | reconciles D-39 with D-43; preserves the frozen landing space |
| D-53 | **`KernelHost` gains a logical-liveness reader.** One readonly member answering _is this controller logically closed_ — the latch itself, not a proxy for it. This is the sanctioned reading D-38 requires, and the only one. | D-38 forbids physical-teardown observations, and after that prohibition **there is no reading left**: the latch is kernel-private and `KernelHost` exposed none, so a behavior could only mirror it by hand. Probe A supplies the failing executable case the freeze rule asks for — the three `presentation.signal.aborted` sites exist _precisely because_ no logical reading was available, and they are the only failures the bracket caused on its own. Requiring every behavior to maintain a private mirror is worse now than before D-47, because the kernel is published and a third-party behavior author would have to know to do it. **This is the only SPI _addition_ in Revision 2 — but not the only SPI change.** D-59 widens `admit`'s return, which is a seam signature. Two crossings, discharged differently: this one by probe A directly, D-59 by api-1 through a chain. An earlier wording of this row called it the sole SPI change, and that was the third of three false single-crossing claims in this revision. | forced by D-38; one of Revision 2's two SPI changes |
| D-54 | **On the pointer path, `preventDefault()` moves to activation, and the drag suppresses its trailing `click`.** The default is not prevented at admission (D-46). It is prevented when the activation threshold is crossed, and at that moment the library also clears any selection the pre-threshold press began. After an **activated** drag the library suppresses exactly one subsequent `click`, in the capture phase. **This is a pointer-path rule and does not reach `command.admit`.** A `keydown` default cannot be prevented once its listener has returned, and a command has no threshold to relocate to — D-32 redefined `PENDING` precisely because the threshold is a pointer-path property. Command admission keeps preventing inside the listener, which is sound because under D-46 command admission **is** the intent test: it has already asked what the event landed on before it answers. **The `click` suppressor's lifetime:** armed at activation, owned by the controller's ingress lifetime rather than the operation's, because the `click` arrives after the operation has ended; disarmed by the first `click`, by the next `pointerdown`, or by teardown, whichever comes first. **A drag cancelled after activation still suppresses** — the browser synthesizes the `click` regardless, and a cancellation is a library or consumer verdict, not evidence the user meant to click. Scroll suppression is **not** `preventDefault()`'s job — it is `touch-action`, which the consumer sets in CSS. | D-46 withdraws the admission call and names no replacement, which leaves the policy incomplete in a consumer-visible way: without relocation nothing prevents the default at all, and without the trailing-`click` suppressor a drag that ends on a link navigates. Both are consequences of moving the call later, not pre-existing defects, so the policy has to carry them. `touch-action` is named explicitly because `preventDefault()` on `pointerdown` was never a reliable scroll suppressor and treating it as one is how the touch story silently breaks. **Evidence limit, stated because the rule is unconditional: probe E is Chromium and mouse only.** Touch adds long-press context menus and tap highlighting that admission's `preventDefault()` also consumed, and nothing has measured them — recorded as an owed measurement, not as a settled case. | completes D-46; supersedes 02's admission-time `preventDefault` |
| D-49 | **A landing that cannot be measured is skipped, not faked.** With one authoritative measurement (D-41), a failed measurement — a throw, or D-42's precondition check finding the placeholder detached or no longer the item's sibling — **reports through `onError`, skips the landing animation, and joins immediately**. It does not fail the settlement, and it does not animate toward a target it does not trust. The drop still terminates normally with its domain result, because the DOM commit has already happened and the reorder is real. | D-41 collapses two measurement sites into one, and that silently converts a tolerated fault into a fatal one: the old contract survived a failed `anchorTarget` per drop, because F-17 and I-29 make it best-effort and the join's pin decides correctness. With one site, failing the settlement (`ARM_FAILED`) would tolerate none. Skipping restores F-17's tier — the failure is **quality only**, exactly as F-16 classifies a visually abrupt correction — while keeping I-31's single terminal. It also **unifies with D-42**: a detached placeholder is precisely the case where an animation must not run, and this replaces D-42's weaker "lands from the unrepaired position" with the behavior probe C1 shows is wanted, since the unrepaired position is the viewport origin and flying to it over 12 frames is the visible defect. A jump cut is honest; a confident animation to `(0,0)` is not. | narrows D-41; supersedes D-42's landing clause; keeps F-16/F-17/I-29's tiering |
| D-50 | **Explicit consumer scoping wins over the default decline.** D-46 declines interactive and editable descendants by default; where the consumer has scoped dragging with `handle()`, the resolved subject governs and the default decline does not re-veto it. The test walks the path between the event target and the resolved subject. | The owner's direction is _"unless the consumer explicitly scopes dragging to that interaction"_, which states a precedence without stating its mechanism. Without this row the two rules are simply in conflict for a grip placed inside an editable region — a real case, since a drag handle inside a rich-text block is ordinary. The consequence is that `handle()` stops being the _only_ descendant-scoping mechanism, which document 03 currently asserts; it becomes the **override** for a policy that now exists by default. | resolves D-46 against `handle()`; corrects 03's sole-mechanism claim |
| D-48 | **The behavior owns the root, and `draggable()` moves to the kernel tier.** `sortable(root, config, y(), landing())` returns a `SortableController` **directly** and calls `draggable()` internally; `freeDrag(root, …)` takes the same shape. `draggable()`, `BehaviorFactory` and the behavior-construction types are published at `@ydinjs/drag/kernel`, where D-47's `draggable(root, (host) => ({ spec, controller }))` is literally the supported form. **Corrected before implementation: `drag.js` does _not_ become the kernel entrypoint.** An earlier wording of this row said it did, and that was wrong in the one way D-47 exists to prevent — `drag.js` carries `FailureStage`, `Point`, `DOMRealm` and the `FAILURE_*` constants, which `sortable.js`'s `onError` structurally depends on, so an ordinary consumer would have had to import from the **kernel** tier to `switch` on an error stage its own handler was handed. §11 names a **subpath**, `@ydinjs/drag/kernel`, and the entry topology follows it literally: **`drag.js` stays the shared vocabulary** — errors, `Point`, `DOMRealm`, and L-11's three future sentinels — and a **new `kernel.js` carries `draggable()` and the authoring types**. **The premise of that correction is void and the conclusion survives (D-64):** the ordinary consumer no longer receives a `FailureStage` at all, so `drag.js` is not kept for the reason written here. It is kept because `DraggableError` is a **class** — a runtime value both tiers hand out and neither owns. **The entry count is now eight, not seven** (D-61 adds `sortable/feature.js`), and the tiers are three rather than two. Shared vocabulary belongs to neither tier, so it gets its own home instead of lodging in whichever tier also happens to need it. | Two contradictions in Revision 2 turn out to be one. D-47's snippet passes a bare factory, which **C4-03 normatively rejected** for publishing `BehaviorFactory` and both private generic parameters at the public boundary — and D-45 §8 writes `sortable(root, …)` while the contract composes `draggable(list, sortable(items, …))`, naming the element twice. Both dissolve if §11's disclosure ladder is read literally: **the public boundary moves.** C4-03's objection was about leaking low-level vocabulary into the ordinary surface, and at the kernel tier that vocabulary _is_ the surface — publishing it there is D-47's whole point, not a leak. The alternative, an exported `defineBehavior` brander, preserves the handshake equally well but leaves both §8 and §11 wrong as written and makes the consumer write two calls where the shipped package wrote one. **Parity improves as a side effect**: `sortable(container, options)` becomes `sortable(container, config, …fragments)` rather than a two-call composition. | resolves D-45 §8 against D-47; moves C4-03's boundary rather than overruling it |
| D-47 | **Custom behavior authoring stays public and factory-shaped.** The progressive disclosure is `sortable()` / `freeDrag()` → sortable config **and feature** utilities → `@ydinjs/drag/kernel`. **The middle rung is a published surface, not a description of the built-in fragments** — D-61; this row's original wording read as the latter and is what let the rung be deleted. The low-level boundary keeps the two-phase handshake `draggable(root, (host) => ({ spec, controller }))`, because the host must exist before a behavior can build host-backed runtime state, while ingress must not arm until the complete spec and controller have been returned. A preconstructed host-free behavior object cannot replace it. | Publication was never the open question — the minimum public **vocabulary** is. D-1 already made the handshake structural, and inverting it would make "no input before install returns" a rule again instead of an unexpressible state. This row exists so that minimizing the exported vocabulary is not mistaken for deferring the surface. | confirms D-1 and D-2; withdraws the proposal to leave the kernel unpublished |

### Revision 2.1 — the reconciliation (D-61…D-65)

Five owner-decision drifts, corrected. Each row names the owner text it restores, because that — not a new argument — is its warrant.

| ID | Decision | Why | Supersedes |
| --- | --- | --- | --- |
| D-61 | **The middle tier is a published, supported surface.** The ladder has three rungs and the second one is real: `sortable()`/`freeDrag()` → **sortable feature and config utilities** → `@ydinjs/drag/kernel`. A new subpath, **`sortable/feature.js`**, publishes the installer vocabulary — `SortableInstaller`, `FeatureContext`, `SortableContribution` and the view types they name — so a third party can author a custom axis, landing or plugin **without writing a behavior**. 03's identity clause 3 (_"the module that declares the installer slot types is not a declared package subpath"_) is **retracted**: it existed to make installer values unconstructible from outside, and constructing them from outside is precisely what this rung is for. | The owner states the ladder in final §11 and in review 3 §1 as `sortable extension author → sortable/feature-utils`, and Revision 2 deleted the rung twice over — D-45's closing sentence sends an installer author to `@ydinjs/drag/kernel`, and 03 §Fragments are public, installers are opaque leaves it as _"if installer authoring is ever supported"_. Both are drifts, and the first is the consequential one: **the kernel tier is where you go to replace the sortable, not to extend it.** A consumer who wants a custom insertion axis would have had to reimplement the collection model, the placeholder, the landing protocol and the release proposal to get one — an overshoot of the entire behavior for one slot. The middle tier exists so the answer to _"my list needs a different axis rule"_ is one installer, not a second sortable. **The cost is the one D-45 already accepted for `SortableConfig`, extended:** `SortableContribution` and `FeatureContext` become a semver surface, and by the closure rule this table runs on, publishing them publishes what they structurally name. Minimizing the middle tier therefore means **narrowing `SortableContribution` itself** — the same tension D-48 records for `BehaviorSpec`, now present at two rungs instead of one. | restores owner final §11 and review 3 §1; retracts 03's identity clause 3 and D-45's "drops to `@ydinjs/drag/kernel`" |
| D-62 | **One public terminal callback, `onEnd(result)`.** `onFinish` and `onCancel` are deleted; the four arms `accepted`, `noop`, `rejected`, `canceled` are the existing `ReorderTransactionResult` union, which is already exactly those four. `SortableFinishResult` and `SortableCancelResult` are deleted with the callbacks — they exist only to partition that union for two callback signatures, and there is one signature now. `onError` stays orthogonal (D-60). | D-40 already decided this — _"the `onEnd` union has four arms"_ — and named no other channel, but every other document kept the pair, so the contract set said `onEnd` in one row and `onFinish`/`onCancel` in eighty. The rename is not cosmetic: **it deletes a defect class rather than renaming one.** F-37 is _"`finalized` used a binary accepted-vs-everything predicate, sending the no-op result to `onCancel`"_ — a routing bug that is only possible when a four-arm result has to be split across two callbacks by a predicate. One callback taking the whole union has no predicate and no routing to get wrong, and D-24's exhaustive discriminant becomes the consumer's switch instead of the library's. It also makes the owner's lifecycle pairing statable in the terms it is written in: _"if `onStart` fires and the controller remains alive, the operation eventually produces one terminal `onEnd`."` | completes D-40; dissolves F-37's mechanism; supersedes 02 §Callbacks' two-callback routing |
| D-63 | **The public custom landing runner is removed from the sortable surface.** `LandingOptions` is `{ duration?, easing? }`; `landing({ run })` and the public `LandingStart`/`LandingHandle`/`LandingContext` exports go. The library owns the landing animation through WAAPI. **Nothing is removed from the kernel**: the seam is unchanged, the kernel still never names a runner type, and a kernel-tier behavior author still supplies one — review 3 §10 says so in as many words. | Standing owner decision from review 1 §5, reaffirmed in review 2 and review 3 §10, and Revision 2 carried none of it: D-56 lists `landing()` among the surviving factories without touching its options, and 03 §`landing()` still documents `run` as _"a first-class citizen"_. The removal is a **tier move, not a capability loss**, which is why it costs the contract almost nothing: the protocol that made a custom runner safe — reserve the hold, seal, then arm, latch completion once — is what makes the _library's own_ WAAPI runner safe, and it stays. What actually leaves the ordinary tier is a **public lifecycle protocol** the owner judged not worth its surface: four types, a handle contract, a completion latch and a relinquishment obligation, all so a consumer could write a spring. | restores review 1 §5, review 2 and review 3 §10; narrows D-56's factory list; re-scopes the preserved-from-probe-1 runner row to the kernel tier |
| D-64 | **Consumer error codes are coarse strings; failure stages stay internal.** `onError` receives a `DraggableError extends Error` carrying a `code: DraggableErrorCode` — currently `'consumer' \| 'interaction' \| 'presentation' \| 'platform'`, names not frozen — and the native `cause`. The **13 `FAILURE_*` constants and `FailureStage` leave the ordinary tier**; they survive internally and are published at the kernel tier, where a behavior author classifies with them. A **total** stage → code mapping is required, and it is library-owned. **The count is thirteen, and this row said fourteen until Revision 2 closure** (A-8): `FAILURE_PRESENTATION_READY = 13` was deleted with the readiness protocol by D-41, and its number was deliberately not reused, so the stage list is thirteen constants whose highest value is 14. The implementation, `tests/exports.node.test.ts` and `tests/kernel/errors.node.test.ts` all say thirteen; this row was the only document that did not. `CancelStage` (`AT_PROPOSAL`/`AT_CONSUMER`) is **not** affected — it is a cancellation stage on a domain result, not a failure stage, and review 3 §12 targets `FAILURE_*` only. | Review 3 §12, unrecorded by Revision 2: _"consumer error codes describe actionable fault classes, not internal pipeline seams."_ The 13 stages are granular on the pipeline axis and silent on the only axis a consumer acts on — _is this my bug or the library's_ — which is what the four codes encode. **This voids D-48's stated rationale and leaves its conclusion standing**, and the distinction has to be explicit or the next reader will re-derive the entry topology from an argument that no longer holds: D-48 kept `drag.js` as a third root because _"`sortable.js`'s `onError` hands the consumer a `FailureStage`"_, and it no longer does. `drag.js` survives on a **different shared symbol** — `DraggableError` is a class, so it is a runtime value both tiers need, and a kernel-tier author must not import it from `sortable.js`. Three roots, same shape, second derivation. That the conclusion survived two independent arguments is worth exactly as much caution as it is comfort: it has now been reached twice and measured never. | restores review 3 §12; voids D-48's rationale while preserving its conclusion; supersedes 03 §The public/internal boundary's "public and stable at the ordinary tier: `FailureStage` … and the `FAILURE_*` constants" |
| D-65 | **`placeholder` is the consumer callback slot.** `placeholder(context): HTMLElement` returns a fresh detached element; once adopted the library owns it for the operation. `createPlaceholder` and `placeholderClassName` are both deleted. | Review 3 §2's config schema and §4 both spell it `placeholder`, and §4 states the negative case explicitly — the slot is the callback, not a record with a `create` member. D-56 split it into two flat keys and argued at length for the second; **the owner has overruled that, and the loss is real rather than notional**: a consumer who wants the default element with one class of their own must now supply a factory that builds the element. That is three lines instead of one, for the case D-56 predicted would be common. It is accepted because the alternative is two slots for one concern, and because a single callback is the shape every other consumer-supplied node in this config already has — `handle`, `visual`, `box` are all `(item) => Element`. | restores review 3 §2 and §4; supersedes D-56's `createPlaceholder` + `placeholderClassName` split |
| D-66 | **`onEnd` is exactly-once domain disposition; `onError` is orthogonal.** Every started operation on a still-live controller publishes **one** `onEnd`, on the failure path as well as the success path. Its argument is **the domain result the frame already holds, and `canceled` when it holds none** — existing result wins, otherwise `canceled`, with the classifying error as the cancellation `reason`. 02 §The join's _"the terminal callback is skipped after a consequential failure"_ and the rule that _"a failed operation reports through `onError` **only**"_ are both **retracted**. **D-23's list loses exactly one item** — _"no terminal callback"_ — and keeps the rest. | Q-14's three-way conflict, resolved by the owner. The distinction that makes it coherent is between **continuation** and **disposition**: D-23 stops a classified failure from doing more _operation work_ — no retirement, no gate arming, no seam success path — and a terminal callback is not operation work, it is the statement of how the operation ended. Skipping it made the library's most serious failures its quietest, which is the defect probe C1 found by a different route: _`onFinish` once, `onError` zero_ was invisible, and _no terminal at all_ is invisible in the same way. **The old reason inverts into the new rule.** The skip existed because the frame still said `OUTCOME_ACCEPTED` and publishing would announce a successful drop; but when the authored commit has already run, the drop **is** accepted — the reorder is real and permanent — and the consumer must be told so. `onEnd` answers _what happened to the data_, `onError` answers _what went wrong_, and neither substitutes for the other. **No SPI change, and the carrier already exists** — but it is **not** `reportFailure`, which an earlier wording of this row named and which is admission-only (_"a failure with **no operation to settle**: `admit` threw, so identity was never minted"_). The in-operation carrier is **`SettlementInput` with `SETTLED_FAILED`**, which already carries `{ stage, error }`, and the failure checkpoint already routes **every** classified failure of a live operation through the settlement seam to deliver it. **`settlement.prepare` writes the fallback into `draft.domain` and returns the existing `PreparedSettlement` sentinel**; `effect` receives a `Readonly` frame and cannot write frame state at all. `finalized` then publishes `current.domain` and nothing else. **Nothing is added to the frame part or to `PreparedSettlement`, so [04](04-frame-slicing.md)'s frame model is unchanged by this row** — the fallback occupies a field that has been `ReorderTransactionResult | null` since D-24. The two facts the kernel does not supply are carried by a behavior-private progress marker — §D-66 §The progress marker, below. | resolves Q-14; retracts 02 §The join's skip and the `onError`-only rule; narrows D-23 by one clause; makes I-31 unconditional except for `destroy()` |
| D-67 | **Landing duration is contextual, and the zero-argument thunk is deleted.** `landing({ duration })` accepts a number or **`duration({ distance, from, to }): number`**, invoked once per landing at settlement and validated against the same domain as the fixed form. `duration: () => number` is removed **from the documented surface**; it is not, and cannot be, a compile error — see F-52. `from` and `to` are the landing's origin-relative deltas, `distance` the straight-line magnitude between them. | Review 3 §10 rejected the zero-argument thunk on its own terms — _"it cannot even observe the distance that motivated dynamic timing"_ — and deferred the contextual form to a proven need. **D-63 supplied the need by removing the alternative.** With `landing({ run })` gone the thunk was the sole surviving carrier of parity L-6, so the three options were: keep a thunk the owner had already judged unjustified, delete it and lose a shipped capability outright, or ship the contextual form. The third is the only one that both discharges §10 and keeps L-6, and it costs nothing the thunk did not already cost: the invocation site, the validation and the reduced-motion ordering are unchanged — **only the argument list grows**, from zero to one object the landing already has in hand at that moment. `from` and `to` are already computed for `LandingContext`; `distance` is one `Math.hypot`. | closes the L-6 conflict D-63 opened; discharges review 3 §10's second clause; supersedes the `() => number` form in D-63's surviving options |

#### D-66 §The progress marker

Out of the table because it is a mechanism rather than a decision, and because a table row is the wrong place for one.

**Two facts the kernel does not supply are carried by one behavior-private monotone marker** — `MINTED → STARTED → RESOLVING`, per operation, cleared in `retire()`. It is advanced at two sites the behavior already writes:

| State | Advanced | The fallback publishes |
| --- | --- | --- |
| `MINTED` | the operation exists | **nothing** — 02 §No start, no terminal |
| `STARTED` | **immediately before invoking `onStart`** | `canceled` at `AT_PROPOSAL` |
| `RESOLVING` | as the first statement of the `ResolutionCommand.invoke` closure | `canceled` at `AT_CONSUMER` |

`RESOLVING` is truthful by construction: the kernel runs that closure _only_ after `release.effect` returns normally, and `invoke: null` means no round-trip at all. Deriving it from `proposal !== null` is **false** — the proposal commits one seam earlier, in `release.prepare`.

`STARTED` advances **before** the call, not after, so that a throw **from `onStart` itself** still publishes a terminal. The consumer has been told the drag began; it is owed an end.

**Pinned, and deliberately not broadened: a failure classified before that point publishes `onError` and no `onEnd`.** The owner's guarantee is an implication over _started_ operations, and D-66 does not make it a biconditional (02 §No start, no terminal).

#### D-62 §The unresolved arm — **resolved by D-66**

> **Resolved, 2026-08-13, by the owner: `onEnd` is exactly-once domain disposition, `onError` orthogonal, existing result wins and `canceled` otherwise.** The section below is kept because it is the record of what the conflict was and why the three candidate answers were not equivalent — and because the answer taken is the third one, which **inverts** the stated reason 02 §Landing (D-16) gave, rather than satisfying it. That inversion is the substance of D-66 and is easy to lose once the prose reads as settled.

**Recorded here and not resolved when written.** Removing `aborted` (D-40) and requiring one `onEnd` per started operation (owner final §4) are jointly unsatisfiable against a rule this contract already carries, and the collision is only visible once the callbacks are one.

02 §Landing (D-16) stated: **"the terminal callback is skipped after a consequential failure."** Its reason is sound — the committed frame still says `OUTCOME_ACCEPTED`, so publishing a terminal would announce a successful drop that the queued checkpoint is about to report as failed. Review 3 §11 saw the same gap and answered it with a fifth arm: `onError(error)` then `onEnd({ type: 'aborted' })`. Owner final §4 declined the fifth arm. **Nothing was put in its place**, and with the two callbacks collapsed into one there is nowhere left for the case to hide.

The gap is narrower than it first reads, and the narrowing matters:

- a **non-consequential** classified failure already publishes both channels — that is D-60, and it is settled;
- a consequential failure **before any domain result exists** could honestly publish `canceled`: the drag was abandoned, which is what D-40 says `canceled` means;
- a consequential failure **after the authored commit has already happened** is the hard case. The consumer's data is reordered, the DOM is committed, and the operation reports through `onError` **only**. Under the owner's pairing that is a missing terminal; under 02's rule, publishing `accepted` would be a lie and publishing `canceled` a worse one, since the reorder is real and permanent.

Three ways out, none taken here: reinstate `aborted` (the owner declined it, but declined it against a five-arm _domain_ taxonomy rather than against this case); narrow the pairing invariant to _"unless a consequential failure occurred"_ and accept that `onError` is then the sole terminal signal for it; or publish the domain result the frame actually holds and let `onError` carry the fault, which makes `onEnd` a report of _what happened to the data_ rather than _how the operation ended_.

**The third is what I would choose** — it is the only one that keeps the pairing the owner wrote, it needs no new arm, and it matches what the consumer must actually do (the reorder committed; they have to know). It also inverts 02's stated reason rather than satisfying it, which is why it is a decision for the owner and not a reconciliation.

**The owner took the third, with the tie-break the analysis above left implicit**: _existing result wins, otherwise `canceled`_. That clause is what makes the third option total rather than merely preferable — case 2 and case 3 stop being separate rules and become one lookup on the frame. **D-66** records it. Q-14 is closed in [05](05-lifecycle-invariants.md)'s resolved table.

### Revision 2.2 — the kernel vocabulary (D-68)

One decision, closing D-47 §11's queued _minimize the kernel vocabulary_ work, [`ledger.md`](../ledger.md) §Open questions 5 and the half of L-14 that was deferred rather than answered.

| ID | Decision | Why | Supersedes |
| --- | --- | --- | --- |
| D-68 | **The kernel tier publishes the structural closure of `BehaviorFactory`, plus every value the SPI hands a behavior whose domain it could not otherwise name — and nothing else.** Added to `kernel.js`: the three `LIFT_*` values and `LiftMode`; the five `SETTLED_*` values; `AT_PROPOSAL`/`AT_CONSUMER` and `CancelStage`; the eight phase values and `Phase`; `toDraggableError`; and the type closure — `Draft`, `Frame`, `KernelFrame`, `OperationIdentity`, `FramePartOf`, `Transition`, `ReleaseTransition`, `SettlementTransition`, `ActionTransition`, `BehaviorInstall`, `LifetimeScope`, `Disposer`, `VisualLiftSession`, `OffsetBox`, `LandingStart`, `LandingContext`, `LandingHandle`. **Four of those are re-homed rather than added** — `Disposer` and the three landing types are published at `sortable/feature.js` today, and `CancelStage`/`AT_*` at `sortable.js`; each keeps that publication as a **re-export** while its declaration site becomes the tier that owns it. **`KernelFrame.phase` narrows from `number` to `Phase`.** Normative surface: [02](02-kernel-behavior-contract.md) §The kernel tier's public vocabulary. | **The tier was not authorable from its own entry, and not marginally** (F-59). `BehaviorConfig.liftMode` requires a `LIFT_*` **value** and `settlement.prepare` requires the `SETTLED_*` values to discriminate the input D-66 travels on; `kernel.js` publishes neither and no path under `kernel/` is a declared export, so the entry could _describe_ a behavior and not _construct_ one. That is stronger than L-14, which recorded the tier as writable inline and not by annotation — the values make it writable in **no** style. **Minimality is a property of the declarations, not of the export list**, which is D-61's rule at the other rung and `sortable/feature.js`'s own stated practice: publishing a type publishes what it structurally names, so the only ways to shrink the surface are to shrink `BehaviorSpec` or to accept its closure. This accepts it, and shrinks nothing — every candidate for elimination (`Disposer`, `FramePartOf`, `BehaviorInstall`, `KernelFrame`) is a name a behavior of the sortable's size writes out of line. **What is deliberately _not_ published is the other half of the decision**: the seam driver, the full `Lifetime`, the frame helpers, the reporter, the invalidation utilities, the protocol event names and the D-46 input-policy helpers. The discriminating rule is _the kernel never hands one to a behavior and never accepts one from it_ — mechanical export of everything internal would have published all seven groups. **Cost, stated:** thirty-two names that were public nowhere become semver surface, including the eight-phase vocabulary D-14 has carried verbatim since probe 1. **Runtime cost is zero** — every added type is erased, every added value is a numeric constant already in the graph, the entry count stays at eight, and `sortable.js`'s bytes do not move. | closes D-47 §11's queued vocabulary pass, ledger §Open questions 5 and L-14's deferred half; corrects 03 §Internal to the ordinary tier, published at the kernel tier; empties `typedoc.json`'s `intentionallyNotExported` |

#### D-68 §Published is not must-name

The two counts are different and only one of them is a usability number.

| Authoring style | Names an author writes |
| --- | --- |
| One factory, seams inline in the object literal | **3–8** — `draggable`, one `LIFT_*`, and the `SETTLED_*` arms it handles. Every parameter is contextually typed. |
| Seams hoisted into their own modules — the sortable's shape | **~30**, which is what `src/sortable/*` imports from `src/kernel/*` today, less the four internal groups |

The published list is 66 names and neither number is 66. That gap is the point: the closure exists so the second style is **possible**, not so anyone types it.

#### D-68 §What self-contained means, and what it does not settle

**Self-contained = `kernel.js` and `drag.js`, with no deep path and no import from another tier.** `drag.js` is not a tier — 03 §The public/internal boundary has it spanning all three — so a behavior author reaching it is reaching sideways, not down. Reaching `sortable.js` or `sortable/feature.js` **is** the inversion D-48 and D-64 exist to prevent, and it is what the tier does today for `Disposer`, `CancelStage` and the three landing types.

**This decision is neutral on whether `drag.js` survives** (handoff §6.5, [`measurements/error-identity.md`](../measurements/error-identity.md)). It adds one data point and takes no position: the kernel tier now names `DraggableError` twice over — as `toDraggableError`'s return and in a behavior's own `instanceof` — which is the same both-tiers-name-it argument the shared root already rests on, neither strengthened nor weakened. If the owner applies the measurement's recommendation, `kernel.js` re-exports the three shared names and the tier becomes one specifier; the vocabulary table below is unchanged either way.

### Phase 18 — free drag (D-69…D-77)

The second behavior's public contract and decomposition. Normative surface: [07](07-free-drag-contract.md). Nothing here reopens Revision 2; **two rows change the kernel**, and both are corrections the second behavior forced rather than preferences it expressed — D-74 because the kernel classifies generic seams with one behavior's vocabulary, D-76 because two Phase 14 decisions are normative and unimplemented.

**D-77 arrived at the phase's closure and changes the behavior that already ships.** It is the only row here that does, and it is the answer to F-67 rather than a free-drag preference: the second behavior made the package's validation rule visibly true of one behavior and false of the other, so the rule was re-derived across both. It **amends D-69's signature**, which no other row does.

| ID | Decision | Why | Supersedes |
| --- | --- | --- | --- |
| D-69 | **Implemented, 2026-08-16 (Phase 19).** Signature amended by D-77. **`freeDrag(item, config, ...fragments): FreeDragController`, from a new `free-drag.js`, in one vocabulary — `drag`.** The first argument is the ingress root, which for a free drag _is_ the item; the second is a **complete** `FreeDragConfig` (D-77); every later argument is a plain partial config merged by slot; the return is the controller. The terminal is one `onEnd(result)` over **three** arms — `accepted`, `rejected`, `canceled` — plus an orthogonal `onError`. | Member for member the shape D-48 gave `sortable()`, so the ordinary tier has one form rather than two. **The arm set is re-derived, not inherited**, which the ledger required: the sortable's fourth arm exists because a reorder can be structurally identity-preserving — `from === to` is a real, resolved, successful transaction that changed nothing — and a free drag has no such state, so `noop` would be an arm nothing can produce. The shipped package mixes `FreeDrop*` types with `FreeDrag*` export-site renames; the drop is an event inside the drag, and the drag is what is configured, controlled and named. | ledger §6's deferred decomposition; the `FreeDrop*`/`FreeDrag*` split |
| D-70 | **Implemented, 2026-08-16 (Phase 19).** **`axis`, `lift`, `threshold`, `home` and the callbacks are plain config keys; `bounds` and `landing` are capability installers on their own subpaths.** The contribution is three slots — `constrain`, `startLanding`, `retire` — and `constrain` is a **paired** capability (`apply` + `invalidate` + `retire`) filling a **single-writer** slot. Four new entries: `free-drag.js`, `free-drag/feature.js`, `free-drag/bounds.js`, `free-drag/landing.js`. | D-56's rule — a capability that installs nothing is a config key — applied to the question _does this slot own state or code a composition without it should not carry?_ `axis` is two comparisons and no state, so a factory for it would ship a module and a closure to save two branches. `bounds` owns a resolved rect, a staleness rule and a re-read source, so an unconstrained drag must not carry the resolver or the clamp — which is the test `plan.md` §Phase 18 sets for the composition model. Pairing follows 03 §Geometry is a paired capability for its stated reason and not by analogy: the behavior owns the events that make the rect stale and the feature owns the rect. **Single-writer buys the extensibility story** — grid snapping or magnetic guides fill the same slot _instead of_ `bounds()`, and the library ships none of it. **Cost, stated:** a composition with `bounds()` makes it four post-`MOVE` indirect calls rather than three; one without pays a property read and a predictable branch. | ledger §6's "features and subpaths deferred to Phase 18" |
| D-71 | **Implemented, 2026-08-16 (Phase 19).** **Every mutable policy slot is a source the library re-reads, `invalidate()` is the only signal, and `moveTo(point)` is a separate motion command. There is no `update()`.** The controller is four members: `invalidate`, `moveTo`, `cancel`, `destroy`. `moveTo` **re-bases** — the visual is at `point` on the next committed frame and later pointer motion continues relative to that. | D-44 generalized from the collection to policy. `updateItems` was deleted on the finding that the package carried **two channels for one thing and re-read neither**; `update({ bounds })` beside a `bounds` source is the identical shape at a different slot. `invalidate()` carries no payload and is applied as a **queued action**, so consumer sources are read inside `action.prepare` where a transaction is open and a throw has a stage — reading them on the `invalidate()` statement would run consumer code at an arbitrary reentrant point. **A controlled position is not policy**: folding it into an options bag is what made the shipped `update({ position })` carry a command. **Two real losses, stated rather than respelled**: live `onMove` replacement and live landing timing, both recoverable in two lines by the consumer. | ledger L-5 and the `FreeDragController.update` row |
| D-72 | **Implemented, 2026-08-16 (Phase 19).** **The coordinate space narrows to deltas.** `localDelta` stays and is derived **by default** from box-quad's ancestor linear part; `coordinateSpace`, `CoordinateMapper` and `localPosition` are dropped; every point on the surface — `home`'s return, `moveTo`'s argument, the request's positions — is viewport. `FreeDragHome` collapses into `Point`. | The shipped default mapper is an `offsetParent` walk — a coordinate module this package deleted and Phase 19 forbids re-adding — so the question was never _is a mapper useful_ but _what can be derived without it_. box-quad already hands the lift the inherited ancestor space's **linear part**, which is exactly what a delta needs and is already consumed for the in-place projection; it exposes no translation, which is exactly what a point needs. The decision follows that seam rather than splitting the difference. **The two shipped stories the option existed for — Zoomed Context, Transformed Stage — are ancestor-transform cases, and drag2's port of the first already works with no mapper.** What a consumer loses is an arbitrary non-ancestor space, which it can map itself because it holds both ends. | ledger L-7 and the `coordinateSpace`/`CoordinateMapper` rows |
| D-73 | **Implemented, 2026-08-16 (Phase 19).** **Lift modes are a consumer domain of three strings, mapped by the behavior**: `'faithful' \| 'flat' \| 'in-place'`, default `'faithful'`. The kernel's `LIFT_*` enum stays kernel-tier. | The ledger's open question 2 asked whether a behavior may expose a kernel-internal enum. It may not and need not: D-68 publishes the enum for a **kernel-tier author** who must fill `BehaviorConfig.liftMode`, and the ordinary tier gets its own domain — which is D-47's progressive disclosure working rather than the inversion it exists to prevent. **The strings are renamed from the shipped ones and it is breaking**: both promoted modes use the top layer in this package, so `'top-layer'` names one of them after a mechanism it shares with its sibling, and `'none'` says _no lift_ for a mode that lifts, suppresses transitions and projects coordinates. Precedent is `vertical()` → `y()` — a layout word for a rule about a coordinate — and the migration is a three-row table. | ledger §Open questions 2 and the `lift` row |
| D-74 | **Three failure stages are renamed and no numeric value changes**: `FAILURE_INSERTION` → **`FAILURE_ACTION_PREPARE`** (4), `FAILURE_PLACEHOLDER_MOVE` → **`FAILURE_ACTION_EFFECT`** (5), `FAILURE_REORDER_RESOLUTION` → **`FAILURE_RESOLUTION`** (8). | **The kernel classifies two behavior-generic seams with one behavior's vocabulary** (F-62), and not as a default a behavior may override: the settlement seam runs under `FAILURE_REORDER_RESOLUTION` and the action seam under `FAILURE_INSERTION`/`FAILURE_PLACEHOLDER_MOVE` for **every** behavior. So a free-drag `moveTo()` whose prepare throws reports an _insertion_ failure, mapped by D-64 to a **presentation** fault, for library arithmetic. Checkpoint E asks whether anything in `kernel/` knows an insertion or a placeholder; three published constants do. **No SPI change, no wire change, and no consumer is affected** — D-64 gives consumers a coarse `code`, never a stage — so the only readers are kernel-tier authors, of whom there are none yet. That window is why this is now or never: D-68 made these names permanent semver surface one revision ago. The rejected alternative was behavior-supplied action stages, which widens `BehaviorConfig` for every behavior to fix a naming problem. | F-62; the stage names D-64 moved to `kernel.js` and D-68 published |
| D-75 | **Implemented, 2026-08-16 (Phase 19).** **A name is qualified when two entries need different structures under one word, and only then.** `FreeDragLift` (against `kernel.js`'s numeric `LiftMode`) and the symmetric pair `SortableErrorContext`/`FreeDragErrorContext` (against one `DragErrorContext` carrying a behavior's own result). `DragAxis` and `DragGeometry` stay unqualified. | Two structurally different types under one name is the identity hazard F-61 found inside the package, arriving from the outside. **The sortable's rename is included deliberately**: leaving `DragErrorContext` where it is would give the first behavior the unqualified word by arrival order, and the package has no released consumer, so symmetry costs one type name now and cannot be had later. The rule is kept narrow on purpose — qualifying every name a second behavior _could_ want would rename half the surface for a collision that does not exist. | 03 §The export topology's `DragErrorContext` cell |
| D-76 | **Landed 2026-08-15.** **D-34 and D-35 land as a discrete kernel step before Phase 19**, against the sortable alone, with zero free-drag code in the tree: the parameterized activation staged type threaded through the construction generics, the lift session recording what `write` composed, `LandingContext.from` reading it, the `BehaviorLiftSession` projection, 05's D-35 matrix group executing, and M-1/M-3 re-measured for the two scalar writes. | **Both are contract-normative and unimplemented** (F-63), and free drag is not merely their first consumer — it is incorrect without them. It stages nothing at activation, so under the shipped signature its `prepare` returns an element it does not own and its `effect` ignores it (F-44 exactly); it constrains its visual on three independent paths, so under the shipped computation every constrained drop opens its landing from a position the visual has never been at (F-45 exactly, and Phase 11 found one of that shape the hard way). **The sequencing is the decision, not the content.** D-35 changes the hot path and re-verifies the landing for the behavior that already ships; landing it beside a new behavior gives any regression two candidate causes and moves the sortable's numbers under a phase that is not measuring them. | `plan.md` §Phase 14's booking of D-34/D-35 to "Phases 19–20" |
| D-77 | **Implemented for the sortable, 2026-08-15**, as the discrete step before Phase 19; `freeDrag()`'s half lands with the behavior itself, under D-69's row. **Required configuration is a required first argument; every later fragment stays `Partial`.** `sortable(root, config: SortableConfig, ...fragments: ReadonlyArray<Partial<SortableConfig>>)` and `freeDrag(item, config: FreeDragConfig, ...fragments: ReadonlyArray<Partial<FreeDragConfig>>)` — **amending D-69's signature**. `y()` and `xy()` return the installer itself, so an axis is written `axis: y()` inside that first argument. The package-wide rule 03 §Public option domains froze is replaced by: **a construction-time throw is permitted only for an invariant over what installers _contribute_** — required configuration is a type obligation, and a consumer scalar's domain belongs to the consumer, the platform, or the seam that consumes it. Five explicit sortable checks go (`axis` missing, `items`/`onReorder` not functions, the axis installer's missing geometry, `threshold`'s domain); `claim`'s single-writer collision stays; `copyUniqueItems` stays and is classified; the landing duration narrows to `=== Infinity`. Sortable construction **diagnostics: 6 → 1** — a count of explicit checks, not of failures: deleting the axis checks deletes their _messages_, and a JS-authored violator still meets a native `TypeError` from the dereference underneath (03 §Validation carries the per-slot table). Free drag's `onDrop` becomes a compile error instead of a first-release `FAILURE_RESOLUTION`. | **F-67, re-derived across both behaviors as one decision.** §1.2 decides it: a missing required slot _is_ expressible at compile time, so paying runtime bytes for it is exactly the rule that section names. **The form matters more than the verdict.** 03 §Fragment factories already asked whether the missing case could be a compile error and answered no — but it examined only a **variadic tuple-merge type**, which it correctly bounded on two failures: a spread call has no tuple to fold, and a `Partial<SortableConfig>` variable erases the literal's key set, so the check silently passes for the one input it exists to catch. **A required first parameter has neither failure**, because it folds nothing and a `Partial` value is not assignable to it. The third option was never considered, and it is strictly sound where the second was unsound. **What is lost, named:** a later fragment can no longer supply a required slot, so a preset that carries one must declare it (`Pick<SortableConfig, 'axis'> & Partial<SortableConfig>`) and be spread into the first argument — one line, still type-checked. **The residual runtime hole is stated rather than patched:** a consumer who writes `{ items, ...spread }` where the spread carries `items: undefined` merges to nothing — and it does **not** reach a seam. It fails at the **construction pull**, where `sortable()` calls `items()` before returning, with the consumer's own native `TypeError` and no classification, no coarse code and no `onError`. ~~fails at the first drag, and is diagnosed by the seam~~ named a route that does not exist: admission reads the prebuilt snapshot, and the only in-seam pull is `action.prepare(COLLECTION)`, which `controller.invalidate()` alone dispatches — so nothing gets as far as a drag. The verdict §1.1 gives it is unchanged and is what the row rests on: "the consumer would only break their own code". **`y()`/`xy()` unwrap because the wrapper's only purpose was fragment position**, which required slots no longer have; the alternative kept `Pick<'axis'>` and made the natural spelling `axis: y()` a type error while the correct one was a spread (§9, §12). | F-67; 03 §Public option domains; 03 §Fragment factories' open question; the D-69 signature |

### The D-77 landing review (D-78…D-80)

The review of D-77's landing ([`../reviews/phase-18-after-review.md`](../reviews/phase-18-after-review.md)) found the implementation correct and the documentation around it not. Of its twenty findings, most are stale prose the commit did not carry. **Three needed a decision rather than an edit**, and they are below. **None reopens D-77's implemented behavior** — no seam, no signature and no runtime outcome moves — which is the constraint the review was read under, and D-80 is the only row that touches `src/` at all.

| ID | Decision | Why | Supersedes |
| --- | --- | --- | --- |
| D-78 | **Implemented, 2026-08-16.** **A public type's closure resolves within its own tier plus the tiers below it, and `AxisInstaller` is published at the ordinary tier.** The ordinary tier closes over `sortable.js ∪ drag.js ∪ sortable/feature.js`; the kernel tier's `kernel.js ∪ drag.js` is unchanged (D-68). `AxisInstaller` is re-exported from `sortable.js` because it names a slot of `SortableConfig`; its own closure — `FeatureContext`, `SortableContribution`, `InsertionGeometry` and everything those name — stays declared at `sortable/feature.js`. **The opacity claim is retracted at all three sites** (03 §The schema, `src/sortable/feature.ts`, `src/sortable/config.ts`): a `sortable.js`-only consumer **can** write `axis: (ctx) => ({ … })`, because contextual typing resolves a parameter's type structurally whether or not its alias is re-exported. `tests/docs.node.test.ts` gains a per-entry run over the ordinary tier's union, beside the kernel tier's. **The two obligations are separate instruments and neither substitutes for the other**: the per-tier TypeDoc run proves only that a public type's closure stays **within the allowed tier union** — it is satisfied whether or not `sortable.js` re-exports `AxisInstaller`, because `sortable/feature.js` is _in_ that union, verified by deleting the re-export and watching it stay green; **publication is a hoistability property**, and it is pinned by the packed ordinary-consumer fixture, which hoists `const hoistedAxis: AxisInstaller` importing `sortable.js` alone and fails to compile without the re-export. | **P18A-04 and P18A-05, and the first was disproved by compiling rather than by reading** — a file importing only `src/sortable.ts` typechecks clean against the very shape 03 says it cannot express. **The naive repair is worse than the defect**: `AxisInstaller`'s closure is substantially the whole of `sortable/feature.js`, so applying _every alias it names_ transitively would publish the middle tier at the ordinary one and dissolve D-61. **Progressive disclosure governs discoverability and reusable authoring vocabulary, not prohibition** — a tier decides where a name is _declared_; it never decides what the compiler will let a consumer write, and it does not by itself decide what they can hoist either, since the hoistable name can be — and here is — published at their own tier. An ordinary-tier consumer may author an installer **inline** _and_ hoist it as `AxisInstaller`; what `sortable/feature.js` buys is the **lower-level named authoring vocabulary** for an installer's parts, not the ability to construct the shape. Claiming otherwise made a property of the entry map out of a property of neither. So the closure rule becomes tier-scoped, which is the `kernel.js ∪ drag.js` precedent applied at the tier that never had one, and D-61's rung survives intact. **Not a freeze-rule event**: an addition to the public consumer surface, which ground rule 5 and §Status both put outside the rule — the same class as D-68. **Stated behavior-neutrally on purpose**: free drag's config carries installer-typed slots and its own middle tier (F-64), and a rule written as a sortable fact gets re-derived per behavior, which is exactly how F-67 happened. | 03 §The schema's opacity sentence; `src/sortable.ts`'s _every alias it names_; the whole-run docs check as the ordinary tier's only closure instrument |
| D-79 | **Implemented, 2026-08-16.** **The `animate()` domain gets an artifact under [`../measurements/`](../measurements/), and its justification narrows to what the call path can reach.** The measurement pins the two load-bearing clauses and only those: `animate()` **rejects** `NaN`, negatives, `-Infinity`, strings and objects, and **accepts `Infinity` and never completes** — which is why the surviving guard is `=== Infinity` and not `Number.isFinite`. **`undefined` is struck from the claim at all five sites**, and `'auto'` is marked reachable from JavaScript only. | **P18A-08 and P18A-09.** Five sites carry the claim — `src/sortable/slots.ts`, `src/sortable/landing.ts`, the README, 03 and 07 — as the sole justification for _deleting_ `requireFinite` rather than relocating it, and **nothing in the tree records it**: no measurement, no probe, and no test of any clause. That is the evidence type this package otherwise refuses — `../measurements/` exists for exactly it, D-76's gate demanded re-measurement before it could land, and `tests/decisions.node.test.ts` exists because a green suite is evidence about the _implemented_ contract only. **The verdict is unaffected and rests on two legs, neither of them the falsified one**: the byte argument (`CODE_OF_SIZE.md` §1.3) and the `Infinity` invariant, which is a library-liveness concern no platform check covers. **What fails is the third leg** — _a finiteness test would have wrongly refused values the platform accepts_ is not a property of this code, because `src/sortable/landing.ts` coalesces `duration` to its default before `animate()` can ever see `undefined`. A reason that survives only as a JS-only string must not be carried in five places as though it were the argument. | the unrecorded Chrome 150 claim at its five sites; F-67's _accepts `'auto'` and `undefined`_ clause |
| D-80 | **Implemented, 2026-08-16.** **Two statements are scoped to what holds, and the collection is validated before the first installer runs.** **(a)** The `AxisInstaller` backstop covers **presence, not well-formedness** — `{ insertion: {} }` passes assembly and surfaces later at the seam — so 03 §Validation and `src/sortable/feature.ts` state the pairing as _the type is total for a TypeScript consumer; the runtime dereference exists for a JavaScript one, and checks that the object exists_. **(b)** **`copyUniqueItems` moves ahead of `assemble`**: the collection is pulled, validated and copied once, before any installer runs, and the validated copy is passed into `install`. _The unwind is total_ then holds across construction rather than within `assemble`, and needs no scope clause. | **P18A-12 and P18A-13 — and the second is rated a scope note by the review that found it, which understates it (F-68).** `copyUniqueItems` throws a `TypeError` on a duplicate element from `src/sortable/runtime.ts`, inside `createSortableRuntime`, i.e. **after `assemble` has returned**: a consumer whose `items()` yields the same element twice leaves every installer's `retire` hook collected and never run, plus a kernel and a realm `draggable()` has already built and nothing destroys, because `arm()` is never reached. Consumer-triggerable, and the shape is worth keeping: **D-77 retained exactly one construction-time throw, and it sits in the one window the unwind does not cover.** **Making the sentence true is cheaper than scoping it** — a reordering, no bracket machinery, no new state — and it is safe precisely because **installers are externally inert by contract** (03 §A feature is a function factory: an installer may allocate, but may not attach a listener, write the DOM, or acquire anything needing release), so running them after the validation is not externally observable. **This does not reopen D-77**: `copyUniqueItems` stays and is still classified at its other call site, and the throw's type, message and destination are unchanged — only its position moves. **The opposite move from D-37, deliberately**: that row narrowed a domain because the guarantee could not be checked, and here it can be made true for free, so narrowing would be the lazy reading of the precedent. | 03 §Validation's backstop sentence and its `src/sortable/feature.ts` twin; the unstated scope on _the unwind is total_ in 03 §Validation and [05](05-lifecycle-invariants.md) |

### The Phase 19 contradictions (D-81, D-82)

Phase 19 landed green and raised two contradictions rather than designing around them. Both are **claim-side**: the shipped behavior is the intended one in each case, and what disagrees with it is a sentence written before the behavior existed. **Neither decision changes free drag's design or the kernel SPI.**

| ID | Decision | Why | Supersedes |
| --- | --- | --- | --- |
| D-81 | **Implemented, 2026-08-16 (Phase 20).** Its contract half landed with the decision and its source half landed here: `src/free-drag/bounds.ts` now names all four seams and states that `FAILURE_ACTION_PREPARE` is unreachable from this row, so the deferred table's witness stopped holding and that row retired with it. **The bounds source's failure attribution is _the seam that next renders_, and the contract says so instead of naming one stage.** The feature resolves lazily on the first `apply` after a staleness mark; `invalidate()` is a staleness flag that calls nothing. A garbage source therefore surfaces at `activation.effect` (`FAILURE_ACTIVATION` → `interaction`) on its **first** resolve, and thereafter at `moved` (`FAILURE_RENDERER_WRITE`) or a `TAG_POSITION` effect (`FAILURE_ACTION_EFFECT`), both `presentation`, or at `release.prepare` (`FAILURE_RELEASE` → `interaction`). **`FAILURE_ACTION_PREPARE` is not reachable from this row at all.** 07 §Validation, B-4 (b), L-3, L-4 and [05](05-lifecycle-invariants.md)'s free-drag group are corrected, and **Phase 20 asserts all four paths plus the negative** — `tests/free-drag/validation.browser.test.ts` §a garbage bounds source, whose last row is that nothing surfaces from the action that marks the rect stale. | **F-71, and the stronger attribution is not semantically required.** A stage answers _where the library was standing_, not _whose data was bad_ — the reading 07's own D-74 note already takes when it declines to defend `presentation` for library arithmetic, and the reading this column's header (_where a bad value surfaces_) states outright. Under it the shipped attribution is correct and the sentence is what is wrong. **The two ways to restore a single stage both buy documentation with runtime**: resolving eagerly inside `invalidate()` puts a layout read or a consumer call on **every scroll and resize event**, which is the one path this package refuses to load; re-raising through a third behavior action contradicts 07's `actionTags: 2`; and a fourth `MotionConstraint` member widens a contract type to move a label. **F-71 understated itself twice and the corrections matter more than the finding** — F-73 (activation is the first resolve, so the dominant path for a bad source was named by neither the finding nor the code comment) and F-74 (the two-consumer-call barrier is not instantiated by the first-party `bounds()`, so L-3's fixture cannot discriminate as written). **What stays open is the general question, not this row**: whether a stage is a promise or a description is Checkpoint E's, and D-81 deliberately answers it only for the width of one table cell. | 07 §Validation's `action.prepare` cell; B-4 (b)'s `presentation` for `bounds`; L-3's _two-consumer-call_ premise; 05's matching clause |
| D-82 | **The duplicate activation-time box-quad traversal is deferred to Checkpoint E, beside F-65, and the SPI does not move in Phase 19 or 20.** `release.effect`'s literal _nothing_ is separately withdrawn: free drag writes the release delta through `lift.write`, F-39 applied to a behavior with no placeholder, and **unbranched** because free drag has no `command` and therefore no pointerless operation. | **F-72, and the freeze rule already decides the first half.** Only a failing executable case reopens the SPI, and one extra traversal per activation — on a path already doing layout work, never per sample, with the coefficients captured once — is not one; it is a measurement Phase 21 owes. **The pairing is the reason to defer rather than to close**: F-65 says the kernel measures something only the sortable needs and F-72 says it withholds something free drag needs from the same traversal, so they are one question about what `ActivationScope` carries, and answering half of it under a phase that is not measuring either would set the SPI by whichever behavior asked last. **The `release.effect` half is not a deferral and must not travel with it** — it is a live contradiction between a normative table cell and shipped, correct code, and F-39 makes the write normative rather than incidental (02 §The command destination, 05 §F-39). Leaving _nothing_ standing would license a later pass to delete the write and pass the contract. | 07's `release.effect` row |

### The Phase 20 mismatches (D-83, D-84)

Phase 20's executable matrix drove the rows that Phase 19's prose only described, and two claims did not survive contact. Both are **claim-side**, both are corrections to sentences rather than to seams, and **neither behavior changes**.

| ID | Decision | Why | Supersedes |
| --- | --- | --- | --- |
| D-83 | **The terminal guarantee is scoped to the observable-start boundary, and per-behavior documents cite [05](05-lifecycle-invariants.md) I-31 rather than restating it.** A classified failure **before** `onStart` — the progress marker still at `MINTED` — publishes `onError` and **no terminal**; a classified failure **after** it publishes **exactly one** on a still-live controller. Applied to 07's B-4 (b) and 05's free-drag group, which now assert the terminal per row against the boundary instead of once for the table. **No invariant changes and no machinery is added**: I-31 and D-66 §The progress marker already say exactly this. | **F-75, and the interesting part is that the rule was already right.** I-31 has read _if a start is notified **and the controller remains alive**, exactly one terminal follows — **and only then**_ since Revision 2.1, and it is stated behavior-neutrally. What failed is a **restatement**: B-4 (b) re-expressed it as a property of a validation table, and a table has no antecedent to carry, so the condition was dropped in the copy. Three rows — `handle`, `visual` and `bounds` on its first resolve — then asserted a terminal that D-66 forbids, and Q-15 forbids for the same reason a press that never activates publishes none. **The scoping is therefore not a concession to free drag** and must not be written as one: patching only 07 would leave the sortable's inherited phrasing free to drift the same way, which is why the durable half of this row is the prohibition on restating I-31 at all. **The executable matrix is what made it undeniable** — the rows were asserted as observed, so the contradiction arrived as a passing test against a contract that says otherwise, which is the one form these documents cannot argue with. | 07 B-4 (b)'s table-wide _exactly one terminal_; 05's free-drag restatement of it |
| D-84 | **A throwing `visual` resolver is `FAILURE_ADMISSION` (1) → `consumer`, in both behaviors**, and `visual` is no longer paired with `onStart` in any row. Free drag calls the resolver inside `admit`; the sortable calls it inside `seedDraft`, admission's second half. `onStart` keeps `FAILURE_ACTIVATION` (2) → `interaction`. 07 §Validation splits the row, and B-4 (b) and 05's group are corrected with it. | **F-76, and the derivation error is the reusable part.** The row's stage was taken from the kernel lines that _name_ the stage rather than from the site that _calls the resolver_ — which is **exactly** how D-81's `bounds` row went wrong one decision earlier, from a different starting point. Two instances make it a pattern worth naming: **an attribution is a fact about a call site, and this table's column header says so**; reading it off a stage constant's neighbourhood produces a plausible answer that is wrong by one seam. **`consumer` is also the better attribution on the merits**, so nothing is traded for accuracy here: the visual resolver is consumer code, it runs before any operation identity exists, and `interaction` would tell a consumer their pointer interaction failed when their own function threw. **The pairing was the mechanism of the error** — two slots sharing one row cannot carry two stages, so the moment they diverged the row had to be wrong about one of them, and it was wrong about the one that moved earliest. | 07 §Validation's `visual`, `onStart` row; B-4 (b)'s `interaction` for `visual`; 05's matching clause |

### Checkpoint E's blockers (D-85…D-87)

The [Checkpoint E review](../reviews/checkpoint-e/review-checkpoint-e-1-codex.md) raised six blockers. **Three need a decision and are below; three are implementation fixes under contracts that already say what to do**, and saying so is itself the disposition rather than a deferral:

- **E-02** — the activation terminal barrier is claimed at `:410-420` and implemented before the last contributed call in both behaviors. **I-36 already requires the read where the review puts it**: after the last consumer-reachable or third-party call, before the next write, publication, progress advance or `onStart`. No new liveness SPI, no shared extraction — the two fixes having the same shape is not evidence of a common mechanism, and D-53's `host.closed` is already in both behaviors.
- **E-03** — `reportFailure` after `destroy()`. **I-31's `destroy()` exception and D-53's logical-closure latch already forbid it**; the guard belongs at the two kernel-owned call sites because the rule is controller lifetime, not domain settlement, and both behaviors reach them identically.
- **E-05** — an invalid `home` result escaping its own quality seam. **D-49 already attributes it to `FAILURE_LANDING_TARGET` on the quality track**; validating and copying inside `anchorTarget` is what makes the shipped code match the attribution (07 §Validation) already publishes. Kernel-side point validation is refused: only free drag crosses a consumer-result boundary here, and the sortable's anchor is behavior-owned DOM geometry.

**F-65 is unchanged and stays booked for Phase 21 measurement.**

| ID | Decision | Why | Supersedes |
| --- | --- | --- | --- |
| D-85 | **Implemented, 2026-08-16 (Checkpoint E remediation).** **`ActivationScope` carries `inheritedSpace`: the inverse inherited linear part, or `null`, derived from the measurement `acquireLift` already took before it mutated anything.** The behavior multiplies; it never measures. `BehaviorLiftSession` is unchanged, no `Box` crosses the seam, and `captureLocalSpace` and its four private index constants are deleted with the second traversal. `InheritedSpace` publishes at `kernel.js` as part of the scope's closure (D-68). | **E-01, and it is a correctness fix rather than the overhead item F-72 filed it as.** The second traversal runs **after** acquisition has changed positioning, dimensions, top-layer state and transforms, so a single activation could lift on one coordinate snapshot and report consumer deltas from another; box-quad's own contract says a second walk may legitimately disagree. **The two reads also disagreed on failure** — `acquireLift` throws `FAILURE_ACTIVATION` for an unreadable space while `captureLocalSpace` silently substituted identity — and deriving from the one successful measurement leaves nothing to disagree with. **Placement is against the review's suggestion and the reasons are in [02](02-kernel-behavior-contract.md) §Why `inheritedSpace` is on the scope**: the session is a positive `Pick` describing post-lift state, and it already holds a same-shaped projection with a **different value** — `null` for both lifted modes — so reusing it would hand free drag the identity under `LIFT_FLAT`, wrong silently. **The tension with F-65 is real and is not hidden**: this adds unconditional kernel work for a need only one behavior has, which is F-65's own complaint pointed the other way. It is accepted on two grounds that F-65 does not have — the marginal cost is arithmetic over a buffer already materialized rather than a **layout read**, and it buys correctness rather than bytes. Phase 21 measures it beside F-65. | F-72's Checkpoint E deferral (D-82), which read this as measurable overhead |
| D-86 | **Implemented, 2026-08-16 (Checkpoint E remediation).** **Free-drag action legality is defined per tag: `TAG_POSITION` and `TAG_POLICY` are legal in `ACTIVATING` and `ACTIVE`, and are deterministic no-ops after.** `prepare` returns the seam's existing `null` discard on a late tag — no failure, no report, no terminal. The `host.closed` guards stay; they answer a different question. | **E-04, and the two tags are defined separately because their reasons do not travel together.** `TAG_POSITION` is refused for **correctness**: from `onEnd` it is FIFO-ahead of `RETIRE` and writes through an already-disposed lift, which the review's probe caught as a stray inline transform on a released element. `TAG_POLICY` is refused for **hygiene**: it writes no geometry and re-enters declared consumer and third-party slots for no observable effect once no later sample exists. The sets coincide today only because free drag takes no sample after release. **`ACTIVATING` is included deliberately, not incidentally** — a `moveTo()` from `onStart` is intentional retargeting, and the queue may drain it in either of the two live phases. **Not solved in the kernel**, and the wrong fix is instructive: filtering behavior tags by phase in the router would break the sortable, which **intentionally** accepts a collection `invalidate()` in phases where free-drag geometry must be frozen — a collection change during settlement is real information and a position write is not. Behavior action legality is behavior knowledge, which is why the kernel routes tags without interpreting them. | 07's action rows, which stated no phase legality at all |
| D-87 | **Implemented, 2026-08-16 (Checkpoint E remediation).** **Cross-behavior installers are unsupported and non-assignable, enforced by one optional `never` slot per direction**: `FreeDragContribution` declares `insertion?: never`, `SortableContribution` declares `constrain?: never`. Negative declaration tests both ways, `AxisInstaller → FreeDragInstaller` named explicitly. | **E-06, and the mechanism is chosen to state the incompatibility rather than to mark it.** Every real slot on both records is optional, so they were mutually assignable and free drag's `plugins` accepted an `AxisInstaller` whose `insertion` the assembler then **silently discarded** — a supported middle-tier API accepting a value and doing nothing with it. **One exclusion per direction is the minimum that works**, because assignability is decided on the declared alias rather than on a particular body, so it also refuses the required-`insertion` case. **A runtime brand and a phantom discriminant are both refused**: each costs a field or a name and says only _these are different_, while `insertion?: never` says the true thing — a free-drag contribution has no insertion, and an object carrying one is not one. **An installer returning `{}` stays assignable to both, correctly**: an empty contribution is genuinely valid for either, and refusing it would be a boundary drawn for its own sake. **The obligation is stated rather than discovered** — a new single-writer slot needs its `never` twin, or the boundary reopens for that slot. At three behaviors that becomes a matrix and an explicit common contribution contract earns its keep; at two it does not, which is F-64's judgement unchanged. | the mutual assignability of `SortableInstaller`, `AxisInstaller` and `FreeDragInstaller` |

### The second Checkpoint E audit (D-88…D-91)

An [independent audit](../reviews/checkpoint-e/review-checkpoint-e-1-claude.md) run after the D-85…D-87 remediation found four items needing judgement. **One re-opens a decision this ledger recorded as closed**, which is the row to read first. CE1-05, CE1-06, CE1-07, CE1-08 and CE1-09 are mechanical remediation under contracts that already hold and need no decision; CE1-10 and CE1-11 are dispositioned below the table as Checkpoint E evidence.

| ID | Decision | Why | Supersedes |
| --- | --- | --- | --- |
| D-88 | **Implemented, 2026-08-16 (second Checkpoint E remediation).** **The cross-behavior exclusion rule is key-set totality: both contribution records declare the same key set, and a key a behavior does not implement it declares `?: never`.** Free drag adds `placeholder`, `beforeInsertionMove` and `afterInsertionMove` to its existing `insertion`; the sortable's `constrain` already stands. `tests/composition.declaration.test.ts` asserts `keyof` equality between the two records, and each open slot gets an **unannotated** counter-example driven through free drag's public `plugins`. | **CE1-01, and D-87's sufficiency claim was wrong** — _one exclusion per direction is the minimum that works_ closed the two defining capabilities and left three sortable slots open. **What appeared to close them was TypeScript's weak-type detection**, which an all-optional target gets for free and which `retire` — the one member both records genuinely share — defeats. A hoisted **unannotated** installer carrying `{ placeholder, retire }` compiles into `plugins` today and is silently discarded by an assembler reading three slots, which is the exact defect E-06 was raised about, surviving the decision that claimed to close it. **The boundary is asymmetric because the slot sets are**, and _one per direction_ described a symmetry that does not exist. **The maintenance obligation becomes a check rather than a promise**: D-87 wrote the twin rule as future work, and it was already owed for three existing slots — a key-set equality assertion fails at the moment a slot is added without its twin, which is the only form that cannot drift. **The pinning requirement is load-bearing and is stated with the rule**: every row of the existing suite starts from an annotated alias, which is the one form the old exclusions already caught, so a fix leaving the suite unchanged is unpinned exactly where the boundary escaped. **The rule is exact over the seven known keys and structurally cannot refuse an eighth, which is recorded as a bound rather than left to be discovered.** A hoisted unannotated installer carrying an unrecognized slot — `{ onGridSnap, retire }` — typechecks into free drag's `plugins` and is discarded by an assembler reading three slots: the same _a supported middle-tier API taking a value and doing nothing with it_ shape CE1-01 objected to, arriving from a typo or a future slot name rather than from the other behavior's capabilities. **It stays a bound, because nothing in the contract asks for more.** Exactness needs branded or exact contribution types — machinery D-88 deliberately declined, and which F-64/D-61 argue against, since a shared exactness mechanism _is_ the cross-behavior composition vocabulary Checkpoint E is convened to gather evidence for rather than to build. [03](03-feature-composition.md) §Fragments are public, installers are opaque already records that structural typing admits a matching literal whether or not a name is exported, and the middle tier's versioning promise covers the members it **declares**, not the absence of members it does not. **The residue is asymmetric with CE1-01's, and that asymmetry is why it is tolerable**: a foreign _capability_ silently discarded is a supported act producing nothing, while an unknown _key_ is a value no tier ever declared. Recorded so a later reader does not read the `keyof` equality row as a stronger guarantee than it is. | D-87's _one exclusion per direction is the minimum that works_, and its reading of the declaration suite as sufficient evidence |
| D-89 | **Implemented, 2026-08-16 (second Checkpoint E remediation).** **`anchorTarget`'s accepted arm reads the committed delta rather than re-deriving it**, so it calls no constraint and there is no fifth `constrain.apply` seam. `release.prepare` derives the release delta and `release.effect` writes it; the join reads the same values. **The invariant this rests on is stated and pinned**: nothing may write that delta between `release.prepare` and the join, which D-86 now guarantees by making both behavior tags no-ops after `ACTIVE`. | **CE1-02, and the redundancy is what makes the choice easy.** The accepted arm re-computed the same values from the same committed frame **in order to re-enter third-party code** — the seam's own comment claimed _no consumer call and no DOM read_, the I-36 Category-1 table omitted `constrain.apply` entirely, and D-81's re-derived four-seam enumeration missed it. Removing the re-derivation makes all three true at once, which is why it beats the alternative of declaring a fifth seam and owing it an attribution, a barrier and a table row. **The barrier defect goes with it**: `host.closed` is read immediately before `home` and not before `deriveMotion`, so a third-party `apply` ran after logical closure while the resolver beside it was guarded — a split inside one seam, E-02's shape one seam further on. **Reachability was narrow and that is not the argument**: with the first-party `bounds()` the resolve is a cache hit, and the defect is real for the third-party constraint the middle tier was opened for. **The derivation error repeats D-81's exactly** — a constraint seam reasoned from where staleness is _raised_ rather than from where `apply` is _called_, for the third time. | 07's `anchorTarget` row; the I-36 Category-1 table's omission; D-81's four-seam list |
| D-90 | **Implemented, 2026-08-18 (Checkpoint E closure).** **Re-opened 2026-08-18 and closed the same day; the source half shipped 2026-08-16.** **A `MotionConstraint`'s members are never invoked with that `MotionConstraint` as their receiver; an author may not depend on `this`** — the mechanism half withdrawn by D-93, the referent corrected by D-94. ~~Contribution members are invoked detached. ~~The three call sites~~ **The five sites** (D-93; this stale count corrected by D-94) are consistent with it~~ — **five sites, not three** (D-93; the stale count survived here alone, CE6-02), and _detached_ survives as the convention's **name** rather than as a sentence anything rests on. The contract section landed. ~~The marker cannot express a partial landing, so the split is narrated here~~ — **both halves are now in the tree, so the split it narrated is closed.** The sentence is on `MotionConstraint` itself, where the third-party installer author reading the published type meets it, and `tests/free-drag/anchor.browser.test.ts` drives each call site alone with a constraint whose members record the receiver they are handed — ~~three members reached through four sites~~ — **three members reached through five sites** (D-93): `invalidate` is called from both the scroll/resize listener and `TAG_POLICY`, and `retire` from both the normal retirement and the assembler's construction unwind. **Closure was measured against the recorded reversion rather than against the fixture's shape**: binding the three `spec.ts` sites fails three rows, and binding the assembler's `retire` fails ~~the fourth~~ **the fourth and the fifth** (D-93, measured), each run before this marker was flipped. The `spec.ts` comment that asserted the missing sentence is true now and says so (C-1). | **CE1-03.** The tree **called** the same three-member record both bound and detached, so a `this`-reading constraint **worked** from `controller.invalidate()` and **threw** on the first scroll and at every retirement — the pre-fix state, and the whole reason the convention had to be written rather than assumed. **Detached rather than bound**, for the reason 03 §Assembly already gives for the sortable's identical flattening — one property read and one call at the seam — and because the alternative leaves **two conventions in one package** for the author who writes against both middle tiers. ~~Nothing fails today only because the one shipped constraint closes over its state, which is the non-discriminating control F-74 names, met by the first third-party author instead of by a test.~~ **True for two days, and no longer** (2026-08-18): `bounds()` is still the F-74 control and still proves nothing, but a fixture that records its receiver now fails at whichever site is bound, so the convention is met by the suite rather than by the first third-party author. | the unstated convention at `MotionConstraint`'s ~~three~~ **five** call sites |
| D-91 | **Implemented, 2026-08-16 (second Checkpoint E remediation).** **`moveTo(point)` requires two finite coordinates. A non-finite one discards the action** — `prepare` returns `null`, nothing is written — **and is surfaced on the platform reporter**: no classified failure, no `onError`, no terminal. A malformed `point` is **not** checked and throws naturally at the read, reaching `FAILURE_ACTION_PREPARE` → `presentation`. | **CE1-04, and the asymmetry with E-05 was decided by silence rather than by judgement.** The surface takes two consumer-supplied points; E-05 decided `home` must be read, checked and copied inside its seam, and `moveTo`'s domain appeared in no table at all. **The silent-table precedent does not transfer**: a `NaN` `threshold` is silent because the drag never starts and no operation exists, whereas here a live operation continues with `onStart` fired and a terminal owed, the visual frozen on one axis, and `NaN` in every geometry object and in the pinned target — the same composition E-05's reason names. **D-77's rule points the same way**: a consumer scalar's domain belongs to the seam that consumes it, and this one is consumed by `action.prepare`. **The channel differs from E-05's deliberately, and the difference is the blast radius, not the principle.** `home`'s value can only be refused by failing the seam that produced it, so it is classified and the drop stands; `moveTo`'s can be refused **before it is written at all**, so refusing it costs the operation nothing — and classifying it would end a live drag over a consumer's arithmetic, which is a worse answer than the poisoning it replaces. Both validate at the seam that consumes the point, and both leave the operation standing. | 07 §Validation's silence on `moveTo` |

**CE1-10 and CE1-11 are recorded as Checkpoint E evidence and change nothing.** CE1-10 measures the shared composition vocabulary and finds it thin: `realm` has one installer reader, `report` has none — it is read by the two **assemblers** — and `root` has no reader anywhere and **denotes a different element per tier**, the collection root for the sortable and the dragged item for free drag. That is evidence _against_ widening F-64, not for it, and the one thing genuinely owed is a sentence documenting `root`'s per-tier meaning — a doc fix, not a redesign. **That sentence is now written, in [03](03-feature-composition.md) §A fragment is a plain declarative partial config, on `FeatureContext` itself**: the sortable's `root` is the **collection root** and free drag's is the **dragged item**, one role and two referents. The third Checkpoint E pass found the deliverable still outstanding four days after the disposition recorded it as the only thing owed, which is the ordinary fate of an obligation written in prose and booked to nothing; it is discharged **normatively** here rather than left to a source comment, and `shared/composition.ts` mirroring it is follow-through with nothing left to decide. CE1-11 answers carried item 5 with a count instead of an argument: after D-74 the vocabulary is behavior-neutral in **name** everywhere, and **three of thirteen stages have a single behavior producer** — two the sortable's, one free drag's. Two behaviors is a small sample and a single-producer stage is not thereby a behavior-shaped stage; the item closes against the measurement rather than against a claim, and **no vocabulary changes**.

### The calling convention's second tier (D-92)

D-90 closed for free drag and the closure exposed the symmetric question it had assumed away. The rationale D-90 used to **choose** detached over bound was that binding one tier would leave _two conventions in one package_ for the author who writes against both middle tiers — and it cited [03](03-feature-composition.md) §Assembly (D-45, H-5) as the precedent the sortable already set. **That precedent stated a technique, not a convention.** 03 said the assembler flattens the pair _so the call sites stay one property read and one call_, and derived no author obligation from it; `InsertionGeometry`'s own declaration says nothing at all. So the argument D-90 rested on was true of the code and unwritten anywhere a reader would look.

| Decision | What and why | Supersedes |
| --- | --- | --- |
| D-92 | **Implemented, 2026-08-18.** **The detached calling convention is shared by `MotionConstraint` and `InsertionGeometry`, and both declarations state it.** ~~The detached calling convention is package-wide, and every published contribution type states it.~~ **Narrowed before implementation**: the package publishes other contribution types — `PlaceholderSlot`, `LandingStart`, `DisplacementHook` — and D-92 neither states the convention on them nor pins them, so the wider claim would have been false in the tree the moment it was recorded. The scope is the two lifted **geometry/constraint capability records**, which are the pair the _two conventions in one package_ argument is actually about. `InsertionGeometry` carries it alongside `MotionConstraint`, and 03 §Insertion geometry is settled presentation geometry is upgraded from technique to obligation (done here). ~~Sortable-side discrimination is owed~~ **— discharged to D-90's standard.** The falsifier was recorded before the fix and run after it: restoring the record and calling all four members bound in `assemble`'s flat slot construction fails ~~**five**~~ **six** rows (D-93, re-measured once the unwind row landed), and re-binding a single lift fails exactly that member's row plus the aggregate. `y()` and `xy()` pass under the same reversion, which is the F-74 control demonstrating itself. **The rows assert the receiver is never the nested `InsertionGeometry` rather than that it is `undefined`**, because the sites disagree on what `this` is — ~~the four sites~~ **five** (D-93): `resolve` and `invalidate` receive the flat slot record, `measure` and the normal `retire` receive `undefined`, and the construction-unwind `retire` receives the assembler's internal hook array — so the uniform `=== undefined` assertion free drag's rows then used would fail the conforming tree at ~~two~~ **three** sites, and free drag's own rows were narrowed to the obligation by D-93 rather than ported. **The `spec.ts` convention comment is rewritten to the invariant only.** | **Yes, the rationale requires it, and the requirement is stronger than symmetry.** _One convention in one package_ pays off only if both tiers state it; **one statement plus one silence is worse than two silences**, because an author who reads `MotionConstraint`'s sentence and then meets `InsertionGeometry`'s silence has positive evidence that the package distinguishes the two — which it does not. **The sortable's exposure is the larger half**: the assembler lifts **four** members where free drag lifts three, `retire` among them as a bare reference pushed into `retireHooks`, and `AxisInstaller` is re-exported from `sortable.js` (D-78), so the reader can be an ordinary-tier consumer rather than only a middle-tier one. **Discrimination is owed for the reason CE1-03 exists**: `y()` and `xy()` close over their state, so they are the same non-discriminating control F-74 names — nothing in the suite would notice the sites being re-bound, and a stated convention no instrument defends is what let three passes go by. **Scope is the calling convention only.** No shared contribution base, no exactness mechanism, no re-opening F-64: two published types each gain a sentence, one document gains an obligation it already implied, and one suite gains rows shaped like the four D-90 landed. | 03's _the assembler flattens the pair … so the call sites stay one property read and one call_ read as a performance note; D-90's implicit assumption that the sortable's precedent was already stated |

**Production comments state the current invariant, not the remediation that produced it.** Recorded here because D-92's remediation must fix an instance of the opposite: `src/free-drag/spec.ts`'s convention comment now spends most of its length on the pre-fix tree — _it is stated because the tree was split three ways_, what _worked_, what _threw_, what _failed only because_ — which is decision-record material that has a decision record. A source comment is read by someone changing the code beside it, and its whole job is to say what must remain true; the history of what used to be false is noise at the point of use and rots as soon as the next change lands. **The rule is not _no rationale in comments_** — a comment may say why an invariant exists, in the present tense, when that is what stops someone deleting it. What does not belong is the narrative of a defect and its repair, and _this comment used to be false_ least of all. That belongs in this ledger, and in this instance it is already here.

### The calling convention's published form (D-93)

D-92 stated the convention in both tiers and both instruments discriminate. Closure verification then found the two tiers stating it **differently**, and the difference is not stylistic: the sortable published the **obligation** — _the receiver is never the record you return_, whose referent D-94 then had to correct — while free drag published the **mechanism** — _calls them as bare functions_. Only one of the two is true at every site.

| Decision | What and why | Supersedes |
| --- | --- | --- |
| D-93 | **Implemented, 2026-08-19.** **The published guarantee is the obligation, and the receiver is otherwise unspecified.** D-90 **aligns with D-92's formulation**: a contributed member is never invoked with the contributed record as its receiver — the record being the **capability record the member is declared on**, once D-94 corrects the referent both tiers inherited — and what the receiver _is_ at any site is not promised. The _calls them as bare functions_ clause on `MotionConstraint` is withdrawn. **Free drag has five sites, not four** — `retire` is reached from the normal retirement **and** the construction unwind — so the enumerations in 07 and COVERAGE move to five and the instrument gains a row driving the unwind path. **The rows assert `receiver !== own()`, not `=== undefined`**, in both tiers, and the sortable gains the same unwind row for the same reason. ~~Also owed~~ **— discharged.** `MotionConstraint` now attributes the lifts correctly (the spec takes `apply` and `invalidate`, the assembler takes `retire` and owns both of its call sites), and 07's replacement wording had to be corrected in the same pass: _calls them from its own state_ was the **same mechanism claim one rung down**, false of the assembler's unwind, so 07 now states the obligation alone and records the attribution as code rather than as guarantee. **Both falsifiers were run before this marker moved**: re-attaching free drag's sites fails all five of its rows, re-binding the sortable's four lifts fails all six of its; the conforming tree was restored and re-verified at 23 passing. **The premise was verified directly rather than taken from the decision**: the unwind hands the hook the internal array, so `=== undefined` was genuinely false there. | **The weakest site-total contract wins, and it is already written in the other tier.** `receiver !== own()` holds at all five free-drag sites and at the sortable's; `=== undefined` holds at four and fails at the construction unwind, where `retireHooks[i]!()` is an indexed call and hands the hook **the internal array** — verified directly, and not an exotic path: it is the path a failing installer takes, which is the path a third-party author most often reaches while developing. **The stronger claim buys the author nothing**, because depending on `this === undefined` is itself outside contract, so the extra clause adds no capability and only a way for the library to be wrong. **It survives representation changes, and the alternative does not**: a later free-drag refactor to the sortable's flat-slot shape would fail four rows _without breaking the convention_ — rows defending the flattening instead of the contract, which is F-74's non-discriminating control inverted, a test that cannot fail for the right reason replaced by one that fails for the wrong one. **And two formulations of one convention in one package is D-92's own defect one rung down**: one statement plus one silence was worse than two silences; one statement plus a stronger, false one is worse still, because an author who reads both concludes the tiers differ. **Making every site literally bare is rejected, not overlooked.** `const hook = retireHooks[i]!; hook();` would make the mechanism claim true today, and it is refused because it buys machinery for prose — the assembler would carry a call-shape obligation on behalf of a guarantee no author can use — and because it is perpetually one refactor from false again: any indexed call, `.call`, `Reflect.apply` or iteration form reintroduces it silently, and nothing notices unless an instrument drives every site. **The tree is not bent to make an over-strong published claim true when the claim narrows to the true one at no cost to the author.** The indexed call also stays the smaller emit, which is the tiebreak `CODE_OF_SIZE.md` would apply anyway. | D-90's _calls them as bare functions_; 07's and COVERAGE's _each of the constraint's four call sites_; free drag's `=== undefined` assertions |

**The obligation is what `this` is _not_ — and the two tiers already measured why that is the only assertable form.** D-92's rows found the four sortable sites disagreeing about what the receiver **is**: `resolve` and `invalidate` are called off the flat slot record and receive it, while `measure` and `retire` receive `undefined`. Free drag's five now disagree the same way, with a third value — an internal array — at the unwind. A convention that names the receiver has to be re-derived every time the calling code's shape changes; a convention that names one thing the receiver is never does not.

### The calling convention's referent (D-94)

D-93 settled **what** is promised — the receiver negative alone. Closure verification then found that the promise names the **wrong object**, and that D-93's own rule had not been applied to two live statements. Both are contract defects with no runtime component: the implementation and the five-site instruments in each tier were already right.

**The referent error is not a wording slip, and its signature is that the sentence is true of the tree it forbids.** `AxisInstaller` returns `SortableContribution & { insertion: InsertionGeometry }`, and a free-drag installer returns `{ constrain }`. The members under the convention live on the **nested** capability record. A fully bound implementation — `contribution.insertion.resolve(…)`, `contribution.constrain.apply(…)`, the exact thing the convention exists to forbid — hands each member the nested record and **never** the contribution object. So _never your contribution object_ was satisfied by the violation. A guarantee the forbidden implementation already meets forbids nothing.

| Decision | What and why | Supersedes |
| --- | --- | --- |
| D-94 | **Implemented, 2026-08-19.** **The forbidden receiver is the capability record the member is declared on** — the `InsertionGeometry` or `MotionConstraint` object itself — **not the contribution object that carries it.** An author may not depend on `this`; every other receiver value stays unspecified (D-93, unchanged). **And no live sentence may promise the mechanism.** `InsertionGeometry`'s _that flattening is a calling convention, and it binds the author_ is withdrawn, as is 03's copy of it and 03's _calling it bare_, which contradicted the measured paragraph directly below recording `resolve` and `invalidate` receiving the flat slot record. Flattening, lift location and current receiver values survive **only** where they are labelled measured code or evidence. D-90's stale _the three call sites_ moves to **five**. _Detached_ survives as the convention's **name**; nothing may rest on it, and every statement of the obligation spells out the receiver negative. **Scope is the two capability records, unchanged from D-92**, and the pre-existing bound is restated rather than closed: free drag's top-level `retire` and the sortable's `placeholder` and displacement hooks are lifted and called the same way, and no published type says so — the convention has never covered them and D-94 does not widen it. | **CE6-01 and CE6-02, and the first is the load-bearing one for Phase 21.** A refactor calling `insertion.resolve()` bound to the nested record satisfies the published sentence **word for word** and fails every intended row — while the reverse, an optimization that stops flattening one member without ever using its record as receiver, satisfies D-93 and is currently forbidden by two live sentences. **A contract and its falsifier cannot use different identities for the one forbidden receiver**, and here the falsifier was right the whole time: both fixtures type `own()` as the nested capability record and compare against it, while their prose calls it _the record the installer returned_. **The prose drifted where nothing executes it** — the instruments, which do execute, never made the error. **Mechanism-as-promise is the same defect D-93 named, one document further out.** D-93 removed _calls them as bare functions_ from `MotionConstraint` and left an equivalent claim standing on `InsertionGeometry` and in 03; a rule applied in one of the two tiers it was minted to unify reproduces D-92's _one statement plus one silence_ exactly. **The cost of leaving it is not cosmetic**: Phase 21 cannot tell which transformations are permitted when one document says the flattening is the promise and another says only the receiver is. | D-92's and D-90's _the record you return_ / _your contribution object_ / _the installer's record_; 03's _the flattening … binds the author_ and _calling it bare_; D-90's _three call sites_ |

**The obligation is stated on the declaration the author reads, and the referent is stated with it.** The sentence lives on `InsertionGeometry` and `MotionConstraint` rather than on the contribution types, which is D-92's placement and is why the referent correction is small: the type carrying the sentence **is** the forbidden receiver, so the durable form is _these members are never invoked with this record as their receiver_ — self-referential, and immune to the drift that produced CE6-01, because there is no second object for it to name by mistake.

### The Phase 21 measurement contract (D-95, corrected by D-96 and D-97)

Phase 21 opened with four standing obligations that cannot change a decision as written, and with F-65 still carrying _Phase 21 measures it_ across a checkpoint that closed without the number. **The measurement plan is [measurements/phase-21.md](../measurements/phase-21.md)**; this row records the narrowing, because three of the four withdrawals are edits to what 05 §Measurements (landed 2026-08-02) obliges.

**D-96 is the audit of D-95 rather than a second plan.** It was taken before a harness was built, on the seven arms whose stated result could not have supported the decision attached to it, and it adds no measurement and moves none of D-95's withdrawals — five of its seven corrections remove work.

**Run outcomes, so this row is not read as a plan after the plan was executed.** M-3′ (2026-08-19) held the union identity exactly and did not reopen the topology; M-1′ (2026-08-19) closed three of its four decisions and raised P-01 rather than answering it; **M-6 (2026-08-20) opened P-01** — on a stream Chromium itself coalesced, `lift.write` reaches p95 3 and max 7 per rAF tick, so the saving a write gate makes is real and D-97's suspension of D-96's **within-frame** half closes with it. The across-frames withdrawal is untouched. **Phase 22 owns the gate and its flush obligation; nothing was implemented.** **M-4′ (2026-08-20) closed all three of its decisions and opened none of them**: the eager rebuild is the dominant term of a committed move (80–97% bare, 36–69% with `layoutAnimation()`), and both P-02 and P-03 close as accepted, named costs — the six-scalar stride saves ~8% of the rebuild at 800 rows against a slot-sequence-equivalent stride-1 composition, and the clean-resolve placeholder read is ≈0.04 ms per frame, flat in list size. The bracket's read placement is exact and asserted in CI, and is recorded as a **read count** rather than as a forced-layout count, per D-96. **The eager position was not re-decided on cost.** **M-5 (2026-08-20) closed F-65 and opened nothing**: window 1 is 0.8–1.6 µs, bounded in situ at ≤ 0.0125 ms and ≤ 2.7% of measured activation at a 32-level transformed ancestry, and arm B confirmed D-85's ground — the `inheritedSpace` derivation is ≤ 6 ns and takes **zero** layout-facing reads. **D-82's deferred SPI question does not open**; it opens on the absolute alone, over 0.5 ms. **Arm B could not have reopened D-85 and is not offered as though it could.** **M-2′ (2026-08-20) closed the last of the five**: per-controller cost is predominantly **common** — 5618 B for a sortable against 4505 B for a free drag, so 1112 B (20%) is behavior-specific and the other 80% is carried by both, with the common figure stated as a **bound** on the kernel's share rather than as the kernel's share. A mixed population costs exactly the mean of the two pure ones, to the byte, so **nothing is shared between controllers** and population heap is linear in the count. Retention over the declared 1000-controller / 1000-drag workload **does not grow with the drag count**, at an instrument whose sensitivity was measured (0.14) rather than assumed. **P-02's retention half opens as a Phase 22 candidate** — 6.29 MB retained at a 100 000-item high water, of which 5.12 MB is scalars no axis rule reads — recorded under P-02's own row, since the §M-2′ decision list does not name it. **Phase 21 therefore closes with three Phase 22 candidates and no optimization work started: P-01, P-06 and P-02's retention half.** The records are [`m3-prime.md`](../measurements/m3-prime.md), [`m1-prime.md`](../measurements/m1-prime.md), [`m6.md`](../measurements/m6.md), [`m4-prime.md`](../measurements/m4-prime.md), [`m5.md`](../measurements/m5.md) and [`m2-prime.md`](../measurements/m2-prime.md).

| Decision | What and why | Supersedes |
| --- | --- | --- |
| D-95 | **Phase 21 takes five measurements — M-3′, M-1′, M-4′, M-5, M-2′ — under the rule that a measurement is taken only if a named decision changes on at least one of its outcomes**, and withdraws four obligations that fail it. **(1) Free drag's part against M-1's cliff is settled by counting**: the cliff is in behavior-part fields, `FreeDragFramePart` has **five** against the sortable's eight, and no workload returns anything but _below_. The polymorphic **shared call site** survives as the open number. **(2) M-2's closure-vs-static-spec model comparison is dropped** — both arms are stand-ins that do not ship, and re-running two stand-ins against a second behavior measures the stand-ins; the real per-controller heap by behavior replaces it. **(3) M-4's _two axis features rather than one_ is not constructible** — exactly one axis feature installs, and `bench/size` asserts the exclusivity. It is replaced by the sharper question it should have asked: **q7's answer 2 was measured when the axis rebuild was lazy, and Phase 11's own answer 1 then made it eager and interior to the committed-move bracket** (C4-03 recorded the change and recorded that nothing was re-run). **(4) F-72 is not a subject**: D-85 closed it as a correctness fix and deleted the traversal. What D-85 left is unconditional kernel arithmetic only free drag reads — **F-65's shape pointed the other way** — and it is measured beside F-65 under M-5, not under a closed finding. **M-5 is the only new measurement**, and its decision rule is **fixed before the run and three-valued** — close, open, or indeterminate — with the indeterminate band stated so the result cannot be argued either way post hoc (see the plan's §The decision rule). Both deciding outcomes are real: below a threshold F-65 closes as an accepted, named cost and the `ActivationScope` SPI does not move again for this reason; above it, the question deferred by D-82 opens with the evidence it was waiting for. The reproducibility standard is unchanged and gains three operational rules already learned in practice — **one measurement file per run; both arms of a comparison in one session; a one-tick null is a null and not an upper bound.** | **Two failure modes, one of which this project has already had.** A measurement whose result cannot alter a decision consumes the phase's credibility and then gets cited as though it had settled something — which is how q7's answer 2 is currently cited: correct when taken, taken against a lazy rebuild, and quoted after the rebuild became eager because C4-03 recorded that the measurements _were not re-run_ and the citation did not carry the caveat. The other is the reverse: **F-65 was booked to a measurement phase by two documents and a checkpoint, and no one ever owed a workload for it**, so a finding with a named decision behind it nearly aged out at the checkpoint convened to read it. **Narrowing is not deferral**: three of these four withdrawals close their obligations by argument — arithmetic, a non-shipping stand-in, an unconstructible configuration — rather than moving them, and each names the question that survives it in the same sentence. **§Check D-56 stays Phase R's** and is not re-imported; Phase 21 may not record it as satisfied. | 05 §Measurements owed's M-1 frame-shape clause, its M-2 construction-model clause and its M-4 shared-read clause, as re-stated by `plan.md` §Phase 21; D-82's pairing of F-72 with F-65 as one Phase 21 subject |
| D-96 | **The Phase 21 measurement contract is corrected by a validity audit before any harness is built**, on the rule that a number which cannot support its stated decision is worse than no number, because it is quoted afterwards as though it could. Seven arms change. **(1) M-1′ arm D's frame gate closes on arithmetic rather than on a run**: a gate removes _resolves_, resolves are bounded by `apply` calls, free drag installs **no frame task** (`createFrameTask` has one caller, `src/sortable/runtime.ts`) and the deployment path commits about one sample per frame — so the gate can only collapse resolves where more than one `apply` runs inside a frame, which is not free drag's shape, and the harness's batched dispatch is precisely the regime that would hide that. The burst shape becomes a **structural assertion** that asserts on every run (one resolve per `apply`, any `k`), the continuous cost becomes **telemetry**, and frame coalescing is **withdrawn as an option** with the workload a re-proposal owes named. **(2) M-1′ arm B's control was not alternation-matched** — M-1 quoted its polymorphic figure against a _monomorphic_ run, so the 6× carried the alternation and a different mean field count alongside the shape count; the control is now the same alternating driver over two objects of one shape, and the residual field-count bias is stated as favouring 1. **(3) M-1′ arm E cannot establish retention by M-1's method**: growth read without an intervening `gc()` is uncollected churn or retention indistinguishably, so the retention half takes M-2's collected-pair discipline. **(4) M-4′'s _forced-layout count_ is not observable in-page** — q7's instrument counts `getBoundingClientRect` calls, and a forced layout is a read that follows a write; the observable is the counter's value at the bracket's four boundaries. **(5) M-3′'s topology test had no threshold and needs none**: the exact form is a **union identity over module graphs** the harness already collects, with the byte delta demoted to telemetry. **(6) M-2′'s frame-task decision is unreachable by its workload** — the frame task is sortable-owned and per-controller, so a mixed population removes frame tasks rather than stressing them and cannot move the 148 B margin; it is withdrawn and only the per-behavior heap and retention decisions remain. **(7) M-5's rule is repaired in three places**: the denominator is defined as the activation path **excluding consumer slots** (otherwise the share is a fixture parameter), the two absolutes are **re-derived as fractions of a 16 ms frame** rather than borrowed from q7's list-pass column, and the opening condition drops its relative disjunct. The stub also acquires the standard's equivalence check. | **Six of the seven are the same failure in different clothes: the measured quantity is not the quantity the decision turns on.** A resolve-per-sample count is not resolves-per-frame; a polymorphic-vs-monomorphic ratio is not a shape-count effect; net heap without a collection is not retention; a read count is not a forced layout; a byte delta is not a duplicated module; a percentage of an undefined denominator is not a share. Each was correctable by naming the observable rather than by enlarging the phase, and **no measurement was added and none of D-95's withdrawals moved** — the audit removes work in five of seven places. **The seventh is a contradiction rather than a mismatch**, and it is the one that would have done the damage: M-5 opened the SPI on _over 10% of measured activation_ while stating two paragraphs later that a cost large as a fraction and small as a number is not on its own a reason to change an SPI. A rule cannot hold a share against a close and also let it force an open. **The audit's own limit is stated too**: it corrects how each arm is scored and does not revisit which measurements Phase 21 takes, which stays D-95's. | D-95's M-1′ arm B control and arms D and E as written; M-3′'s _near the sum_ byte test as decision-driving; M-4′'s forced-layout count; M-2′'s frame-task-policy decision; M-5's opening disjunct on measured activation and the q7 derivation of its two absolutes |
| D-97 | **P-01 does not close on D-96's arithmetic, and D-96's arithmetic is corrected on its own subject.** M-1′ raised, rather than assumed, whether the argument that withdrew frame coalescing for lazy constraint resolves also closes the per-sample visual render. It does not. **Three claims were running together and are separated here.** _Our architecture_ guarantees only that `lift.write` is **synchronous with its trigger** — no queue, no batching, no frame task between the event and `visual.style.transform` — which makes writes-per-frame equal **triggers**-per-frame and delegates the bound rather than setting one; it also **adds** a trigger the platform does not control, since `moveTo()` dispatches `TAG_POSITION` synchronously, renders through `lift.write`, is legal in `ACTIVATING` and `ACTIVE`, and is unbounded per frame. _The platform_ guarantees no maximum of one `pointermove` per presented frame: coalescing is a permitted user-agent behaviour, `getCoalescedEvents()` exists because a dispatch may carry several movements, and `pointerrawupdate` exists as the un-throttled stream — throttling is user-agent **policy**. _Typical Chromium behaviour_ is frame-aligned dispatch at about one per frame, and that is what D-96 leaned on while presenting it as structure. **The consequence for D-96 is real but narrow.** Its withdrawal splits: **holding a rect across frames stays withdrawn** on the independent ground that it serves the previous frame's rect and **moves the visual**, and **collapsing several resolves inside one frame is suspended**, because that variant is not a correctness change and rested only on the dispatch-rate claim. The measured half — one resolve per `apply` at every `k`, asserted continuously — is untouched, and D-70's lazy resolve stays affirmed. **P-01 cannot inherit the surviving leg**, because a write gate is **not** a correctness change: only the last transform written before a paint is presented, so collapsing writes inside one frame is work elimination with an identical visual result. **M-6 is added** — a census of `lift.write` and `constrain.apply` calls per `requestAnimationFrame` tick, under input injected through the **browser's input pipeline** and never `dispatchEvent`, with `getCoalescedEvents().length` as the control that makes a too-slow injector visible instead of letting it read as a bound. Its rule is fixed before the run and three-valued, the cost of one write is **not** measured unless the census opens the question, and a close names its engine. | **The implementer was right to raise it and right about which half was missing, and the verification changed the answer.** Extending an argument by analogy is how a claim outlives the evidence for it — the failure D-95 named and q7's answer 2 had already suffered. Here the analogy fails on the one property that distinguishes the two gates: the bounds gate was refused because it **changes what the user sees**, and a write gate does not, so the reason that carried the first cannot carry the second. **Checking it exposed that D-96's own withdrawal was half-grounded** in exactly the way this project's standard warns about elsewhere — M-1 recorded its cliff as _Chromium's and not a portable constant_ while D-96 promoted a Chromium dispatch policy to structure. The correction is a **suspension rather than a reversal**, and only of the variant the dispatch claim was load-bearing for. **The economy is deliberate: M-6 counts before it costs.** The saving a gate makes is `(writes per tick − 1) × one write`, so the count is decisive and the cost is worthless until the count exceeds one — measuring the write first would produce precisely the convenient number M-1′ declined to quote. **What a write gate would cost, if the census opens it, is a flush obligation on every terminal path** — a deferred write that never runs leaves the visual behind the operation — and that is Phase 22's to weigh. **M-1′ is otherwise closed as measured**; this decision adds no obligation to it. | D-96's characterisation of the deployment path as committing about one sample per frame, as structure rather than as engine behaviour, and the unqualified withdrawal of frame coalescing that rested on it; D-95's count of five measurements |
| D-98 | **M-4′ question (c) resolves the other way: the full-candidate rebuild is an implementation of a correctness requirement, not the requirement, and it opens P-06.** The run concluded that the rebuild's contents need not shrink because _its reads are the whole of it, and the read set is the destination view the rule is defined over_. **The second clause does not follow from the first.** The rule's **domain** is the destination view — the index must _contain_ n − 1 entries. The cache's **refresh set** is whatever invalidation says changed, and those are different quantities. `RectIndex` already serves cached geometry rather than fresh: `refresh` returns on `!dirty && measured === snapshot.version`, so on a frame where the pointer travels inside one slot it reads nothing and the previous scan stands. **Correctness here rests on invalidation completeness, not on freshness**, and a rebuild is owed only where an invalidation says geometry moved. **At a committed placeholder move the library knows exactly what moved, and knows it in the same statement**: `view.insertion` is the destination gap, the incumbent gap is the placeholder's slot, and q7 answer 1's finding — _items outside `[old gap, new gap)` do not change position_ — is why `layoutAnimation()`'s displacement set is the span. The bracket therefore re-reads n − 1 rows to rediscover a fact the library computed one line earlier and already hands to another feature through `DisplacementView.insertion`. The **slot→element mapping is invariant across the move**: `movePlaceholder` moves only the placeholder node, the snapshot version does not change, so `items` order is identical and only rects move. **P-06 is recorded — an eager rebuild that stays in the same window and updates the span from preserved geometry instead of rescanning — with four boundary conditions it may not cross.** (i) **The reason must be carried.** `invalidate()` is one flag whose doc states that any single reason forces the same full rebuild; the fast path exists only when the sole cause is a committed move the library itself made, so scroll, resize, collection publication, activation and release must still force a full scan — which widens `invalidateInsertion`, a real contract cost. (ii) **No `box` resolver.** `getBox` returns an arbitrary consumer element whose geometry the list's flow does not govern, so _only the span moved_ is not a claim the library may make about boxes it did not lay out; under the default `box === visual` it may. (iii) **`y()` only.** In a linear flow, removing an extent at one gap and inserting it at another shifts every row strictly between by one scalar; in a **wrapping** container the same move is a reflow that changes lines, and q7's own reopener already says the span assumption is axis-list-shaped. `xy()` keeps the full scan until someone proves the wrap case. (iv) **What the full scan silently buys is periodic re-synchronisation** — not immunity, since the warm path already serves stale rects within a slot dwell, but at every invalidation point it absorbs drift the library never observed: a CSS transition on a row, a late image or font, a container query, a consumer hook mutating the tree. Committed moves are frequent, so that correction is frequent today; an incremental path gives it up between full scans, and P-06 must state a re-sync policy rather than discover one later as a hysteresis bug. | **The conclusion was an implementation assumption wearing a semantic argument's clothes, and it is worth being precise about which half was right.** _Its reads are the whole of it_ is correct and is the run's finding. _So the read set is the destination view_ is a separate claim that nothing in the run establishes — it restates what the index holds as though it constrained how the index is maintained. **The evidence against it is the project's own, twice over**: the cache's warm path proves the library does not require freshness, and q7 answer 1 proves it already knows the moved set, computes it in the bracket, and publishes it to a second feature in the statement before the rescan. **The delta does not even need to be modelled** — margins and `gap` make it more than the placeholder's own extent — because reading **one** span row after the move and differencing it against its cached value yields the exact scalar in O(1) reads. That is an existence proof that the boundary is real, not a design, and Phase 22 owns the design. **Nothing measured is disturbed and no prior answer is contradicted.** P-02 and P-03 stay closed on their numbers; the eager position stays outside cost-driven re-decision under D-95, and P-06 is deliberately shaped to preserve it — the candidate is a **smaller** eager rebuild in the **same** window, never a lazier one. What does change is a framing: 03's _a committed move performs one full pass, the one it was always going to perform on the next frame_ is a description of the current implementation and not a floor, and the re-timing claim it sits inside is unaffected. | M-4′'s answer to question (c) as written in [`measurements/m4-prime.md`](../measurements/m4-prime.md), and its _no Phase 22 optimization opens from this run_ |
| D-99 | **Phase 21 closes, and Phase 22 enters with three candidates and a fixed order.** **Bookkeeping.** All six measurements ran — M-3′, M-1′, M-6, M-4′, M-5, M-2′ — and every obligation is run, withdrawn by argument (D-95's four, plus M-2′ decision (a) by D-96) or **owned elsewhere and named**: **§Check D-56 stays Phase R's** and is not recorded as satisfied; **P-04 and P-05 were never given a Phase 21 workload** and stay backlog, un-evidenced and explicitly not closed — M-5's activation figures are context for P-04's low prior, not a closure. **F-65 stays closed** as an accepted, named cost (≤ 0.0125 ms, ≤ 2.7% of activation) and **the `ActivationScope` SPI does not move**. Three stale readings are corrected: `plan.md`'s _M-4′ opens no Phase 22 optimization_ (superseded by D-98), M-4′'s _neither P-02 nor P-03 opens_ (its **time** half), and M-2′'s retention claim, below. **M-2′'s retention conclusion is narrowed to what its instrument supports.** The falsifier reads back **0.14** of a known 800 B-per-drag injection, so the series rejects a **material linear** per-drag retention — nothing accumulating at or above ≈46 B per drag — and is **not** a claim of absence: sub-threshold retention is not excluded, nor is one whose observability degrades with drag count, since 0.14 was established at 1000 drags and not re-established at 4000. **No further measurement is owed**, because no Phase 22 candidate turns on the difference. **The three candidates, with payoff, invariant cost, evidence gap, next step and stop condition, are tabulated in `plan.md` §Phase 22.** **P-06** leads — the rebuild is 80–97% of a bare committed move and its remaining uncertainty is **architectural rather than empirical**, so it needs a decision and not another run; D-98's four boundary conditions and the eager window are preserved, and the one-span-row delta stays an existence proof. **P-02's retention half** follows, split into a **shrink policy** (local to a private module, no SPI) and a **stride narrowing** (re-couples the deliberately dimension-neutral `RectIndex` to the axis rules), kept as separate sub-candidates. **P-01 is last and is expected to be declined**: arm 1's platform surplus is real at p95 3–8 writes per rendering opportunity, but one `lift.write` is a single `style.transform` assignment, so a decline follows unless it costs ≳ 23–80 µs. **The write cost is measured before any gate is designed**, and the **terminal-flush obligation survives into any deferred-write design that is ever attempted.** **The ≈80% common per-controller heap is prioritisation context and not a fourth candidate.** | **The order is not the order of the numbers, and that is the decision.** P-06 is also the largest, but it leads because Phase 21 has given it every number it will get: what stands between it and code is a contract for reason-aware invalidation and a re-synchronisation policy, which is architecture. P-02's shrink half is second because it is the cheapest correct thing in the package — one private module, no SPI, an exact structural figure — and its only open question is a premise about the collections consumers hold rather than a property of the code. **P-01 is last precisely because its measurement is expected to end it**, and that is worth taking: the arithmetic predicts a decline, and taking the number turns a prediction into a recorded verdict instead of an assumption. Designing the gate first is how a flush obligation across every terminal path gets paid for a benefit that was never there — the failure mode this project has been avoiding since D-95. **The M-2′ narrowing is the same discipline applied to a result rather than to a plan.** The measurement is good and its conclusion was nearly right; what it could not support is the absolute form. An instrument that reads back a seventh of a known injection rejects a material leak and nothing stronger, and a later phase quoting _leaks nothing_ would be citing a bound the run never had — which is exactly how q7's answer 2 outlived its evidence. | `plan.md` §Phase 21's _M-4′ … none of them opens a Phase 22 optimization_; M-4′'s question (b) as scoped to P-02 as a whole; M-2′'s _the workload leaks nothing that accumulates_ and its matching closure bullet |
| D-100 | **Implemented 2026-08-21.** **P-06 survives as a _verified_ incremental refresh, and the verification is the architecture rather than a safeguard on it.** Design: [`p06-verified-refresh.md`](../p06-verified-refresh.md). D-98's span claim was checked rather than inherited, and it holds in a linear non-wrapping flow for a structural reason — a row's position is the cumulative sum above it, and removing the placeholder's contribution at one gap while adding the identical contribution at another leaves prefix and suffix sums unchanged. **It fails in four ways, all real**: collapsing margins make the two contributions unequal, wrapping makes `δ` non-scalar, a `transform` on one row moves it outside any flow argument, and **scroll anchoring** shifts everything under the library's own placeholder write with the compensating `scroll` event dispatched after the bracket. **One property makes three of the four cheaply detectable**: in a linear flow any change to any row shifts every subsequent row, so flow drift is _suffix-shaped_ and visible at the last candidate. **So the span is a hypothesis the feature checks with four reads — an in-span witness that yields `δ` by differencing against the cache, an after-witness, a suffix witness and a before-witness — and any refutation falls back to a full rebuild in the same window.** `δ` is **measured, never modelled**, which removes margins, `gap` and box-sizing from the design; D-98's one-row idea survives as that mechanism and not as its proof. **Reason-aware invalidation costs nothing**: `measure` has exactly one call site — the committed-move bracket — so being called _is_ the signal, `invalidate()` and `measure()` are consecutive statements in one run-to-completion `action.effect` so nothing can interleave, and "the bracket plus something else" is a **count** of pending invalidations rather than a new SPI. ~~**The re-sync policy is a full rebuild every `k` moves, and it caps the payoff at `k×`** — that exchange rate is the policy, stated rather than buried; `k = 8` turns ~3.4 ms into ~0.42 ms at 800 rows.~~ **Corrected against the run (D-102's record).** `k×` is a **ceiling, never the expected value**, and the sentence after it assumed a verified move costs approximately nothing. It does not: at 800 rows a verified move is ≈1.0 ms of the general path's 3.5 ms, because **the dominant term of the rebuild in the deployed regime is the forced layout after the placeholder write, not the per-row reads** — one flush, then ≈3 µs per row. **The real ceiling is `full / verified` ≈ 3.5×**, and `k` only decides how close to it the mean lands: `(full + (k−1)·verified) / k` puts `k = 8` at the measured **2.67×**, or 76% of what is achievable. **So `k` is not the binding constraint and D-100's _raise it once the instrument has run_ is retracted** — the instrument has run, `k = 16` would buy ~12% and `k = 4` would cost ~24%, and raising it spends drift tolerance for very little. `k = 8` stays. **The contract cost is one additive field on `InsertionRuntimeView`**, the fifth widening of a consumer-declared view in the same form as the previous four, carrying a value the behavior's object already holds. **The eager window, `y()`-only, `getBox === null` and the eight-condition boundary are preserved**, and the slice's load-bearing deliverable is a **permanent structural equivalence assertion** — every fast-path buffer must equal a full scan's. **Landed as designed**, with the implementation record and the measured before/after in `p06-verified-refresh.md` §What landed. **Three things the run settled that the design left open.** The `count` contingency **did not fire**: at 50 rows the verified path is 1.62× (bare) and 1.22× (animated) faster against a 0.0006 ms resolution, so **no count gate exists and no threshold was invented** — the eight conditions are the whole boundary. **The realised saving is ~2.6× at 800 rows and `k` is not what caps it**: a verified move costs ≈1.0 ms of the general path's 3.5 ms while doing four reads instead of 799, because the dominant term of the rebuild in the deployed regime is the **forced layout after the placeholder write** (≈1 ms at 800 rows) and not the per-row reads (≈3 µs each). Raising `k` would therefore buy little and spend drift tolerance for it. **Two divergences were recorded rather than absorbed** — the bare `__DEV__` read, and `xy()` carrying the fast path in the shared `createRectIndex` closure — **and both are settled by D-101 and D-102 below**; the sizes quoted with the second were budget overruns quoted as deltas, corrected in `p06-verified-refresh.md` §What D-101 and D-102 settled. | **The candidate was admissible only if its state could be locally falsified, and the honest answer was that the state D-98 named cannot be.** A remembered previous gap can silently disagree with reality, and no amount of care makes it self-checking. **Verification is what dissolves that**: the remembered gap is demoted from a fact to a proposal, and four reads decide whether it was right — so the fast path never depends on state it cannot check, and the equivalence assertion turns the whole hypothesis into something the suite refutes rather than something the design asserts. **Scroll anchoring is why the suffix witness is not defensive programming.** It is triggered by exactly the DOM mutation this fast path is for, it moves rows the span argument says cannot move, and its `scroll` event arrives too late to invalidate the frame that read them — a case the analysis found rather than assumed, and the single strongest reason not to ship the unverified form. **The `k×` cap is stated because it is the one place the design's payoff and its safety are the same number**, and a later phase raising `k` should know it is spending drift tolerance rather than tuning. **What this decision declines to do is as much of it as what it does**: no second widening, no ninth condition if the instrument fails, no attempt to detect a single transformed row, and no tuning beyond the first value. | D-98's expectation that reason-aware invalidation requires widening `invalidateInsertion`; its one-span-row scalar delta read as a sufficient mechanism rather than as an existence proof |
| D-101 | **Implemented 2026-08-21.** **`__DEV__` is package vocabulary, not kernel vocabulary, so the behavior tier binds it directly and `DEV` is added to neither column of 02 §What stays internal.** The flag is declared in `src/globals.d.ts` at package scope; `kernel/dev.ts`'s `DEV` is the kernel's own local binding. The bare read is **ratified as the correct form**, not tolerated — it was `rect-index.ts`'s when this was decided and **D-102 moved it to `sortable/verified-refresh.ts` the same day**, which is where the tier's one binding now lives; `rect-index.ts` is byte-for-byte its pre-P-06 self and contains none. The ratification is of the _form_, not of the file: adding `DEV` to the internal groups would assert a behavior-tier reach into `kernel/` that must not exist, and publishing it would put a build-time flag with no runtime meaning into the surface a third-party behavior compiles against. **The cost is zero in both directions** — `__DEV__` folds to `false`, the branches are dead, the binding is dropped — so this is a boundary decision and not a size one, and it must not be argued on bytes. **The trigger for the next step is named**: a second behavior-tier dev assertion in a **second module** creates a tier-local `sortable/dev.ts`, which is the behavior tier's own binding and still imports nothing from `kernel/`. **The gate should be made to see this rather than left blind to it**: `tests/kernel/vocabulary.node.test.ts` inspects `../kernel/*` import specifiers, and a bare global is invisible to it — so the tier-local binding rule is owed a matching assertion (one `DEV` binding per tier, `__DEV__` read nowhere else), or the next author will re-litigate it by reaching for the import instead. **Landed 2026-08-21 as three assertions rather than one**, in that file's §The `__DEV__` binding: at most one binding module per tier; **exactly one read of the ambient inside it**, because a module guarding branches with `__DEV__` rather than with its binding makes the first rule unenforceable; and the positive list of binding tiers, because the two negatives are both satisfied by a tree where nothing binds it and by one where a new tier quietly acquires an assertion. **Corrected 2026-08-21 against the P-06 review** (P06-03, P06-04). A tier is the **top-level directory under `src/`**, not `dirname` — under which a second binding at `sortable/sub/a.ts` would have been its own tier and satisfied a rule it plainly breaks — and the third assertion pins the set of **tiers** rather than of files, so the first can now fail on its own: a second binding inside `sortable/` fails it while the tier list stays green, and moving the binding between `sortable/` modules fails neither, which is correct because the rule is about tiers. Line comments are stripped as well as block comments, and the gate now **states its limits instead of implying a parse**: a string literal carrying the token counts as a read, and the failure mode is a loud false positive rather than a silent false negative. Falsified in five directions — a sub-directory binding, a new tier, a within-tier move, a line comment and a string literal — each landing where the rule says it should. | **The implementer routed around an executable boundary and said so, which is why this is a decision and not a finding.** Reaching across and then legislating for it would have widened a second contract to land a design whose declared budget was one additive field. **The gate's own discriminating test gives the answer**: a third-party behavior author has no `__DEV__` define, so the ambient is not something the boundary hands over in either direction — it is a property of this package's build, and `kernel/dev.ts` already requires a missing define to fail loudly at import. **One premise underneath `kernel/dev.ts` has expired and is raised rather than resolved here**: that comment justifies a consumer being unable to re-enable assertions on the grounds that _behavior authoring is not on the public surface_, and D-68 put it there. Nothing in P-06 turns on it and this decision does not touch it, but it is a live gap for whoever next reads the dev-build story. | the reading of 02 §What stays internal under which any behavior-tier reach for a kernel-tier concept must be named in its table |
| D-102 | **Implemented 2026-08-21.** **The implementation boundary moves: `xy()`-only compositions must carry no P-06 machinery, and the budgets stay red until they do not.** The verified fast path currently lives in `createRectIndex`'s shared closure, so every sortable composition links code that D-100's condition 1 makes unreachable for `xy()` — measured at **+135 B** on the minimal `xy()` composition and **+135 to +164 B** across the sortable surface. **This is not a shared-code cost and is not re-based away.** The fast path is not shared cache logic used by one axis; it is `y()`-only **by contract**, and code only one feature may ever execute belongs in that feature's module graph — the rule every other optional feature in this package is already held to, and the one `bench/size`'s `withoutAxis` exclusivity assertions exist to enforce. **The required property, not the factoring**: an `xy()` composition's module graph reaches no P-06 module, asserted the same way every other absence claim is, with the dimension-neutral `RectIndex` keeping the full scan and staying dimension-neutral. **The residue is the falsifier**: if what must stay behind in `createRectIndex` after the split is not materially smaller than +135 B, the split bought nothing, and the decision reverts to accepting the cost with a re-base rather than being defended. **Until then the declared budgets stay red**, so the number stays visible. **Landed, and the falsifier answered decisively: the residue is _zero_.** `src/sortable/rect-index.ts` is byte-for-byte its pre-P-06 self — no hook, no `settle()`, no optional parameter — because `sortable/verified-refresh.ts` **wraps** the cache instead of reaching into it, mirroring the two state bits it needs from the calls it already intercepts (`invalidate`, `retire` and every `refresh` arrive through the wrapper, so the mirror cannot drift; `dirty` needs no variable, since `pending === 0` **is** clean). **`minimal (xy)` returns to 10 787 B, byte-identical to before P-06**, and the graph absence is asserted in `bench/size` as a peer of the unselected-axis claim. **Two corrections to the numbers this row quotes**, both from `p06-verified-refresh.md` §What D-101 and D-102 settled: **the `+135 B` this row states as the measured cost is a _budget overrun_, and the carried cost was 288 B** — larger than stated, so the decision is unaffected in the direction that matters, and the row is left as written with the correction beside it rather than restated, because the decision was taken on the number it names. The stale-build explanation first offered for the misreading was itself falsified by the review (P06-05) and is withdrawn: the size suite builds in `beforeAll`, the overruns reproduce exactly on a clean build of the folded state, and what happened is that a `over budget by N B` row was read as a delta; and the split is **not free on the `y()` side**, costing ~66 B more than the folded form for the wrapper and the module boundary, so the trade is 288 B off `xy()` for 66 B onto `y()` on top of the optimization's own ~300 B where it belongs. **The budgets are re-based only now**, on the condition being met, with the ~150 B headroom convention preserved and `xy()` and free drag unchanged. | **135 B is small and that is exactly why it needed deciding rather than absorbing.** Re-basing would spend headroom Phase 21 had just re-established, on the one thing the size instrument exists to catch — one axis feature's optimization linked into the other's bundle — and `plan.md` already records the rule this would invert: a budget may defer defence in depth, never a fix. **The split does not re-couple the shared cache**, which was the reason to hesitate: the fast path is `y()`-only under D-100 already, so the factoring follows a boundary the design drew rather than cutting a new one across it, and the per-refresh indirection it adds is one call per committed move against a bracket measured in milliseconds. **The implementer leaving the budgets red rather than re-basing them is what made this decidable**, and that instinct is ratified here: an absorbed number is a number nobody reads again. | the option, left open by P-06's implementation record, of accepting the shared-code cost with a budget re-base |
| D-103 | **Implemented 2026-08-21.** **P-06's fast path gains a second in-span witness at `hi − 1`, because the review showed P-06 does not create this failure mode — it amplifies one the contract already names.** When the row carrying an unexpected offset **is the in-span witness**, its `δ` is wrong by that offset and is applied to the whole span while every other witness honestly agrees: up to `hi − lo − 1` rows wrong for up to `k` moves, where D-100 accepted **one**. **The escape is a pure vertical translation and nothing else** — the anchor's `left`/`right` equality already catches horizontal movement and its `bottom − cached ≠ δ` test already catches a size change — and a running FLIP offset has exactly that shape. [03](03-feature-composition.md) §The bracket already states that _one element still carrying an offset is enough to corrupt the rebuild_, and `beforeMove`/`afterMove` are a **published** SPI, so a third-party displacement feature releasing less than the first-party one produces this in the rebuild window. **Under the general path it corrupts one row; under P-06 it corrupts the span** — so the guard restores **equivalence with the path P-06 replaces**, which is what P-06 claims to be, rather than buying a new safety property. **Cost: one read and one `live()` reading**, ~0.3% of a verified move that the record measures at ≈1.0 ms dominated by the post-write flush; skipped when `hi − lo === 1`, where both witnesses would be the same row. **D-100 case 3's risk statement is replaced**: no exposure at spans of 1 or 2; **exactly one strictly interior row** at spans ≥ 3, which equals the general path's exposure and is the acceptance criterion; span-wide only if both witnesses carry an identical pure vertical offset in the same frame, which is named and accepted. **The fixture obligation replaces D-100's single _a row transformed mid-drag_ with three**, the third asserted both with the equivalence instrument on (it must report a mismatch) and off (exactly one row may differ), so the residual radius is pinned rather than assumed. **Landed with the guard costing one read and the skip behaving as specified**: the M-4′ committed-move workload oscillates between adjacent slots, so `hi − lo === 1` and it takes the same four reads it did before — the measured before/after in `p06-verified-refresh.md` §What landed is unchanged and was re-run to confirm, so no performance claim moves. **Five fixtures, and the two that matter fail against the tree without the second witness**: a `translateY` on row `lo` and one on row `hi − 1` both refuse where they previously took the fast path. The interior case is asserted twice — with the instrument on it throws, with it off exactly one slot of the packed buffer differs from a fresh scan, read directly rather than inferred through `resolve`. **P06-02 landed with it**: `verify` threads `live()` between every candidate read and, on abort, stops, retires and reports nothing — asserted on read count, on the cache being left empty, on no throw, and against a control that still throws when nothing tore down. | **The trigger's likelihood was never the question, and treating it as one is how the wrong answer gets reached here.** P-06's whole claim is that it is a smaller rebuild in the same window — not a behaviourally different one — so a condition under which the fast path is **strictly worse than the general path** falsifies the claim regardless of how rare it is. Restoring parity costs one read on a path whose dominant term is a forced layout, and accepting a widened envelope that a reviewer had to demonstrate would mean re-accepting, on weaker terms, a risk that was signed off at one row. **What the review actually found is a gap in D-100's own reasoning rather than in the code**: case 3 was written as _one row_ because the analysis pictured the drifting row as passive, and never asked what happens when it is the row the measurement is taken from — the same class of error as a workload that entails its own result, which this phase has now caught three times. **P06-02 is not this decision's subject and is not architecture**: the equivalence scan must thread `live()` like `refresh` does, must stop and **report nothing** on abort — a partial scan legitimately differs and comparing it turns a correct teardown into a false mismatch — and _it is `DEV`-only_ is not a waiver, since the repository builds `__DEV__` as `true` and every in-repo fixture runs that path. | D-100 §The design's four-witness set and its case 3 risk statement; the single transformed-row fixture in D-100's slice |
| D-104 | **Implemented 2026-08-21.** **Measured and earned first** — record [`p02-shrink.md`](../measurements/p02-shrink.md). **P-02's shrink sub-candidate survives as one branch at a site that already exists**, and the stride sub-candidate is not designed, not reasoned about and not added to it. Design: [`p02-retention-shrink.md`](../p02-retention-shrink.md). **The lifecycle point is `refresh`, not `retire()`**: growth and shrink are the same decision about the same resource driven by the same number, the branch sits after the warm-path return and the I-36 entry barrier, and the scan below rewrites every slot — so nothing in the buffer is live at that instant. `retire()` would have to **predict** the next operation's need from the last one's; `refresh` holds the real `list.length` and so needs no state that can go stale. **A timer or idle hook is refused by construction**: releasing a correctly sized buffer on elapsed time makes a deterministic cache timing-dependent and needs a policy input the library has no basis to own. **Destruction needs nothing** — the closure drops and the buffer goes with it; M-2′'s figure is a **live** controller between drags. **The gate is `capacity > 4 × n`, and its soundness is a consequence rather than a tuning choice**: `capacityFor` makes `n ≤ capacity < 2n` for any fitted buffer, so `capacity > 2 × n` is already unreachable without a real collection shrink, and `4×` is one doubling looser as the cheapest available hysteresis. **A shrink allocates strictly less than it frees**, which is the property separating this from a memory-for-allocations trade. **Exact-fit shrinking is refused** — it would recover a fitted buffer's legitimate slack and pay with a reallocation on every single-item growth. **Churn has an exact definition and an exact falsifier**: a reallocation on a workload whose collection size never changes, asserted as **zero** over 1000 drags at a stable 100 000 items, and that arm is the one that can kill the candidate. `STRIDE` stays 6, `retire()` is unchanged, `xy()` and `verified-refresh.ts` are untouched. **The run answered both halves and they answered differently.** **Mechanically the derivation is exact**: at a stable 100 000 items over **1000 drags** the gate never fires — one allocation for the initial growth, `byteLength` byte-for-byte constant, **zero** reallocations — a real shrink produces **exactly one** further reallocation and never a second, a shrink frees 6 144 kB and allocates 6 kB, and `capacityFor(n) > 4 × n` is false for every `n` from 1 to 4096. **The arm that could have killed the candidate did not fire.** **The workload half is answered too, and the answer is yes.** A first pass declined it and was wrong: it bounded the drivable collection at ≈3 800 rows from M-4′'s **general** rebuild curve — measured _before_ P-06, on the path P-06 replaced — and skipped the interval from 2 000 items to 20 000, in which the **2 049** bucket boundary sits with a 192 KiB buffer. **Measured on the current tree that interval is comfortably drivable**: one committed move costs **5.70 ms bare and 5.74 ms animated at 2 100 rows** — about a third of a frame — and 6.21/8.35 ms at 3 000, averaged over 60 real frames so the `k = 8` full rebuilds are inside the figure. **And every firing from the 4096 bucket clears D-99's ~100 kB**: the gate needs `4096 > 4 × n`, so it fires only below 1 024 items and `capacityFor(1023)` is 1 024 — the smallest possible reclaim is **144 KiB**, while no firing from the 2048 bucket clears the threshold at any destination. (**Correction, P02-01**: the largest is 192 KiB less 48 B; the 186 KiB this row stated is the reclaim at the 65–128 destination the earning workload uses, not a range endpoint. The load-bearing half is the lower bound, which is what the D-99 argument rests on, and it is unchanged.) **The smallest earning workload**, driven end to end on one live controller through the public surface: a collection reaching **≥ 2 049 items**, a drag at that size, republication below **1 024** through the `items()` pull source and `controller.invalidate()`, and a **subsequent drag at the smaller size** — which is the only moment the policy can fire, since `refresh` runs only inside an operation and an idle shrink reads **zero** geometry. **Landed the same day, as the slice and nothing else** — `STRIDE` stays 6, `retire()` is unchanged, `xy()` and `verified-refresh.ts` are untouched, and no timer, idle hook or `retire()`-time prediction exists. **One divergence from the designed slice, and it is this decision's own sentence applied**: the design asks for _one `else if` beside the existing growth branch_ and what shipped is **one branch with two triggers and an inner settle guard**, because growth and shrink _are_ the same decision about the same resource — an `else if` writes them as two that happen to be adjacent. It also keeps `capacityFor` off the path when neither trigger fires, and it gives **the `n = 0` correction its one natural home**: for any `n ≥ 1` a firing gate implies `fitted < capacity` so the guard is free, and at `n = 0` the gate reads `capacity > 0`, which the one-slot buffer a previous empty refresh just produced also satisfies — without the guard an empty collection reallocates 48 B on every scan forever. The growth path is semantically identical, and a cache asked only for an empty collection now allocates **nothing at all**. **The falsifiers are re-pointed at the shipped cache and stay load-bearing**: mutations of the landed branch are each caught — gating at `2 ×` (1 test), at `8 ×` (1), removing the gate (2), dropping the settle guard (1), shrinking to an exact count rather than a power of two (6), and reverting to growth-only (6), over `tests/sortable` plus the P-02 file (492 tests). The equivalence check changed with them, from _instrument against cache_ to **a cache that shrank against a cache that never grew**. **The review then found the one mutation that failed nothing** (P02-03): a shrink written as `values = values.subarray(0, fitted)` reports the fitted `byteLength`, increments the allocation counter, keeps every invariant — and retains the whole high-water allocation, so the policy could have been reduced to a no-op with the suite green. The instrument was M-2′'s, which answers _how much does this cache hold_; what this decision is landed for is what a shrink **releases**, and every arm claiming a reclaim now reads `values.buffer.byteLength`, which takes that mutation from **0 failures to 6**. The 126-failure row this decision quoted as its strongest falsifier is withdrawn (P02-02): replacing the allocation _unconditionally_ clamps the buffer on **growth**, which D-104 did not change. The equivalence arms also compared `count` after `retire()` — `0 === 0`, with one contents obligation resolving to two empty slices — and are taken on a live cache now (P02-04). **Cost, corrected against the built artifacts** (P02-05): **+14 B on `minimal` and +14 B on `minimal (xy)`**, +14 to +21 B across the rest, brotli — **no row moves the +34 B this row first published**, and no budget moves. `minimal (y)` and `minimal (xy)` pay the **same** 14 B, which is this row's dimension-neutral claim measured rather than argued, and this is not a P-06 repeat: the shrink is a property of the cache both axes share. Real headroom on the `y()`-bearing rows is **114–139 B**, not the _~150 B_ convention these records quote. **P-02's stride half is untouched and not closed by this.** | **The headline number is not this candidate's payoff, and the decision is written to stop that misreading before it starts.** At a stable 100 000 items the gate does not fire and **6.29 MB is retained — correctly**, because the next drag scans 100 000 rows. This policy recovers **historical excess only**: capacity a collection used to need and no longer does. Of D-99's 6.29 MB, **5.12 MB is unread scalars and belongs to the stride sub-candidate**, which is not designed here; the two may not be added together or quoted for each other. **So the candidate's real payoff is unmeasured** — M-2′ measured a high water and never a collection that shrank, which is precisely the "deployment shape" gap D-99 recorded — and the falsifier workload is also the missing measurement. **That is why the second stop condition is the important one**: if supported deployments do not shrink their collections, the policy is correct and worthless, and it should be declined on D-99's own terms rather than landed because it is cheap. Landing a nearly-free change is not a reason; it is how a backlog grows entries nobody can later justify. | D-99's pairing of P-02's 6.29 MB figure with its shrink sub-candidate, as though shrinking recovered it |
| D-105 | **P-01 is declined, 2026-08-21; reviewed and the margin corrected 2026-08-22** — record [`p01-write-cost.md`](../measurements/p01-write-cost.md), review [`p01-review-claude.md`](../reviews/phase-22/p01-review-claude.md). **Measured before anything was designed, and no production gate exists**: the coalescing gate is a harness prototype in the measurement file, which is what D-99 required before a design could start. **The verdict follows D-99's own stop condition and the premise behind that condition does not survive it.** D-99 declined on the arithmetic that repeated same-frame assignments overwrite a property read once at paint, so the second through eighth write is _a CSSOM set and little else_ — exactly right for a write onto an **already-dirty** style tree, measured at **0.485–0.489 µs** across three runs, the most reproducible figure in the file and the one the deployment never pays. **What makes that premise inapplicable is a structural fact, not a price**: the deployed free-drag path issues **one visual write per input dispatch**, and the browser cleans the style tree to hit-test the next dispatch, so no two visual writes are ever adjacent on a dirty tree and every in-situ write is a clean-tree write. **That fact is now held by a permanent structural row** rather than by an opt-in report (P01-06) — a maximum of one write per dispatch across a real pointer flood, paired with an anti-vacuity count, and it discriminates: a second `visual.style.transform` assignment in `presentation.ts`'s `write()` fails exactly that row. So a future behavior that begins writing twice per sample trips a test instead of requiring someone to re-read the record. **The decline is argued from one reading and that reading reproduces**: at M-6's primary pace the typical frame's surplus is **17.9–25.3% of the 160 µs bar across thirteen runs** — the record's three, the review's seven and three more in remediation — roughly a fourfold margin. **The wider figures this row first carried were the range of three runs and are withdrawn as bounds** (P01-02): the end-to-end per-write reading spans 17.9–29.8 µs rather than _10–23 µs_, and the wave-4 tail spans 43.2–93.8% rather than _at worst 63%_. **Wave 4 is corroboration and not ground**: arm E's own null control — the identical alternation with both sides ungated, added in remediation (P01-01) — reads ≲1.4 µs at wave 2 against a 22–29 µs signal, but reaches **8.46 µs at wave 4 against 15 µs**, so wave-4 per-write figures sit barely above the instrument's noise. **One accounting assumption is now stated rather than buried** (P01-03): the surplus is `(n − 1) × perWrite`, which prices the write the gate _retains_ at the in-dispatch cost, where it would actually run in a rAF callback on a clean tree at 4.15–5.43 µs — so every surplus figure here is a **lower bound in the direction of its own conclusion**. **The gate's runtime cost is not what declines it** — 0.023–0.042 µs per request, 0.087–0.092 µs per frame, break-even at 1.2–1.3 writes — and those are over-estimates. **What declines it is the terminal-flush obligation against the size of the prize**, and the two halves of that claim have different evidence: the obligation itself is **proven** by three structural rows (a pending request stranded by an unflushed teardown, caught up by a flushing terminal path, and a second flush writing nothing, each broken and each failing), while the enumeration _release, cancel, destroy and the landing hand-off_ is **not** — it was asserted about code this run did not inspect, and the shipped lifecycle already writes at [`kernel.ts:1652`](../src/kernel/kernel.ts#L1652) on the non-null-target path, leaving the null-target jump cut (D-49) as the path where a flush is unambiguously owed. The count is a question for a design that does not exist and the decline does not rest on it. **Two claims are withdrawn outright**: _there is no cheap tail_ (P01-04) — the two paces disagree in direction and the wave-4 ordinal-0 bucket holds two to seven informative readings — and the reopening condition _a flush obligation that becomes free_ (P01-08), which measures nothing and lowers the cost side rather than moving the surplus. **The reopening conditions that remain are both measured quantities**: a behavior writing more than once per sample, and a device materially above M-6's ~129 /s primary pace. **The error class is P-02's, met again**: P-02 bounded a collection from a rebuild curve measured on the path its own optimization replaced; P-01 priced a write from a regime its own deployment never enters. **Nothing else moves**: `moveTo()` traffic stays classified rather than gated (M-6), P-02's stride sub-candidate stays undesigned, and the committed-move forced flush stays where P-04 and P-05 are. | **A measurement that changes nothing has to say which of _already optimal_ or _not worth taking_ it found, and this is the second** — but it is only honestly the second once the instrument stops agreeing with the prediction for the wrong reason. The dirty-tree figure would have confirmed D-99 to three significant figures and been quoted forever as _one write is half a microsecond_; it is a true measurement of a regime a free drag cannot reach. **The review's own lesson is narrower and sharper**: the decline was right, and every figure quoted around it was quoted to a precision the instrument did not have — a three-run range printed as a bound, twice, in a record whose stated subject is arithmetic that reads as fact. **The cross-check that makes arm E trustworthy is not internal**: its coalescing ratios reproduce M-6's census means of 2.25–2.27 and 5.25–5.38 across three independent harness runs, which neither file could establish alone. | D-99's expectation that P-01 would decline because one visual write is a CSSOM set and little else — the verdict stands, the ground under it does not |
| D-106 | **The bundle-structure entry closes as justified, 2026-08-22 — no size candidate is earned** — record [`bundle-structure.md`](../bundle-structure.md). **Re-measured from a fresh build rather than reconstructed from published deltas**, because P-06 and P-02's shrink each recorded a per-row delta and a delta recorded at landing is a claim about the tree at landing. **All twelve declared rows are under budget, the union identity holds at 47 = 47, nothing is emitted into two chunks, and every absence assertion passes** — including the `xy()`/P-06 exclusivity claim that actually failed once and was fixed by D-102, which is the only evidence this instrument would catch the next one. **The subpath set is affirmed unchanged at two behaviors and two axes**: ten runtime entries each carrying machinery a composition either imports or does not, and two type-only entries that measure nothing and say so, which is the exception D-56's rule admits by name. **The three-root topology's own standing doubt closes on a measurement that does not depend on believing the table** (03 §The export topology: _the topology has never been checked against a consumer who did not already believe it_). `drag.js` bundles to **121 B and one module** — a consumer who wants `err instanceof DraggableError` pays that, not the 6.51 kB kernel — so D-48's shared-vocabulary root is now a measured property rather than a derivation. **Where the fixed cost lives is the substantive finding, and it is not a defect**: the cross-behavior shared set is **16 modules**, with ~7.6 kB counted twice across the two complete compositions — ~57% of a two-behavior page — and a free-drag-only bundle is **bounded rather than split**: it strictly contains the twelve-module 6,514 B `draggable` floor, so **~75% is kernel** and its eleven-module free-drag layer is **≤ 2,203 B**. **Corrected 2026-08-22 on challenge**: the first version divided a brotli bundle that does not divide, reporting ~87% and _~1.1 kB of free drag_ by subtracting a sixteen-module estimate from a fifteen-module composition; the honest figures are marginals against a stated base — **1,546 B** for the complete thirteen-module layer over a sortable page, **1,396 B** for the minimal eleven-module one, and 445 B versus **150 B** for the same two optional modules depending on whether `shared/landing-runner.js` is already paid for. The layer is 30,778 B rendered across 13 modules rather than thirteen nearly-empty ones. That corroborates M-2′'s ~80%-common _heap_ from an independent direction — different quantities, same structure — and it stays what D-99 called it, prioritisation context rather than a candidate. **Every candidate considered is declined, and the reason is uniform**: what is material is not locally removable, and what is locally removable is immaterial or refused by a standing decision. The kernel floor is the library and `draggable`-alone is not a deployment; keyboard ingress and `input-policy` are accessibility and safety positions a bundle pass may not reopen on byte grounds; `box-quad` is not ours and its placement is already asserted; `sortable/spec.js` splits into a ~60 B module boundary that buys nothing tree-shakeable. **No production code changed and none should.** **The budgets do not re-base.** Slack is 114–154 B against the ~150 B convention and the drift is one landed change — P-02's +34 B, which added no module and was therefore absorbed under M-3′'s own rule. The instrument's sensitivity where it matters is unchanged: the two modules that have ever appeared cost ~361–388 B and ~60 B, and at both 150 B and 117 B of slack the first trips and the second does not. Re-basing on a schedule rather than an event would invert D-102's ratified instinct that an absorbed number is a number nobody reads again. **Re-base conditions**: a row goes negative, erosion stops being attributable to a named landed change, or L-11 lands — 05 §Measurements owed makes the frozen export map part of M-3, so L-11 re-measures whether or not it moves a byte. **Carried live as [SC-1](../obligations.md) from 2026-08-22 (D-116)**, where the condition is stated in the present tense and its citations are re-derived against today's tree; the wording here is the wording as decided, and the register is where a later pass reads it. ~~**One obligation is left open and is booked as a finding rather than carried here**: F-77, the two published runtime roots with no standing bundled-isolation assertion~~ **F-77 closed 2026-08-22** — both roots are declared compositions with graph and budget assertions, each of the two predicted regressions was injected and each is caught by exactly one half, and no production code changed. The bundle-structure entry now carries no open obligation | closes 03 §What isolation cannot shake (composition weighed at **283 B, 2.4%**, stable across 266/289/283 B and three feature additions) and the export-topology doubt at 03 §The export topology; corrects M-3′'s shared-set breakdown; leaves D-99's stride, forced-flush, P-04 and P-05 untouched |
| D-107 | **The bundle optimization question closes with no candidate, 2026-08-22** — record [`bundle-structure.md`](../bundle-structure.md) §The optimization question. D-106 closed the entry on accounting and structural verification, which is not a search for something to remove; **this is that search, run separately and by ablation** — the candidate is neutralised in the emitted code and the bundle re-compressed, because counting source characters prices all of this several times too high. **The one material non-structural cost is error message text: 741 B on `complete`, 6.3%** — larger than any optional feature this package ships and larger than P-06's whole module. **It splits, and both halves are declined.** **The author-facing half splits in two, ~446–471 B on every composition including the bare `kernel.js` root, and both are declined.** **A1** — `validateFramePart`, ~276–300 B — has explicit cover: contract 04 §Dev-only invariants names it a **production** check because _its failure mode is silent state corruption_, review 4 §28 recorded the trade, and `kernel/frames.ts` already imports `DEV` and keeps a dev-gated family beside it, so the gate was declined in writing rather than forgotten. **A2** — `arm()`'s `config.actionTags` and `command.types` checks, ~152–161 B — has **none on record**, sits four lines above a `DEV`-guarded assert, and was the only thing in the sweep that looked like a live candidate. **Both are declined on one argument, which came out of trying to remove them and is stronger than A1's own record.** `kernel/dev.ts` justifies stripping author-facing assertions on the premise that _behavior authoring is not on the public surface_, and states its own revisit condition — _the day it is, this needs revisiting_. **That day passed at Revision 2.1**: D-61 published `sortable/feature.js`, D-70 `free-drag/feature.js`, and D-68 rebuilt `kernel.js` to publish `BehaviorFactory`'s whole closure so a third party can author a behavior. Gating these blocks would strip validation from a **published** authoring API in the exact build a third-party author ships. **The finding is F-78, and it points away from a size candidate**: the remedies `dev.ts` names — a dual build under an `exports` condition, or a per-site `process.env.NODE_ENV` comparison — give an author their assertions back and cost the production bundle nothing. That is an `exports`-map question and belongs to the API deliverable. **Class B, 424 B of runtime and consumer-callback diagnostics, is declined on judgment**: D-64 deliberately narrowed the consumer to a coarse `code` with detail on the `cause`, so stripping the cause's message leaves `presentation` and nothing else in the one situation the design exists for; 04's gating rule covers assertions the type system cannot prove, not the diagnostic surface of errors that fire. **Overturned by** evidence of a bundle-constrained supported deployment, or by a machine-readable reason replacing the prose — which is an API change and belongs to the API deliverable. **Carried live as [SC-2](../obligations.md) from 2026-08-22 (D-116)**; the register also records that the second half is not free-standing, being owed as O-1. **Four sweeps found nothing and are recorded so they are not re-run**: `__DEV__` folds completely; `box-quad` shakes correctly, with `cache`/`projection`/`quad` verifiably absent and their 259 B not paid; the largest single literal, `LIFTED_PROPS`, is 563 B raw but **142–152 B compressed**, is a documented correctness requirement, and **is already collapsed into one delimited string by the minifier**; and cross-behavior duplication is already collected by the compressor — free drag ships alone at 2.91:1 and adds to a sortable page at **6.33:1**, so source de-duplication would compete with a compressor that has already found most of it. **The large costs are intrinsic**: three modules are 47% of own code — `kernel/kernel.js`, `sortable/spec.js`, `free-drag/spec.js` — the state machine and the two behavior implementations, none separable without removing a capability | closes the optimization half of the bundle deliverable that D-106 left implicit; **corrects D-106's `renderedLength` ranking**, which counted JSDoc and overstated every decline 3–8× and misordered the floor; no production change |
| D-108 | **Implemented 2026-08-22.** **F-78's remedy, and it is neither of the two `kernel/dev.ts` names: un-gate the four kernel author-facing checks, retire `kernel/dev.ts`, and leave `sortable/verified-refresh.ts`'s own binding alone.** Record [`api-surface.md`](../api-surface.md) §F-78. **The whole set is four sites and every one is cold** — `assertFrameShapesMatch` once per `arm()`, `assertFrameScrubbed` twice per drag at operation retirement, the unconsumed-staged report once per transaction open, and the unclassified-`host.fail()` report only on an already-failing path. The per-frame dev work in this package is `sortable/verified-refresh.ts:585`, which is a **different flag in a different tier** under D-101 and stays gated. **Contract 04's own discriminator already classifies these as production checks**: `frames.ts` says the kernel-key collision check is production _because its failure mode is silent state corruption rather than a stale reference_, and a non-deterministic part factory and a reset that leaves a live reference are both silent state corruption — a retained element leaks a DOM node across every later operation. `validateFramePart` is the precedent, unconditional at the **same site**, ratified twice and re-declined by D-107 four days ago. **The consumer cost is near zero**: `report()` is the platform channel — `reportError`/`console.error` — while `host.fail()` is what reaches `onError`, and for a consumer composing only first-party behaviors none of the four can fire. **Nothing new is exposed either**, since `sourcemap: true` already ships every assertion's source in 50 `.js.map` `sourcesContent` fields. **D-101 survives and is vindicated**: the kernel's binding disappears because the kernel had no hot dev work and the sortable's stays because it does, which is the asymmetry that rule exists to make expressible. **Cost: this adds bytes and no number is guessed here** — slack is 114–154 B and this may exceed it, and **M-3′ licenses that re-base by name** (_a budget re-bases rather than a correctness fix shrinking_). ~~Measurement owed at landing.~~ **Measured at landing: 282–305 B, roughly twice the slack, and no module appeared** (corrected 2026-08-22 by API-01 from a first-published _283–340 B_, which subtracted the previous re-base's _Landed figures_ list rather than the pre-slice tree and so charged D-108 with 14–46 B of D-103 and D-104 growth on seven sortable rows; the budgets are unaffected) — the whole cost is two assert messages, two report messages, `sameKeys`, `validateFrameDescriptors` and two loops. Every affected budget re-based visibly in `bench/size/measure.ts`; `baseline B` did not move because it is the shipped predecessor, and the `drag.js` vocabulary root is byte-identical at **121 B**, which is F-77's assertion confirming the error vocabulary still does not pull the kernel. **The four checks are now read out of the packed tarball by `consumer.node.test.ts`**, which is the artifact a third-party author installs and the one place the gate was invisible before: this repository builds `__DEV__` as `true`, so every in-repo fixture ran the checks the published build had folded away. | **Both remedies `dev.ts` names fail on evidence.** A `development` **`exports` condition is invisible to this package's own graph**: every internal edge is a relative specifier — zero occurrences of `@ydinjs/drag2` in any emitted file — and `sortable.js` reaches the kernel as `import { draggable } from './kernel.js'`, which never consults the map. A kernel-tier author composing `sortable()` would load **two kernel graphs**; not a state hazard, since all module-scope kernel state is immutable, but **two `DraggableError` class identities**, and `drag.ts` publishes `err instanceof DraggableError` as _the_ cross-tier check and exists for that reason. Making it safe needs a complete parallel tree on all ten runtime subpaths — roughly double the published JavaScript. And a per-site `process.env.NODE_ENV` comparison is a **`ReferenceError` on the production path** for a `platform: 'neutral'` browser consumer with no `process`, while `dev.ts` has already ruled out the `typeof` guard that would prevent it. **The window is the other half of the argument**: un-gating makes a non-deterministic part factory throw where it is silent today, which is breaking after publication and free before it — D-75's reasoning about `DragErrorContext`, applied to a second case. **Overturned by** a call graph that makes one of the four hot — a `scrub()` or a frame-shape assertion reachable per frame rather than once per operation, most plausibly `assertFrameScrubbed`; the decision is per-site and does not require all four to be alike. **Carried live as [SC-3](../obligations.md) from 2026-08-22 (D-116)**, which is where a later pass reads it; this row keeps the wording it was decided in. **That is a standing condition and not owed work** (D-114 (a), closing API-03): coldness here is settled **structurally** — `arm()` runs once per controller and all three `scrub()` sites are terminal — so an observer recognises the overturning condition without anyone running anything. ~~a measurement showing one of the four is not cold~~ was the original wording, and it read as an obligation nobody had booked while the argument that discharges it was already in this row | closes F-78; the other half of D-107's A1/A2 decline |
| D-109 | **Implemented 2026-08-22.** **`OnStart`, `OnEnd` and `OnDragError` are qualified per behavior, before publication.** All three are exported unqualified from **both** ordinary roots with **different structures** — `OnStart` takes `HTMLElement` against `DragGeometry`, `OnEnd` `ReorderTransactionResult` against `FreeDragTransactionResult`, `OnDragError` `SortableErrorContext` against `FreeDragErrorContext`. The spelling is the implementer's, matching the existing `SortableErrorContext`/`FreeDragErrorContext` precedent; **the decision is that they are qualified, not what they are called**. **Landed as the behavior prefix**, applied mechanically so the spelling is derivable rather than chosen per name: `SortableOnStart`/`FreeDragOnStart`, `SortableOnEnd`/`FreeDragOnEnd`, `SortableOnDragError`/`FreeDragOnDragError`. `FreeDragOnDragError` stutters and is kept anyway — a rule whose output has to be argued case by case is not one, and the alternative spellings all renamed the base as well as qualifying it. **Three classes of name are deliberately left alone**: `OnMove`, `OnDrop` and `OnReorder` exist on one root each and so meet no condition; `ResolveHandle` and `ResolveElement` collide _and_ match structurally, which is the counter-case; and `AxisInstaller`/`FreeDragInstaller` were already qualified. **The instrument is `consumer.node.test.ts`**, whose fixture imports both ordinary roots into one file and compiles against the _packed_ declarations — so an unqualified pair would collide there before it could reach a consumer | **This is D-75's own rule, unapplied to the next three cases that met it.** D-75 states the test as _different structures under one name — which is the **only** condition that qualifies a name_, and states the window as _the package has no released consumer, so symmetry costs one type name now and **cannot be had later**_. The condition is met exactly three times and was applied zero of them, in the same file where D-75 renamed `DragErrorContext` — **the alias naming the renamed type was itself left unqualified**. **Not a blanket rule, and the counter-case proves it discriminates**: `ResolveHandle` and `ResolveElement` also collide across the two roots and are structurally identical, so leaving them unqualified is correct. **Cost:** three type names, zero runtime, and nothing after publication | applies D-75 to the cases it did not reach |
| D-110 | **Implemented 2026-08-22.** **`SortableInstaller` is published from `sortable.js`.** A re-export of an existing declaration; zero runtime cost, and its middle-tier publication at `sortable/feature.js` is unaffected | **D-78's own rule, stated on the sibling type in the same tier**: _`SortableConfig` names this slot, so an ordinary consumer must be able to hoist an installer into a typed `const`_ — the argument that published `AxisInstaller`, holding verbatim for a type `SortableConfig` names at **two** slots (`landing?`, `plugins?`) and `sortable.d.ts` exports **zero** times. The mirror already does it: `free-drag.d.ts` publishes `FreeDragInstaller` for three slots. **The consequence is must-name-and-cannot**: a sortable consumer typing an ordinary-tier config slot must import the **types-only middle-tier entry** to do it, while a free-drag consumer never does — the tier inversion D-48 and D-64 each exist to prevent, reached a third time by a third route | applies D-78 to the case it did not reach |
| D-111 | **Implemented 2026-08-22.** **The published artifact must contain no reference to a file it does not ship, and `clean-build` must cover every directory `files` ships.** Four required properties, remedies left open: the **31 dangling `//# sourceMappingURL=…d.ts.map` references** stop being published — either the comment goes or the maps are emitted and shipped, and that choice is the monorepo's rather than drag2's; **`drag.js.map` leaves `files`**; **`clean` covers `kernel`, `shared` and the root `landing-runner.js` orphan**; **`type-fest` moves to `devDependencies`** | **The generated half of the package contract is sound and the hand-maintained half has drifted.** `exports` is regenerated from `files.json` byte-for-byte, so subpath style, condition order and the two lists' set-equality cannot drift; all 24 targets resolve. `package.json` `files` is the one hand-maintained list and is where every defect landed. **Zero `.d.ts.map` exist in the monorepo and `npm pack` ships zero**, so all 31 references dangle in the tarball; the cause is upstream, tsdown reusing one `sourcemap` option across the JS and dts passes. **`drag.js.map` is a stale line rather than a build defect** — a pure re-export chunk has no mappings, so rolldown emits neither map nor comment, reproduced in isolation. **F-79 is the serious one and it has already fired**: `kernel/` and `shared/` are shipped and never cleaned, and the root `landing-runner.js` orphan is the same failure that reached the tarball only because it fell outside the allowlist. **`type-fest` never crosses the declaration boundary** — two `import type` uses inside function bodies, zero occurrences in any emitted file. **Landed 2026-08-22, remedies chosen:** the comment goes rather than the maps arriving — `dts.sourcemap` is stated `false` and a `build:done` pass strips the reference the shared `sourcemap` option makes the dts plugin write anyway, so the monorepo-wide alternative stays open without this package publishing a dangling reference in the meantime; `drag.js.map` leaves `files`; `files.json`'s `clean.extras` gains `kernel`, `shared` and the two `landing-runner` orphans; `type-fest` moves to `devDependencies`. **Three permanent instruments land with it**, because every one of these four was invisible to a green suite: the packed-tarball sourcemap row now scans `.d.ts` as well as `.js` — an instrument checking one half of what its name claims is how 31 references shipped — a new row asserts every `files` entry exists in the tarball, and `packaging.node.test.ts` derives the clean pathspecs and asserts they cover every shipped path, which holds F-79 without running `git clean` against a working tree | closes F-79; the `drag.js.map` half is the recorded item that led to it |
| D-112 | **Implemented 2026-08-22.** **The instrument is `tests/references.node.test.ts`**, and the form converged exactly where clause 4 said it would: **a citation is checkable as far as it is delimited**, so a one-word heading with prose running on fails and the same citation punctuated does not. Sixty-seven references were re-pointed or delimited across `src/`, `tests/`, `bench/`, `.scripts/` and all eight contract documents, plus the two dangling `src/` paths and a stale `kernel/dev.ts` reference in the monorepo's own `vite-config.ts` that nobody had found. **Four kinds are classified and none is skipped**: a contract citation; a citation that names its target document by markdown link — so `bundle-structure.md` §Headroom and `q7.md` §Answer 1 resolve rather than being demoted for living outside `contract/`; a citation into dated history; and a **specimen**, a citation quoted inside a code span, which is what lets this row and the instrument's own prose name a dangling reference without making one. **Every cross-reference in the normative tree resolves, and the check is executable.** Record [`maintainability.md`](../maintainability.md) §1. Four required properties, the mechanism left to the implementer: **(1) scope is `src/`, `tests/`, `bench/`, `.scripts/` and `.plan/contract/`** — dated history (`.plan/reviews/**`, `.plan/measurements/**`, and the decision rows of this table) is out under the **tense rule**, which is the other half of this decision: a ledger row is a dated act and is allowed to describe the tree as it stood, while a source comment describes _this_ code and is not. **(2) Three reference kinds resolve**: a contract citation `NN §…` to a heading _or a declared row id_ in `contract/NN-*.md`; a backticked repository path on disk; a cited test or source file exists. **(3) A deliberately retired reference is marked**, and strike-through is the marker — the convention already used for retired symbols, extended to paths and sections; `tests/kernel/vocabulary.node.test.ts:391` correctly records that `src/kernel/dev.ts` _is retired_ and must not fail. **(4) Unparseable is a failure, not a skip** — every `§` occurrence in scope is classified resolved or failed, which forces the citation form to converge without legislating a syntax, and is D-115's rule applied to this instrument itself | **Twelve confirmed dangling references survived twenty-two phases, six checkpoint reviews and three Phase 22 entries, and nine of them are in shipped source.** `src/sortable/feature.ts:3` cites `03 §A feature is a function factory` — the heading **D-45 itself renamed**, at the top of the file that defines the middle tier. Three modules cite `03 §Validation`, which is a heading in **07**; `src/sortable/spec.ts:1509` cites `02 §The join`, which is in **06**; `:899` cites `02 §F-40`, which is in **05**. `src/kernel/presentation.ts:540`'s `01 §Partial activation` occurs nowhere in `.plan/`. Two sibling modules disagree about where their shared dependencies live — `src/sortable/landing.ts:4` says `src/landing-runner.ts` and `src/sortable/feature.ts:57` says `src/composition.ts`, both under `src/shared/`, while the free-drag mirror states both correctly. **The largest instance is unrecorded**: `05 §Measurements owed` is not a heading in 05, and it is cited by contracts 00 and 07, `plan.md`, `bundle-structure.md`, four measurement records, `bench/size/measure.ts:5` and `tests/perf/m1.browser.test.ts:12` — including inside **D-106's live re-base condition**, so the authority for a condition that has not yet fired points at a heading that does not exist. **This class gets the pass's one instrument because it is the only one that is a pure string-to-existence check with no semantic model, and because human judgment has already declined it twice**: `05 §What would reopen this` was found and written down in `bundle-structure.md` and again in `plan.md`, and left both times on the ground that _re-pointing a citation is not this document's to land_. That is a defect with no owner, which is what an instrument is for. **The negative half of the census is why the scope is this narrow**: every mechanically-checkable count in `src/` is correct, every deleted symbol is correctly struck with its decision id, every export claim matches the emitted declarations, and every D-109 rename is clean — the prose hygiene is high and the defects concentrate in references alone; closes `bundle-structure.md` §Corrections to the record, item 3 and the `05 §What would reopen this` half of F-82; takes the tractable mass of F-81 |
| D-113 | **Implemented 2026-08-22.** **The guard is in `tests/packaging.node.test.ts`** and reads the emitted tree. **The two legitimate orphans are recognized rather than listed**: a **module header** is the first block of its `//#region`, and a **marked** block carries strike-through — D-112's retirement marker applied to a declaration instead of a reference. The false block is deleted; `kernel/spec.d.ts`'s orphan was a **section header** and is demoted to `//` comments in source, because it documents a group and not a declaration and had no business being JSDoc. **A published declaration carries no doc block that documents nothing.** Record [`maintainability.md`](../maintainability.md) §2. The detector is five lines against the emitted tree — a `*/` followed by a `/**` with no declaration between — and it belongs beside the existing packaging assertions, which already read the built artifact and the packed tarball | **`sortable/feature.d.ts` ships a sentence denying D-61, D-78 and D-110, directly above the sentence stating them.** `src/sortable/feature.ts:185` carries two consecutive doc blocks on `SortableInstaller`; only the second is the effective JSDoc, and the first reads _The authoring shape — internal and unstable, and unexported from the package for that reason_ while the second reads _**D-45 withdraws the brand and D-61 publishes the type**_. **Both are copied into the declaration**, at lines 119–125 and 126–150, so the third-party installer author D-61 created and D-78 published for opens the type they are told to implement and reads that it is unexported. **This is `kernel/dev.ts`'s expired premise, in the same tier, surviving the same three decisions** — and it is F-81's mechanism in its purest form: an orphaned block is prose with no subject, so no compiler, no TypeDoc run and no reviewer diffing declarations can ever tell it that it is wrong. It is invisible by construction and it ships. **Six orphaned blocks ship across 32 emitted declarations, and only one is false**: two are deletion notes correctly struck (`~~SortableCallbacks~~`, `~~SortableFinishResult~~`) and three are module headers that lost their anchor when the bundle was flattened. **This brushes the API entry deliberately and reopens nothing**: that entry read the export lists, not the doc blocks attached to them, and this says only that the artifact must not carry a sentence denying a decision it already made |
| D-114 | **Implemented 2026-08-22.** **The register is [`obligations.md`](../obligations.md)** — seven live, one withdrawn, three discharged. **Check D-56 is withdrawn unsatisfied**, by the owner: its before/after measurement belonged to the deletion's landing window, that window was missed, and **no later measurement substitutes** — a figure against any subsequent tree answers a different question, which is what the check's own wording forbids. D-56 itself remains accepted; what is withdrawn is the lost falsifier. **Q-4's narrow half closed as bookkeeping**, its answering event having occurred — free drag landed at Phase 19 with two action tags against the sortable's three, so the fourth never arrived. **Q-6 is recorded and not closed**: the missing matrix row is bookkeeping, but which recovery a reorder rejected against a _moved_ home should take is a semantic question, and a re-read is not a licence to decide one locally. **An obligation names a destination that can receive it.** Record [`maintainability.md`](../maintainability.md) §3. Three parts, no new machinery. **(a) An _Overturned by_ clause states a standing condition, never owed work** — a standing condition is one an observer recognises without anyone doing anything; owed work is an obligation and goes where obligations go. **Applied to D-108**: its clause is standing, and its own structural argument — `arm()` runs once per controller, all three `scrub()` sites are terminal — already discharges what the clause appears to owe, and should say so. **(b) A destination must be a thing that can close, and closing it discharges or re-books every obligation booked to it.** **(c) The live set is carried in one register** rather than distributed across nine documents | **API-03 is one instance of five, and the shape is not _somebody forgot_: every one was booked to a named destination, and the destination then closed without taking it.** Phase R closed over **Check D-56**, whose window is now permanently gone — the check reads _run `bench/size` immediately before and after the deletions land_ and _a later phase may not record it as satisfied without the two numbers_, and four records each hand it back rather than take it. The API deliverable closed over **D-107's Class B** clause, which had routed itself there in as many words, by answering _anything in the bundle entry stays closed at `d949cfeb`_ — both passes correct, the obligation lost between them. Checkpoint E closed over **D-81's** general question, which occurs once in the whole record, in D-81's own row. **D-46/D-54's touch measurement** has no destination at all, because the deferred vocabulary has no form for a measurement. And _the next pass over 05_ was never a destination. **(b) is a rule rather than a test because no instrument can observe a destination closing** — `tests/decisions.node.test.ts` cannot spell `Phase R`, `Checkpoint E`, _a measurement_ or _the owner_, and its witnesses are source-level facts. What can observe a close is the pass that performs it, and each of these three closed correctly on its own terms because nothing asked it at that moment; records F-82; makes API-03's remedy general |
| D-115 | **Implemented 2026-08-22.** **All four `docs.node.test.ts` runs now assert their own premise** — `--treatWarningsAsErrors`, so the property under test is an exit status, plus a read-back of the emitted JSON naming the modules each run must have converted, so a run that converted nothing cannot pass quietly. The packaging parser's premise is asserted directly: every relative specifier a general parse finds must be one the narrow regex found, and a dynamic `import()` anywhere in `src/` fails. **Both were falsified before landing**, and so was D-112's resolver, in both of its halves. **An instrument asserts its own premise, or states why the premise cannot break.** Record [`maintainability.md`](../maintainability.md) §4. The applications are already invented in this repository: `--treatWarningsAsErrors` plus an artifact read-back for the three un-hardened docs runs; one assertion for the packaging parser — every `import`/`export` statement in `src/` matches the form it understands, so it fails loudly the first time one does not; and D-112 clause 4 for the citation form | **Three instruments can fail _open_ — pass while the property they assert is false — which is strictly worse than stale prose, because it converts to a green suite.** **(a)** `tests/packaging.node.test.ts`'s `relativeSpecifiers` is one regex, `/from\s*'(\.[^']*)'/gu`, blind to a side-effect import, a double-quoted specifier and a dynamic `import()`. **It is correct today and correct only by coincidence** — verified: zero of all three forms across 210 import statements — and **three assertions depend on it**: ship-list coverage, optional-module isolation, and since D-111 clean-build pathspec coverage. `import './register.ts';` silently drops a subtree from `reachableFrom()` and all three go green. Recorded as **M-05 at Checkpoint B**, owned by _Phase 11 M-3_, live ever since. **(b)** Three of the four runs in `tests/docs.node.test.ts` grep a captured stream for the word _warning_, so a reworded summary or a stream TypeDoc declines to write makes the assertion **weaker without failing**. The file names this — _the worst way for a gate to break_ — invents the fix for the fourth run, and declines to apply it to the other three because _rewriting them is a change to instruments that are currently green and were not what Checkpoint E asked about_: correct scoping then, nothing scheduled since. **(c)** The citation convention itself, which is why D-112's census is indicative rather than exact. **This pass made the same error twice and caught both**: a grep of the built tree appeared to contradict D-107's _`__DEV__` folds completely_ — the hits were `.js.map` `sourcesContent` and preserved JSDoc — and two automated citation censuses returned different totals, both wrong in both directions, with only a hand audit settling which instances are real. That is the phase's fifth and sixth instance of **a proxy read where the quantity was meant**, after P-02's superseded curve, P-01's unreachable regime, the free-drag division and D-106's `renderedLength` ranking; the first four reached the record and these did not; closes the M-05 half of F-82 and Checkpoint E's E-08 residue |
| D-116 | **Implemented 2026-08-22.** **The three live clauses are in [`obligations.md`](../obligations.md) §Standing conditions as SC-1, SC-2 and SC-3**, each stated in the present tense with its citations re-derived, and D-106, D-107 and D-108 each cite their id from the wording they were decided in. **The register is the sixth scope root of `tests/references.node.test.ts`**, so everything the tense rule calls live is inside the resolver and the row-shape skip is sound rather than convenient. **The backstop is in `tests/decisions.node.test.ts`** — a bold condition lead-in in a decision row must name an `SC-n`, every cited id exists in the register and every registered id is cited from the ledger, with the open premise stated in that file rather than implied by a green row. **A ledger row states a condition as it was decided; the register carries it live.** Record [`maintainability.md`](../maintainability.md) §6. MNT-03's diagnosis is accepted in full — D-112's tense rule classifies by **register** and its resolver skips by **container**, so a live clause inside a dated row is unreachable — and the resolution is to **change the specification rather than the resolver**: the row-shape skip is the right mechanism, and what makes it _true_ is that nothing live sits inside a row. **The class has already cost this package a decision, one tier down**: F-78 is _`kernel/dev.ts` … its own stated revisit condition has fired unnoticed_ — a live condition embedded in prose whose triggering event, Revision 2.1 publishing three authoring entrypoints, passed unobserved until a bundle sweep tripped over it, and D-108 exists to clean up after it. MNT-03 is that shape one tier up, in the file where the record keeps its conditions. Four parts. **(a) A standing condition leaves the ledger.** Any clause a later pass must act on — an _Overturned by_ condition, a _Re-base condition_, a _Revisit condition_ — is carried in the register under its own `SC-n` id, in the present tense, and the row cites the id instead of stating the condition twice. **It is not an obligation and does not join the _Live_ table**: D-114 (a)'s distinction is that a standing condition is _recognised_ rather than _owed_, and merging the two would undo the decision that built the register. **(b) The register is a scope root of `tests/references.node.test.ts`** — the one `.plan/` file outside `contract/` that is, because it is the one that is present-tense by construction. Everything the tense rule marks live is then inside the resolver and everything outside it is dated: MNT-03's gap closes on **one scope root and no new mechanism**. **(c) A lifted clause is re-derived, not moved.** Its citations resolve against today's tree at the moment of lifting — which is what repairs `05 §Measurements owed` without reopening D-106 — and each site that stated it gains a pointer to the `SC-n` under a dated note; for the witness that is D-106's row and [`bundle-structure.md`](../bundle-structure.md) §Headroom, which states the same clause with the same dangling citation. **The tense rule protects a reference that _was_ true, not one that was never true.** D-101's ~~`src/kernel/dev.ts`~~ is history and stands as written; `05 §Measurements owed` was never a heading in 05, so it is not history but a defect that happens to be old. That distinction is the whole of what the ledger owes here, and it is why **no sweep of the ledger follows from this decision**. **(d) The residue gets a backstop, not a proof.** Whether a _new_ decision embeds a condition rather than registering it cannot be observed by an instrument — D-114 (b)'s argument, not re-derived. What is checkable is the vocabulary the record actually uses: **a bold condition lead-in in a ledger row must name an `SC-n`, and every `SC-n` must exist in the register and be cited from the ledger.** Bold is the discriminator because it is already this record's typography for a live clause, while an italic or backticked mention is already how it quotes the term — D-114's own row says _Overturned by_ _about_ the rule and must not be flagged, which is D-112's **specimen** applied to a lead-in. Closed set, string-to-existence, no semantic model: D-112's own admission criterion, and it belongs in `tests/decisions.node.test.ts`, whose subject is already this one file. **Its premise is open and is stated rather than hidden** (D-115): a fourth lead-in escapes it, so the register entry is the load-bearing artifact and the check is a backstop over three known forms. Non-vacuity floor: the standing set is non-empty and every entry is cited. **The live set is three, not twenty-six.** Removing the skip surfaces 26 unresolved citations inside D-rows, and MNT-03 is explicit that most are correct past tense — D-45's row naming the heading D-45 itself renamed is precisely what the tense rule exists to protect. The bold lead-ins census to three live clauses: **D-106's re-base conditions**, **D-107's Class B overturn**, **D-108's overturn**. A rule whose entire live population is three does not need a mechanism; it needs a home, and D-114 already built one. **The record already applies this rule with an exception, which is the tell**: MNT-03 observes that rows _are_ edited when a clause goes live — this branch rewrote D-108's overturn clause and struck the original wording, and D-106 carries a struck F-77 sentence with a dated replacement. So _a row is a dated act_ was never the rule the record followed; the rule it followed was _a row is a dated act **plus** whatever live text nobody had anywhere else to put_, and this removes the second half | closes MNT-03 and makes D-112's row-shape skip sound rather than merely convenient; corrects `maintainability.md` §The tense rule and its _No decision-ledger row is edited_ note; records F-83; **leaves D-106, D-107 and D-108 themselves untouched** — only the location and the citations of their live clauses move |

### Decisions not yet implemented

**The artifact F-63 asked for** (K-5). D-34 and D-35 were stated in the present tense by three contract documents and were not in the code, across a checkpoint and a whole revision, with every suite green — because a green suite is evidence about the _implemented_ contract only, and nothing in the repository compared a decision booked to a later phase against the code that had not implemented it. Phase 18 found them by reading, which is the discovery this table exists to make unnecessary.

**The list is the artifact; the check is that it is complete.** `tests/decisions.node.test.ts` asserts both directions: every decision row above carrying the marker **Unimplemented (_destination_)** has a row here whose _Lands_ cell is the **same** destination, and every row here names a marked decision. So a decision cannot be deferred quietly, and it cannot be listed as deferred and then forgotten either — because each row carries a **witness**, a fact about the tree that is true _while the decision is unimplemented_. When the decision lands, its witness stops holding and the check fails until the row is deleted. A stale entry is as loud as a missing one.

**The destination is a closed vocabulary of three, and both halves read the same one** (F-70). `Phase <n>` — a numbered phase; `Before Phase <n>` — a discrete step ahead of one, which is how D-76 and D-77 both landed; `Remediation` — work booked to neither, which is what a review's findings produce. **An unrecognized destination fails the suite rather than being skipped**, and that is the half worth stating: while the marker and the row were each read as `Phase _n_` and anything else was dropped, a decision deferred to any other destination parsed as _no marker and no row_ — and two silences agree with each other, so both completeness assertions passed on a document that accounted for nothing. Nothing shaped like a marker or like a row of this table may go unparsed either.

Two witness forms, both source-level: `absent: <path>` — the path does not exist — and `present: <path> :: <text>` — the file still contains the thing the decision replaces.

| Decision | Lands | What is missing | Witness |
| -------- | ----- | --------------- | ------- |

~~**Four of these six share one witness**: the behavior is absent, and one absence is why all four are unimplemented. They retire together, in the pass that creates the entry.~~ **They did, on 2026-08-16** — Phase 19 created `src/free-drag.ts` and every absence witness stopped holding in the same commit, which is the retirement this paragraph predicted. D-75's was separate because half of it was a rename inside the behavior that already ships, and that half landed with them: `DragErrorContext` is `SortableErrorContext` now.

**The table is empty again, and an empty table is a state rather than an omission.** Checkpoint E's three blocking decisions populated it on 2026-08-16 and retired from it the same day, in the remediation pass that implemented them — the shortest cycle the instrument has run, and the one it is best suited to: three decisions taken together, listed, and then checked off against witnesses rather than against memory. D-85's witness was `captureLocalSpace`'s declaration and the two behavioural ones were absent test files; all three stopped holding in one commit. ~~Every decision this contract states in the present tense is implemented.~~ It has now been empty, re-populated, emptied and re-populated again, which is the cycle the instrument is for: D-81 re-populated it with a single source comment — the one place in the tree that still names `action.prepare` for a bounds source — and the paragraph is struck rather than deleted because the empty state it describes is what the relaxation below was written against, and that relaxation still holds. The instrument stays — `tests/decisions.node.test.ts` asserts both directions and the destination vocabulary whether or not there is a row to check, and its `rows.length > 0` guard is what would otherwise let the two completeness assertions pass vacuously. That guard now fails on an empty table, which is the one case it was not written for; it is relaxed to _the section parses_, and the vacuity it exists to prevent is covered by the marker→table direction instead. **That relaxation is what let the row retire cleanly here**, and Phase 20 exercised it a second time. **D-90 re-populates it a third time, and this row is the first booked against a decision this ledger had already recorded as implemented** — which is the case the instrument was built for and had not yet met: not a decision deferred and forgotten, but one _marked done_ whose stated deliverables were never in the tree. The witness is the fixture comment asserting a discrimination the fixture does not have, so the row retires when the instrument is made to measure what D-90 forbids rather than when anything is merely renamed. **It retired on 2026-08-18, on exactly that condition and not on a rename.** The fixture now records the receiver each site hands it, and the reversion the row named was run before the row was deleted: three `spec.ts` sites bound fails three rows, the assembler's `retire` bound fails the fourth. The table is empty a fourth time — and this cycle is the one worth keeping, because the entry that opened it was booked against a decision already marked implemented, and closing it required producing the two artifacts the marker had claimed rather than restating the claim. **D-94 opens a fifth cycle, and it is the first row booked against a defect in a decision's _wording_ rather than in its delivery.** D-92 and D-93 both landed everything they named; what neither noticed is that the sentence they landed points at the wrong object. The witness is therefore the superseded sentence itself rather than a missing artifact — the row retires when the published declarations name the capability record, and cannot retire on a rewording that leaves the referent alone. **It retired the same day, on exactly that condition.** Both declarations now state the obligation self-referentially — _never invoked with this record as their receiver_ — so the sentence carrying the old referent is gone rather than rephrased around, and the five-site instruments that always compared against the nested record kept passing unchanged, which is what a referent correction with no runtime component should look like.

**The API pass opened a seventh cycle with four rows at once, and all four retired together on 2026-08-22.** D-108 through D-111 were booked as one remediation slice because they are one pass over the published surface rather than four independent errands, and each carried a source-level witness: ~~`src/kernel/dev.ts`~~'s `export const DEV`, `free-drag/config.ts`'s unqualified `OnStart`, `sortable.ts` re-exporting `AxisInstaller` alone, and `package.json`'s `"drag.js.map"`. **Every one stopped holding in the implementing commit** — the module is deleted, the three aliases are qualified per behavior, `SortableInstaller` publishes beside its sibling, and the stale allowlist entry is gone. **Three of the four also landed a permanent instrument rather than only a change**, which is what stops the class recurring: the packed-tarball suite now scans `.d.ts` as well as `.js` for dangling `sourceMappingURL`, checks that every `files` entry exists in the tarball, and reads the four author-facing assertion messages out of the packed `kernel/`; `packaging.node.test.ts` derives the clean pathspecs and asserts they cover every shipped path; and `vocabulary.node.test.ts` pins the `__DEV__` binding tiers to `['sortable']`, so a kernel re-gating fails directly. The table is empty a seventh time.

**D-100's row retired on 2026-08-21, on exactly the condition it named.** It was booked to retire not when the fast path landed but when the permanent equivalence assertion existed beside it, and that is the order the work took: the assertion is `__DEV__`-gated, runs on every fast-path refresh in every suite run, and was driven into D-100 case 3 — a `transform` on a single in-span, non-witness row — to show it rejects a buffer the fast path got wrong, with the un-perturbed fixture as its control. Four mutations of the shipped code were then run against the whole sortable suite; the one that shifts `δ` across the wrong range fails **three files**, two of them composed drags and one of them driven by real browser input, which is what establishes that the assertion guards the fast path where it actually runs rather than only where it is unit-tested. The witness was the `invalidate()` doc sentence _one flag, because any single reason forces the same full rebuild_ — a sentence the design makes false, since the flag is now a flag **and a count**, and the count is the reason signal. The table is empty a sixth time.

**D-102's row retired on 2026-08-21**, on all three of the conditions it named at once: the fast path is `src/sortable/verified-refresh.ts`, `bench/size` asserts its absence from the `minimal (xy)` graph, and the declared budgets are green. The witness was `RESYNC_INTERVAL`'s presence in `rect-index.ts`, and it stopped holding in the strongest available way — that file is byte-for-byte its pre-P-06 self, so the witness is not merely relocated but the module it was in is unchanged. **The absence assertion was falsified before being trusted, and the first attempt was too weak to count**: a dead reference to the module from `xy()` did not fail the gate, correctly, because a reference the bundler eliminates never enters the graph and the claim is about reachability rather than about an import statement. A real `createVerifiedRefresh` call fails both the graph row and the budget row. The table is empty a seventh time.

**D-103's row retired on 2026-08-21.** The witness was the after-witness read following the anchor's `δ` test with nothing between them, and it stopped holding twice over: a second in-span witness at `hi − 1` now sits between them, and the three unchanged-witnesses were unified onto the outcome triple in the same pass, so the line the row named is gone rather than merely displaced. **The guard was falsified against its own absence**, which is what the row asked for: removing the second witness fails five tests, requiring it to be read but not to agree on `δ` fails two, and moving it to `lo + 1` fails six. The table is empty an eighth time.

**D-104's row was removed on 2026-08-21 as a declined decision, restored the same day, and retired the same day again on landing** — three transitions in one pass, and the middle one is the interesting one. The decline rested on a drivable-size bound extrapolated from a pre-P-06 curve and on an interval the measurement had skipped; both were caught in review, the interval was measured, and the candidate was earned. **The witness held correctly throughout all three**, which is the lesson: a row taken out on a decline is taken out on an _argument_, and an argument that skipped an interval is exactly what a witness cannot catch. On landing it stopped holding for a reason worth recording — the witness named _the growth branch standing alone_, and the branch is not standing alone any more, because growth and shrink shipped as **one** decision rather than as two adjacent ones. The table is empty a tenth time.

**What this table is not.** It carries decisions the contract states in the present tense and a later step — a phase, a pre-phase step or a remediation pass — implements. It does **not** carry D-10, whose mechanism is deliberately _unspecified_ and unbuilt: an unimplemented specification and an unwritten specification are different states, and only the first is a gap between what this contract says and what the code does. It also does not carry findings — F-67 is open, bounded and scheduled in its own row, and a finding is a question rather than a commitment.

## Findings

Severity is about the model, not implementation effort. Resolved entries are kept with their original numbers so the review record stays readable.

| ID | Finding | Status |
| --- | --- | --- |
| F-1 | The prepare/effect split raises the callback count | **Downgraded to a note.** Thirteen top-level `BehaviorSpec` members plus one optional, ~18 functions, against probe 1's fifteen. A wash, not a cost — and the Phase 14 revision added one optional member, not a protocol. |
| F-2 | Part factories must be deterministic and folded in a fixed order for both frames; TypeScript proves neither | **Open, tier C.** ~~`__DEV__` shape assertion.~~ **Unconditional since D-108** — the key-set assertion is a production check in every build, so the mitigation is stronger than this row claimed until 2026-08-22 (corrected here; it was an instance of F-81 created by the decision that closed F-81's first). Much narrower than before D-15, which removed the kernel-slice half. |
| F-3 | "No aggregate type" is conditional on features abstaining | **Resolved, and restated.** No _participant_ declares a concrete whole-frame type; the kernel's private generic is `KernelFrame & Part`. It holds because of how composition is expressed (D-15), not because features are forbidden. |
| F-4 | ~16 closures per controller from D-4 | **Measured and accepted (M-2, 2026-08-02 — [measurements/m2.md](../measurements/m2.md)).** The closure model costs **3.6× the heap per controller — 506 B against 141 B, stable from 100 to 1000 controllers**. It also **calls at least 2.9× faster** (0.0013 µs against 0.0038 µs), because a captured closure is a direct call while the static-spec form is a property load plus an indirect call. So the trade is not "pay heap for simplicity" as this entry assumed — it is pay heap for speed on the one path that runs every frame. Kept. |
| F-5 | `admit` runs inside native dispatch and can throw into the event loop | **Resolved by Q-1's answer, and this row said "Open" until Checkpoint D review 4 (C4-03).** Inherited from probe 1's Q-1. The wrap is three lines and is in place; the observable question Q-1 held open is answered in §[05](05-lifecycle-invariants.md)'s resolved table — a throwing `admit` becomes a **controller-level report**, not a classified failure, because admission runs before operation identity is minted. The kernel catches the throw, leaves the controller idle and usable, and reports through `onError` with `FAILURE_ADMISSION` and no operation, via `BehaviorSpec.reportFailure` (Phase 4). |
| F-6 | A forgotten gate hold silently finalizes early | **Reduced to the behavior's half by D-41 (Revision 2).** With readiness deleted the consumer cannot forget a hold, because the consumer never had one to take — the row below describes obligations that no longer exist. What survives is first-party: a behavior installing `landing()` and never taking the corresponding hold, which stays a **test obligation** for the same reason it always was, sealing detecting a _late_ hold and never a _missing_ one. Original text follows. ~~**Partly resolved, and improved by D-33.**~~ Sealing detects a _late_ hold, never a _missing_ one, so the structural claim was too strong. D-33 removes most of the consumer's half: a consumer no longer creates or supersedes a readiness gate; a stale acknowledgement is rejected and reported; and acknowledging _without_ having declared is reported too, which is a declared contradiction rather than an inference from DOM mutation. What survives is one row — declaring nothing and rendering anyway — and it is why this stays a **test obligation**: any fixture that renders asynchronously must declare as well as acknowledge. The behavior's half — declaring a presentation and then not holding the gate — is first-party code and remains a **test obligation**: any fixture installing `landing()` or declaring an authored presentation fails loudly if the corresponding hold is never taken. |
| F-7 | Landing-target arithmetic duplicates across behaviors | **Accepted, minor.** Export it as a pure helper, never a seam. |
| F-8 | D-8 adds an indirect call to the move path | **Withdrawn — it never did.** Probe 1 and this model spend the same three post-`MOVE` calls (`spec.moved`, `lift.write`, `frame.schedule`); only which party makes the middle one changed. Kept as a number only so the review record reads continuously. |
| F-9 | The kernel cannot type the consumer resolution; it threads an `unknown` | **Neutral.** Validation was always the behavior's. |
| F-10 | Contribution objects are structurally polymorphic at the assembler | **Non-issue.** Construction-time, once per feature. Recorded so it is not rediscovered. |
| F-11 | `resetFramePart` exhaustiveness is unprovable | **Open, tier C, inherited.** ~~`__DEV__` heuristic~~ **unconditional since D-108** — it catches retained objects, not stale scalars, and now does so in every build (corrected 2026-08-22; F-81's class, in the findings register). Identical in probe 1. |
| F-12 | Teardown crosses two owners: `spec.retire()` may throw, and runs while the behavior's DOM is still attached | **Resolved by contract.** The seven-step order in [01](01-construction-ownership.md) is normative; the kernel wraps `retire()`. |
| F-13 | The landing target goes stale when the authored commit inserts, removes or resizes content above the placeholder | **Confirmed by the probe, resolved by D-16.** |
| F-14 | React repositions or detaches the injected placeholder | **Disproved.** The probe establishes that React does neither. |
| F-15 | A new keyed item inserted into the destination gap leaves the placeholder in the wrong **semantic** gap | **Confirmed by the probe, resolved by D-16**'s re-anchor. |
| F-16 | When a short landing completes before readiness, the authoritative correction at the join is visually abrupt | **Accepted, quality only.** Correctness is unaffected. A retargetable runner improves it; the kernel guarantee does not depend on one. |
| F-17 | `anchorTarget()` and `retarget()` on the readiness path are fallible, with no defined failure response | **Resolved by contract.** Neither is load-bearing, so both are best-effort reports rather than classified failures: the runner keeps going, no hold moves, and the join's authoritative pin is what decides correctness (I-29). |
| F-18 | A post-commit `effect` has no rollback, so it can leave a resource visible but unowned | **Resolved by contract.** Register each release before making the resource visible; publish private references only once every resource is owned; consumer callbacks last (I-30). |
| F-19 | The generic transition driver was not total: an `effect` throw escaped to panic, `rollback` failure had no policy, and activation's discard and post-effect checkpoint were missing | **Resolved by contract.** One shared core, per-seam wrappers with their own discard and failure policies. |
| F-20 | Legal `null` returns from `release.prepare` and `settlement.prepare` could strand an operation; `ResolutionGate` had no linearity | **Resolved by types.** Release stages a non-nullable `ResolutionCommand` (D-18); settlement returns `PreparedSettlement \| SeamRejection`. |
| F-21 | A synchronous landing completion raced the hold that was installed after `start` returned | **Resolved by contract.** Request → seal → arm (D-7). |
| F-22 | The join and feature retirement were not robust across code the kernel does not own | **Resolved by contract.** Presentation release in a `finally`; runner destruction best-effort; per-hook wrapping in reverse installation order. |
| F-23 | `host.fail` targets the current operation, so a late continuation could fail the wrong one | **Resolved by contract.** Driver classification, the kernel's `inSeam` latch, `FeatureContext.report` instead of `fail`, and a closed `FailureStage` union. |
| F-24 | The hot-path accounting contradicted itself: "zero allocations, two indirect calls" versus a transform string and three calls | **Withdrawn, restated, then measured (M-1, 2026-08-02 — [measurements/m1.md](../measurements/m1.md)).** Three indirect calls, one string, plus a `{ x, y }` in the in-place lift mode. The generic frame copy this entry queried is **0.098 µs of a 2.64 µs sample (3.7%)** and is kept — with the bound M-1 found: its cost jumps 10× between 12 and 16 behavior-part fields, and this frame sits 4 below that cliff. |
| F-25 | The reentrant-cancel counterfactual reversed FIFO | **Corrected.** `cancel()` from inside `onReorder` enqueues first and wins; the completion is stale. Now a test, not a counterfactual. |
| F-26 | The tree-shaking criterion named an impossible minimal build | **Corrected.** A minimal one-dimensional sortable contains one-dimensional axis geometry; what must be absent is _unselected_ geometry, free drag, landing and layout animation. The subpath/export table is now written down. |
| F-27 | Classification did not stop incompatible continuation: activation retired past its own failure, release invoked `onReorder` after a failed effect, settlement armed a half-requested plan, the join emitted `onFinish` for a failed drop | **Resolved by D-23.** F-19 was not actually resolved before this. |
| F-28 | An invalidating collection replacement was discarded along with the consumer's update | **Resolved by D-25.** |
| F-29 | The five settlement statuses had no total mapping, and the reference turned a skipped/no-op resolution into a rejected, home-recovering drop | **Resolved by D-24.** |
| F-30 | A resource _returned_ from a reentrancy-capable callback could leak — a `LandingStart` handle, or a whole operation minted after `admit` destroyed the controller | **Resolved by D-26.** |
| F-31 | The reference placeholder writer could not express a start gap, and `homeInsertion` carried no real neighbours | **Resolved by D-27.** |
| F-32 | The `ACTIVATING` collection deferral contradicted FIFO — `onStart` runs before `START_COMMITTED`, so an `updateItems()` from it is appended first | **Resolved by deletion.** I-30 already publishes the runtime before `onStart`, so `ACTIVATING` reconciles exactly like `ACTIVE`. The invalidating case is resolved by `host.cancel` latching synchronously, so `START_COMMITTED` cannot activate a cancelled operation. |
| F-33 | Kernel-owned cancel/failure could not construct behavior-owned terminal state — `outcome`/`recovery`/`domain` live in the behavior's part and no seam could write them | **Resolved by D-24.** All five settlement cases return to the behavior. |
| F-34 | `host.fail()` bypassed `SeamOutcome`, so an explicit classification still allowed every forbidden continuation | **Resolved by D-28's latch.** |
| F-35 | A landing-create failure rolled back its hold and let the _original_ accepted settlement finalize before the queued checkpoint | **Resolved by `ArmOutcome`** (D-28). |
| F-36 | A throwing `resetFramePart` could skip the second scrub and the ingress abort, making `destroy()` non-terminal | **Resolved by D-29.** |
| F-37 | `finalized` used a binary accepted-vs-everything predicate, sending the no-op result to `onCancel` | **Resolved by an exhaustive switch** on the domain discriminant. |
| F-38 | Revalidation existed after `LandingStart` but not after the `anchorTarget` before it, so `start` could run after a synchronous `destroy()` | **Resolved.** Revalidate on both sides. |
| F-39 | The final pointerup render existed only in the trace, not in the normative seam | **Resolved.** It is part of `release.effect`. |
| F-40 | `moved()` had no wrapper, so a render or schedule throw became a panic | **Resolved.** Kernel-wrapped; scheduling narrowed from inside via the latch. |
| F-41 | The public proposal and result types regressed probe 1's preserved contract | **Resolved by D-31.** |
| F-42 | "Public stable feature type, internal unstable shape" was not a coherent boundary | **Resolved by D-30's brand, then resolved differently by D-45 (Revision 2).** The incoherence was in trying to make one _value_ both. D-45 splits it across two levels instead: the config record is public and stable in full, and the installer it names is opaque in full. Neither half is the third state this finding rejected. |
| F-43 | The SPI had no route for discrete input: `admit` takes a `PointerEvent`, `KernelHost` owns no extensible ingress, `dispatch` returns `void`, and nothing mints an operation without a press | **Resolved by D-32.** Probe 13a, four compile-proved negatives. The load-bearing one is `dispatch` returning `void`: the information flowed the wrong way through the only behavior-initiated entry that existed. |
| F-44 | `BehaviorSpec.activation` pinned its staged type to `HTMLElement`, so a behavior that stages nothing had to return an element it does not own and an `effect` that must ignore it | **Resolved by D-34.** Probe 13c N-1 — the sortable's shape written into the kernel rather than into the sortable. |
| F-45 | `LandingContext.from` was derived from pointer displacement, so a constrained, snapped or externally driven visual lands from a position it is not at | **Resolved by D-35.** Probe 13c N-2. The signature is a landing that _opens with a jump and ends correctly_, because the target is behavior-supplied and the kernel re-pins at the join — the same shape Phase 11 found in the lift geometry, with every test passing throughout. |
| F-46 | The authored-presentation protocol put four obligations on the consumer, whose only failure signals were a 500 ms silence and, for a hold never taken, nothing at all | **Resolved by deletion (D-41, Revision 2).** All four obligations go with the protocol: a consumer that renders asynchronously awaits its own commit inside `onReorder`, which is an ordinary `await` with ordinary failure behavior, and there is nothing left to declare, acknowledge, mismatch or forget. This is the stronger resolution the row below was reaching for — including obligation 3, which it called irreducible, because it was irreducible _given a protocol_. Original text follows. ~~**Largely resolved by D-33.**~~ Probe 13b B-1. **Inherited from the shipped package, not introduced by the rewrite** — the identical `createCommitTracker` is in both packages' stories. Not a correctness defect: every shipped and ported story works. Obligations 1 and 2 are gone; 3 is irreducible; 4 **splits** — losing an acknowledgement you declared is bounded by the deadline, while never declaring at all stays undetectable at tier C, because the declaration is opt-in and flipping its default would break the legitimate synchronous consumer. I-35 and 05 §F-46 are worded against that split and claim nothing more. |
| F-47 | The terminal barrier stopped at the seam boundary: a behavior invoking consumer code more than once inside one seam had no obligation between invocations | **Resolved by I-36, then re-scoped by D-37 (Revision 2).** Back-filled into this table at Revision 2 — it was recorded in [05](05-lifecycle-invariants.md) §F-47 and `ledger.md` L-12 and never carried here, which is exactly how a finding gets rediscovered. The defect was real and its fix is retained at the **floor**: a sequence that calls consumer code repeatedly reads the latch between calls. What is withdrawn is the **ceiling register** the fix grew afterwards — the promise of no geometry read at all — and the stretch table that tried to discharge it whole-program. |
| F-48 | Admission calls `preventDefault()` before the activation threshold, so a press that never becomes a drag still consumes native focus, caret, selection and form-control behavior — and command admission never asks what the event landed on | **Confirmed by probe E, resolved by D-46.** Eight of ten cases release-blocking, **six with no drag ever activating**. The sharpest is a single `ArrowRight` inside a nested text input: caret frozen, `defaultPrevented`, `onStart` once, request `{from:2,to:3}`, `onFinish` once — a complete accepted reorder from one keystroke in a form field. Two root causes, one per ingress, and both are policy: declining is already total and only `pointerdown` was ever prevented, so click, `href` and ctrl-click were never at risk. |
| F-49 | A destructive authored commit lands the row at the viewport origin and reports complete success | **Confirmed by probe C1, resolved by D-42.** `replaceChildren`, an `innerHTML` rebuild and container replacement all detach the placeholder, which then measures `0×0` at `(0,0)`; the row travels `(46,133) → (0,0)` over 12 frames and teleports back. **All five strategies report `onFinish` once, `onError` zero times and leave zero residue** — the failure is invisible to the consumer and unattributable from the outside. D-42 declines recovery and adds the two-read precondition check instead. |
| F-51 | Consumer callback slots declared as method shorthand are checked **bivariantly**, so the four-arm exhaustiveness D-62 promises is not checked at all — and the inline property form that fixes it is rewritten back to shorthand by this repo's `method-signature-style` lint rule on every `lint-fix` | **Found by the Revision 2 compiled fixture, resolved by named type aliases.** The fixture's `n12` was written to assert that a handler narrowed to `accepted \| noop` cannot be an `onEnd`; it compiled. Two layers had to be peeled: `strict` does not make method parameters contravariant, and `just lint-fix` silently undid the first fix. **A rule the next format reverses is not a rule** — 03 §The schema now declares every callback slot as a named alias and says why. This is the first defect in either revision found by a machine rather than a reader. |
| F-52 | The contract claimed `duration: () => number` becomes a compile error under D-67; it does not, because a zero-parameter function is assignable to any signature | **Found by the fixture, corrected in place.** `n6` was withdrawn. The correction is a net gain: the migration is **source-compatible** — a shipped `() => 200` keeps working and keeps returning the right number — which nobody had claimed because nobody had checked. Enforcement would need a runtime arity check, which is not worth its cost for a form that still behaves correctly. 03 §`landing()` now says _removed from the documented surface_ rather than _deleted_. |
| F-55 | The contract's two-window footprint rule computes **`0`** in the default composition, where `box === visual` | **Found in implementation, corrected in place.** 06 §Two windows asserted that under `box === visual` the subtraction _"is just the visual's own height"_ — i.e. that `boxPost ≈ 0`. It is not: the sortable lifts with `LIFT_FAITHFUL`, whose promote branch sets `position: fixed` plus an explicit width and height, so the lifted element's offset box is **unchanged** across `acquireLift` and `boxPre − boxPost` is zero. The pre-D-43 code depended on exactly that — `applyMechanics` read `visual.offsetHeight` _after_ the lift and got the right number. api-1 never measured this case: every case it ran used a nested pair (`box = .row-box` wrapping `visual = .card`), where the box genuinely collapses (62→32, 62→2) because it stays in flow while the visual leaves it. **The rule is now stated on the identity** — `box === visual` ⇒ `boxPre`; otherwise `boxPre − boxPost` — which keeps api-1's result for the nested case and the pre-existing behavior for the common one. D-43, D-52 and the 06 trace are corrected; the owner decision is untouched, since the two-window rule is what api-1 established and nothing here withdraws it. |
| F-56 | D-42's precondition is attributed to the **kernel** in 06's trace, which cannot perform its two reads | **Found in implementation, corrected in place.** The trace marks the check `[K]`, but both reads are behavior state: the placeholder belongs to the sortable runtime, and `item` is a frame field H-2 and D-15 forbid the kernel to name — the same "the kernel learns one sortable-shaped thing" defect Checkpoint C found four times. The check therefore opens `anchorTarget`, and the ownership question turns out not to matter to the _behavior_ the contract specifies, because D-49 already requires a failed check and a throw to be treated identically: the behavior throws a diagnostic naming the placeholder and the item — which the kernel could not have authored — and the kernel routes it on the quality track. 06 is corrected; D-42 and D-49 are untouched. |
| F-57 | `removeAttribute('style')` does not remove the attribute once an inline property has been written through the CSSOM | **Found in implementation while landing D-39, measured, worked around.** In Chromium — connected and detached elements alike — an element whose `style.width` was set keeps `style=""` in `getAttributeNames()` and `outerHTML` after `removeAttribute('style')`; `attributes.removeNamedItem('style')` removes it cleanly. This matters because 05 states D-39's guarantee as an **attribute-map comparison** and names `style=""` as a residue that must be absent, so the difference is the guarantee rather than a cosmetic one. The undo removes the attribute node. **The obvious alternative is worse**: restoring three style _properties_ individually cannot clear the attribute at all, which is the ordering rule 05 was reaching for and which this replaces with one whole-attribute restore. |
| F-58 | The two-window footprint subtracts on **both** axes, so `footprint.width` is `0` for every composition where `box !== visual` | **Found in application review 1, corrected in place; D-43's owner decision and F-55 both stand.** `boxPre − boxPost` measures a **collapse** — a scalar on the list's flow axis — and the footprint it feeds is a **box**, two extents; the contract stated the first and wrote the second. On the cross axis a block-level box in a vertical list takes its width from its containing block on both sides of the lift, so the difference is `0`: arithmetically correct and the wrong quantity. The placeholder therefore shipped `width: 0px` **unconditionally** on the composed branch, and the landing target is displaced as well wherever the cross alignment is not stretch — `align-items: center`/`end`, `margin-inline: auto`, `direction: rtl` — because `anchorTarget` reads the placeholder's `left`. **Every fixture that ever measured this rule was height-only** (api-1's cases A and B, probe C1's `180 → 210`, F-55), so `OffsetBox` was carried through symmetrically and the substitution was never checked — including inside the covering test, whose own `footprint.width` is `0` while it asserts `height`. The correction: **`footprint.width = boxPre.width` always; only the height subtracts**, window 2 narrows to `box.offsetHeight`, and the identity branch becomes the degenerate case (`box === visual` ⇒ no collapse ⇒ `footprint = boxPre`) rather than a second rule. **The declared scope narrows with it**: a composed `box !== visual` is supported **with `y()` only**, because the rule fixes the flow axis to `height` and `xy()` over a wrapping flex row was never measured. Three alternatives were rejected — per-axis subtraction with a zero fallback (a two-order-of-magnitude discontinuity across a one-pixel input change, since `offsetWidth` is integer-rounded), threading the flow axis from the axis feature (a discriminator `SortableContribution` deliberately does not have, for a value that is `boxPre` on the cross axis regardless), and leaving the cross extent unset (shrink-to-fit reproduces the defect in the same fixtures, and makes the element `anchorTarget` measures a function of the consumer's stylesheet). |
| F-53 | `destroy(): Promise<void>` cannot be type-enforced against use in a `void` position, so D-36's 46 floating-promise sites are a **lint** obligation rather than a compiler one | **Found by the fixture, recorded as a limitation.** Any return type is assignable to a `void` return position by design, so `tsc` is silent where `no-floating-promises` and `strict-void-return` both fire. Consequence worth stating because it is load-bearing: **the lint rule is the only instrument that checks D-36's call sites**, and a repo that disabled it would lose the check entirely rather than degrade it. |
| F-54 | `DraggableError` was said to require its own entry root, because a class needs one identity across tiers | **Disproved by measurement** — [`measurements/error-identity.md`](../measurements/error-identity.md). A module reachable from two entries is emitted **once** in both build modes: preserved as its own module under `unbundle: true`, hoisted into a shared chunk under `unbundle: false`. Cross-entry `===` and `instanceof` hold in six assertions across three entries. **This is the second justification for the third root to fail** — the first was D-48's structural-dependency argument, voided by D-64. What survives is one declaration site and a tier-neutral specifier, both maintenance arguments. The measurement recommends re-exporting the shared vocabulary from each behavior entry as well, so an ordinary consumer needs one specifier; not applied, and the reason is in the record. |
| F-59 | `kernel.js` publishes no value a behavior must produce, so the kernel tier **cannot be constructed from its own entry** | **Confirmed by the rewired fixture, resolved by D-68.** `BehaviorConfig.liftMode` needs a `LIFT_*` value and `settlement.prepare` needs the `SETTLED_*` values to discriminate D-66's carrier; neither is exported, and no path under `kernel/` is a declared package export, so there is no supported specifier that reaches them. **Stronger than L-14**, which recorded the tier as writable inline and not by annotation — a contextually typed factory still cannot fill `liftMode`, so the tier is writable in **no** style. **Three names, not two:** the fixture's header counts `LIFT_FLAT` and `SETTLED_FAILED` and misses that it also reaches into `src/kernel/failures.ts` for `AT_PROPOSAL`/`AT_CONSUMER`/`CancelStage` — which a behavior needs (D-66 derives the fallback's stage) and which today are published only at the **ordinary** tier. A behavior author's supported route to them is an import from the sortable. **The narrower residue no export closes:** `KernelFrame.phase` is `number`, so a behavior is handed a value with no public domain even after the constants ship; D-68 narrows it to `Phase` on the argument that made `FailureStage` a closed union |
| F-60 | The instrument that checks surface closure has two blind spots, and the kernel tier sits in both | **Found while resolving F-59.** `tests/docs.node.test.ts` is the package's closure check and it reported **17** unresolved references for `kernel.js` — a list D-68 now empties. The 17 are an **undercount**, systematically. (1) **It closes over types, so value holes are invisible**: `LiftMode` is on the list and `LIFT_FLAT` is not, because no public type references the constants — the exact defect F-59 names is the one this instrument cannot see. (2) **It resolves across entries, so a tier inversion reads as clean**: `LandingStart`, `LandingHandle`, `LandingContext` and `Disposer` resolve through `sortable/feature.js` and `CancelStage` through `sortable.js`, so TypeDoc is satisfied while the kernel entry's closure runs **through the behavior tier**. A whole-run check answers _is every public name documented somewhere_; the tier boundary needs _is every name in this entry's closure reachable from this entry's own tier_. Acceptance criterion in [05](05-lifecycle-invariants.md) §Test matrix, _Kernel vocabulary_ |
| F-61 | `ActionTransition` and `SeamRejection` are each **declared twice** — in `kernel/seams.ts` and in `kernel/spec.ts` | **Fixed while landing D-68, and pinned.** Harmless while both were internal and structurally identical; publishing one of each would have made it the hazard 03 §The export topology's identity clauses 1–2 exist to prevent — a consumer's compiler resolves the published declaration while the driver consumes the other, and the two drift independently. Both are now declared **once**, in `kernel/seams.ts` beside `Transition`; `kernel/spec.ts` re-exports them and `kernel.js` publishes them from that single declaration. Source-level assertion in `tests/kernel/vocabulary.node.test.ts` — _should declare the doubly-declared seam types exactly once_ — because a structural duplicate is invisible to both the compiler and a runtime identity check. The row said _open_ after the fix shipped, which review 2 (B-3) caught: this table's own convention is that a finding records its resolution in place |
| F-62 | ~~The kernel classifies two **behavior-generic** seams with the sortable's vocabulary, and not as an overridable default~~ · **renamed 2026-08-15** | **Found by Phase 18, resolved by D-74, landed with D-76's kernel step.** As found: the settlement seam ran under `FAILURE_REORDER_RESOLUTION` (`src/kernel/kernel.ts:1676`) and the action seam under `FAILURE_INSERTION`/`FAILURE_PLACEHOLDER_MOVE` (`:2209`) for **every** behavior — the behavior may narrow from the inside with `host.fail`, but an uncaught throw takes the kernel's name. So free drag's failing `onDrop` reports a _reorder resolution_ failure and a throwing `moveTo()` reports an _insertion_ one, which D-64 maps to a **presentation** fault for library arithmetic. **This is the sharpest available answer to Checkpoint E's first question** — does anything in `kernel/` know a collection, a placeholder or an insertion — and the answer was three of the thirteen constants D-68 published as permanent surface one revision ago. Renaming is free **only** while the kernel tier has no consumer, which is why it was taken now. The three are `FAILURE_RESOLUTION` (8), `FAILURE_ACTION_PREPARE` (4) and `FAILURE_ACTION_EFFECT` (5); the old names in this cell are the record of what was found, and no value moved |
| F-63 | ~~**D-34 and D-35 are contract-normative and unimplemented**, and no instrument says so~~ · **resolved 2026-08-15 by D-76's kernel step**; the instrument is §Decisions not yet implemented above, held by `tests/decisions.node.test.ts` | **Found by Phase 18, scheduled by D-76, landed 2026-08-15.** As found: `BehaviorSpec.activation` was `Transition<Part, HTMLElement, ActivationScope>`, `LandingContext.from` was `pointerX - originX`, and `VisualLiftSession.write` composed and wrote and recorded nothing. All three are now what the contract says — the parameter is threaded to the construction types, `write` records, and `from` is sampled from the session. `plan.md` §Phase 14 books both for "Phases 19–20", so this is a **scheduling fact, not a drift** — what makes it a finding is that two decisions the contract states in the present tense sat unimplemented across a checkpoint and a whole revision with every suite green. **The instrument gap is the reusable part, and it is the half that outlives the fix**: `docs.node.test.ts` closes the documented surface, `packaging.node.test.ts` asserts the entry table, the fixtures assert the shapes that exist, and **nothing checked a decision booked to a later phase against the code that had not implemented it**. A green suite is evidence about the implemented contract only. Acceptance criterion K-5 in [07](07-free-drag-contract.md), discharged by §Decisions not yet implemented and `tests/decisions.node.test.ts` |
| F-64 | Three composition types are structurally identical across the two middle tiers | **Found by Phase 18, deliberately not resolved.** `FeatureContext`, the installer shape and two contribution slots (`startLanding`, `retire`) are behavior-neutral and both tiers need all of them. Inventing a cross-behavior composition vocabulary — a generic `Installer<C>`, a shared contribution base — **is the generalization Checkpoint E exists to evidence**, and building it in Phase 18 would consume the evidence it is meant to produce. What lands instead is the narrower thing that costs nothing: **one declaration, two publications** — the D-68 re-home pattern applied to `FeatureContext` and `LandingOptions`, so the two tiers share a type **identity** rather than a structural coincidence, pinned by acceptance criterion B-7. **Falsifier:** a third behavior, or a first-party capability installable into both compositions without a per-behavior wrapper |
| F-65 | ~~The kernel performs a sortable-shaped measurement at every activation, for every behavior~~ · **closed 2026-08-20 as an accepted, named cost** — M-5 arm A ([`measurements/m5.md`](../measurements/m5.md)). **Window 1 is 0.8–1.6 µs per activation**, bounded in situ at **≤ 0.0125 ms** and **≤ 2.7%** of measured activation at a 32-level transformed ancestry — both legs of the pre-declared rule satisfied by two orders of magnitude, so **the `ActivationScope` SPI does not move again for this reason and a later phase may not reopen it without a new workload showing a different number**. The mechanism is in the source: `acquireActivation` reads `getBoundingClientRect()` on the statement **before** window 1, so the two offset reads are served from a layout that has just been flushed, which is why the cost stays flat while measured activation grows six-fold from shallow to deep ancestry. | **Found by Phase 18, carried to Checkpoint E, measured at Phase 21.** D-52's **window 1** — the box's offset box, read by the kernel immediately before `acquireLift` and handed down as `ActivationScope.boxPre` — exists so a behavior can derive a placeholder footprint from two reads straddling acquisition. Free drag has no footprint, takes no second read and never names `boxPre`; the read happens anyway, because it is unconditional kernel work. **Not a defect and not free**, and sharper than F-62 because renaming cannot fix it: removing it means making window 1 conditional on something the behavior declares, which is an SPI change with no evidence behind it yet. Phase 21 measures it; Checkpoint E decides |
| F-66 | `FreeHomeRequest`'s drop rationale was voided by the `DragSubject` drop, and the ledger asked Phase 18 to re-derive it | **Re-derived; the drop stands, on a different argument.** The original was _"a bare alias of `DragSubject`, which is itself exported from the same entry"_, and Checkpoint D (D5) dropped `DragSubject`, so the alias is no longer an alias of anything public. It is dropped anyway because **the shared kernel name is what was refused, not the shape**: free drag publishes its own two-field `FreeDragSubject` from its own entry — which F-51 requires, since `ResolveHome` names it and a slot a consumer cannot hoist is not a writable surface — and nothing shared is resurrected. **Net: one published name where the shipped package had two**, which is the outcome the original row wanted |
| F-67 | ~~The package's construction-time validation rule predates `CODE_OF_SIZE.md`, and the two behaviors now disagree~~ · **resolved 2026-08-15 by D-77** | **Re-derived across both behaviors as one decision, which is what the row asked for.** The answer is a **required first config argument** with later fragments `Partial` ([03](03-feature-composition.md) §Public option domains, rewritten; [07](07-free-drag-contract.md) §Validation): required configuration is a type obligation, and the only construction-time throw the package keeps is `claim`'s single-writer collision, which is an invariant over what installers _contribute_ and no signature can state it. The sortable goes from six construction throws to one and the two behaviors state one rule. **The half that had to be found rather than decided** is that 03 already asked this question and answered _no_ on evidence that was sound for the candidate it examined and silent about the one that works — see D-77. Historical statement of the divergence follows. **Free drag was reconciled at Phase 18; the sortable was not, and that was the owed half.** 03 §Public option domains states _every option throws a `TypeError` outside its domain_ as frozen and package-wide, and `src/sortable/` implements it — `assemble()`'s three required-slot checks, `requireFinite` for `threshold` and the landing duration, `claim`'s single-writer diagnostic, `copyUniqueItems`. Free drag now ships **zero** construction throws and one predicate ([07](07-free-drag-contract.md) §Validation), so the rule is true of one behavior and false of the other. **The precedent was already inside the package and applied inconsistently**: `src/sortable/landing.ts` declines to validate `easing` on exactly the Code of Size's reasoning — _it is a CSS easing function, the platform is the only correct parser for one, and `animate()` reports a bad value itself_ — three lines above a `requireFinite` on `duration` that the same argument mostly deletes. **Measured, not argued** (Chrome 150): `animate()` rejects `NaN`, negatives, `-Infinity`, strings and objects itself, so those halves of `requireFinite` are redundant; it **accepts `'auto'` and `undefined`**, which a finiteness test would wrongly refuse; and it accepts `Infinity` and never completes, which is the one value a library invariant depends on — so the surviving guard is `=== Infinity`, not `Number.isFinite`. **It did not gate D-76** — the kernel step touched the activation staged type, the recorded lift delta and three stage renames, none of which this question can reach — and **D-76 landed 2026-08-15 without it**, leaving F-67 the last gate holding Phase 19. **Deciding it per behavior is what produced the divergence**, which is why the resolution is one row covering both and why §What would falsify this model's required-slot bullet is struck by the same decision |
| F-70 | ~~The deferred-decision instrument reads one destination form and silently skips every other~~ · **resolved 2026-08-16 by widening the instrument**; no decision needed | **Found by the D-77 landing review's remediation pass, and it is the F-63 shape inside the instrument built to catch F-63.** Both halves — the decision row's **Unimplemented (Phase _n_)** marker and the §Decisions not yet implemented row's _Lands_ cell — matched a numbered phase and **dropped anything else on the floor**. A decision booked anywhere but a numbered phase therefore parsed as no marker _and_ no row, and **two silences agree**: both completeness assertions pass, in both directions, over a document that accounts for the decision nowhere. The gap was latent while every deferred row said _Phase 19_ and was about to be reached in earnest — remediation work is booked to neither a phase nor a release, and D-76 and D-77 had already landed as pre-phase steps the vocabulary could not spell. **Resolved as bookkeeping, not as a decision**: the destination is now a closed vocabulary of three — `Phase <n>`, `Before Phase <n>`, `Remediation` — read from **one** pattern by both halves, and an unrecognized destination, an unparseable marker or an unparseable row of that table is a **failure** rather than a skip. Falsified in both directions against the real document before landing. P18A-17's orphaned D-77 paragraph is removed in the same pass, as the same class of defect the instrument cannot see: prose about a row that no longer exists |
| F-71 | The bounds source is read at the seam that next **renders**, not at `action.prepare` as the table in (07 §Validation) says | **Found while implementing Phase 19, and raised rather than designed around.** `MotionConstraint` has three members and `invalidate()` is a **staleness mark**: scroll and resize raise it many times a second and each resolve is a layout read or a consumer call, so resolving inside it would put both on the scroll path. The rect is therefore re-read on the next `apply`, which runs from `moved` (`FAILURE_RENDERER_WRITE`), from a `moveTo` effect (`FAILURE_ACTION_EFFECT`) or from `release.prepare` (`FAILURE_RELEASE`). **The coarse code is unchanged for the two hot paths** — both map to `presentation`, which is what B-4 (b) asserts and what 07's table is written in terms of — and differs only on the release path, where it is `interaction`. **The alternatives were each worse or unavailable**: resolving eagerly in `invalidate()` puts a layout read on every scroll event; re-raising through a third behavior action contradicts 07's `actionTags: 2`; and adding a fourth `MotionConstraint` member widens a contract type to move a stage. Recorded for Checkpoint E, which is where the question _is a stage a promise or a description_ belongs. **Triaged and closed by D-81**, which corrects the description rather than moving the resolve — and found the finding understated itself twice (F-73, F-74): the first resolve is at **activation**, not at any of the three seams named here, and the release path's `interaction` is not the only divergence. **Closed executably in Phase 20**: all four paths are asserted, each driven to the seam it names |
| F-72 | Free drag measures the visual's box space a **second** time, because the kernel does not expose the linear part it already measured | **Found while implementing D-72, and it is F-65's mirror.** `acquireLift` performs one box-quad traversal and keeps `ancestorA…ancestorD` — the inherited linear part — module-private, handing the behavior a `BehaviorLiftSession` of four members, none of which carries it. `localDelta` needs exactly that inverse, so `activation.effect` calls `coordinates()` again on the same element at the same instant. **One extra traversal per activation, on a path already doing layout work, and never per sample** — the coefficients are captured once and the warm calls are four multiplies, which is what D-72 promises. **Not fixable by the behavior**: the alternatives are re-adding a coordinate module (which D-72 and Phase 19 both forbid) or an SPI addition (which is a kernel change with no evidence behind it yet). **The pair is the interesting part**: F-65 says the kernel measures something only the sortable needs, and this says it withholds something free drag needs from the same traversal. Phase 21 measures both; Checkpoint E decides whether `ActivationScope` should carry the space. **Deferral made explicit by D-82**, which also reconciles the `release.effect` wording this finding sat beside |
| F-73 | The **first** resolve of a bounds source is at `activation.effect`, which neither F-71 nor the code comment names | **Resolved by D-81.** The rect starts stale, and `activation.effect` calls `deriveMotion` to place the visual at the accumulated grab delta — so the first `apply` of any operation happens there, and a garbage source almost always surfaces at `FAILURE_ACTIVATION` → `interaction`. F-71 listed `moved`, a `moveTo` effect and `release.prepare`; `src/free-drag/bounds.ts` lists the first two. **Both omitted the dominant path**, and F-71's claim that the coarse code is unchanged _for the two hot paths_ is true and beside the point, because the path a bad value takes first is neither of them. **The lesson is about how the finding was derived**: it was reasoned from where staleness is _raised_ — scroll and resize — rather than from where `apply` is first _called_, and the resolve site is decided by the second |
| F-74 | Free drag does not instantiate the two-consumer-call barrier with the first-party `bounds()` | **Resolved by D-81, and it is a test-validity finding rather than a defect.** `TAG_POLICY` reads the `axis` source, checks `host.closed`, then calls `constrain.invalidate()` — which under lazy resolution sets a flag and calls nothing. So the sequence F-47 and I-36 describe has **one** consumer-reachable call in the shipped composition, and L-3's fixture, written against `bounds()`, passes whether or not the barrier is there. The barrier is still owed, because `constrain` is a middle-tier slot that admits arbitrary third-party code — which is precisely what the fixture must supply to discriminate. **A guard whose only test cannot fail is indistinguishable from a deleted guard**, which is the same shape as D-77's deleted-check assertions and D-80 (b)'s negative control. **Discharged in Phase 20**: L-3 is asserted with a middle-tier `constrain` installer that records its `invalidate()`, and the `bounds()`-based control is kept beside it, asserting its own _non_-discrimination so a later reader does not mistake it for coverage |
| F-50 | The placeholder is sized from the visual's offset box, which is not the footprint the visual removed | **Confirmed by api-1 and reproduced live by probe C1, resolved by D-43.** Where the box keeps a sibling in flow, the visual's 60 px stands in for a 30 px removal and the list runs 30 px too tall — `180 → 210` — **for the entire drag**, not just at landing. No single-window rule is correct in both nested cases, which is why D-43 takes two. |
| F-68 | ~~The one construction check D-77 retained throws in the one window the unwind does not cover~~ · **resolved 2026-08-16 by D-80 (b)'s landing**; `copyUniqueItems` now runs at the construction boundary, ahead of the first installer, and the negative controls in `tests/sortable/composition.browser.test.ts` fail against the old position | **Found by the D-77 landing review's resolution pass; the review that surfaced the window rated it a scope note (P18A-13), and following it into the code says otherwise.** `copyUniqueItems` throws a `TypeError` on a duplicate element, and it is called from `src/sortable/runtime.ts` inside `createSortableRuntime` — which runs inside `install`, **after `assemble` has returned**. So a consumer whose `items()` yields the same element twice leaves every installer's `retire` hook collected and never run, plus a kernel and a realm `draggable()` has already built and nothing destroys, because `arm()` is never reached. **Consumer-triggerable rather than a hypothetical library bug**, which is the whole difference between a scope note and a defect. **The shape is the part to keep**: D-77 deleted five construction checks and retained exactly one, and the one it retained is the one sitting outside the unwind bracket — so the step that made _the unwind is total_ normative is also the step that made it least true. **Resolved by D-80 (b)** — validate the collection before the first installer runs, which makes the sentence true instead of scoping it down, costs a reordering, and is safe only because installers are externally inert by contract |
| F-69 | ~~A consumer `items()` throw is safe only by argument position, and nothing says so~~ · **resolved 2026-08-16 by D-80 (b)'s landing**; the pull and the assembly are statements rather than sibling arguments, and the second negative control reconstructs the swap and shows it stranding every hook | **Found alongside F-68 and worth its own row, because the fix for one is what removes the other.** `merged.items()` and `assemble(…)` are sibling arguments in the same call in `src/sortable/behavior.ts`. A throwing `items()` is currently harmless for exactly one reason: **left-to-right argument evaluation puts it before `assemble`**, so no installer has run and there is no hook to strand. Swap the two arguments — a change no reviewer would flag, since neither reads as ordered — and a throwing `items()` leaks every installer's `retire` hook. **An undocumented, load-bearing evaluation order is the C5 lesson verbatim**: where a rule names an order, check every site that performs it; here no rule names one, and a site performs it anyway. **Resolved by D-80 (b)**, which pulls and validates the collection as a statement ahead of `assemble` rather than as a sibling argument to it, making the ordering deliberate and the property total rather than positional |
| F-75 | B-4 (b)'s _exactly one terminal_ is false for the classified rows that surface **before** `onStart` | **Found while writing Phase 20's matrix, and it is a claim-side contradiction with D-66 rather than a defect.** The clause is written over the whole classified table, but three of its rows — `handle`, `visual` and a garbage `bounds` source on its first resolve — surface at admission or activation, i.e. before the behavior's progress marker leaves `MINTED`. D-66 is explicit that at `MINTED` the consumer never heard the drag start and **no terminal is published**, because an end for a beginning it has no record of is worse than no end; Q-15 says the same thing for a press that never activates. The two sentences cannot both hold, and the code follows D-66. **Asserted as observed**: `tests/free-drag/validation.browser.test.ts` publishes the report and asserts the _absence_ of the terminal for the admission and activation rows, and asserts exactly one for every row that fails after `onStart` — including the bounds release path, so the split is executable rather than argued. What is owed is one sentence in B-4 (b) scoping the clause to rows that surface after the drag has started; the sortable's equivalent rows are worth re-reading against it, since the phrasing was inherited. **Resolved by D-83, and the re-reading found the rule intact**: I-31 was already correct and already behavior-neutral, so what is scoped is a restatement, not the invariant — and the durable half is the prohibition on restating I-31 in a per-behavior document, since a copy with no antecedent to carry is how the condition was lost |
| F-76 | 07 §Validation, under `CODE_OF_SIZE.md`, attributes a throwing `visual` resolver to **activation**, and both behaviors resolve it at **admission** | **Found by driving the row rather than reading it.** The table pairs `visual` with `onStart` at `FAILURE_ACTIVATION` (2) → `interaction`, citing the kernel's activation lines. Neither behavior calls the resolver there: free drag resolves the visual inside `admit` — D-59's common form, since `box === visual` for a behavior with no placeholder — and the sortable resolves it in `seedDraft`, which is admission's second half and split out for D1's reason. A throw is therefore `FAILURE_ADMISSION` (1) → **`consumer`**, one seam earlier and one code away from the row. **The code is right and the row is wrong**: the visual resolver is consumer code called during admission, so `consumer` is the honest attribution and `interaction` would be a worse one. The kernel line numbers in that column are the site that _names_ the stage, and reading them as the site that _calls the resolver_ is how the row was derived. Pinned as observed in `tests/free-drag/validation.browser.test.ts` — _should classify a throwing visual resolver at admission, not at activation_ — so the correction cannot be lost, and the same re-reading is owed for the sortable's copy of the table. **Resolved by D-84, which corrects both behaviors and splits the row** — and the derivation error is a repeat: D-81's `bounds` row went wrong the same way one decision earlier, by reading a stage off the kernel line that names it rather than off the site that calls the resolver. Two instances from different starting points make it a pattern, recorded at D-84 rather than left in this row |
| F-77 | `drag.js` and `kernel.js` are published runtime entries with an isolation claim and no standing bundled-graph assertion | **Closed 2026-08-22 by the harness change it asked for, plus one it did not.** Both roots are now declared compositions in `bench/size/measure.ts` and are asserted on every `just size`: `vocabulary root - drag.js` at **121 B** with `only: ['kernel/errors.js']`, and `kernel root - kernel.js` at **6,514 B** with `present` and no `sortable/` or `free-drag/`. **The graph half needed a capability the harness did not have**: the invariant is _`drag.js` reaches `kernel/errors.js` and nothing else_, which neither `absent` nor `absentPrefixes` can state, because the one module that must appear lives inside the one subtree that must not — so `Composition` gains **`only`**, the bundled graph exactly, on the same vacuity argument that produced `absentPrefixes`. **And the graph half alone would not have closed it.** The packed `kernel/errors.js` carries a **bare** `import "./failures.js"` — `tsdown` inlines the thirteen `FAILURE_*` constants as literals — so machinery arriving from `failures.ts` lands _inside_ `errors.js` and moves no module count. **Both predicted regressions were injected and each is caught by exactly one half**: a `DraggableError` method returning `STAGE_TO_CODE` leaves the graph at one module and grows the artifact **121 → 190 B**, caught by the budget; a side effect in `failures.ts` adds `kernel/failures.js` while staying **under** budget at 140 B, caught by the graph. Neither trips the other's half, so the two are not redundant. **The budget is 29 B of headroom rather than the standing ~150 B, deliberately** — that convention is sized to roughly one module against 8–13 kB compositions, and on a 121 B root one module's slack is larger than the artifact; under the first regression every other row moved ~10 B and stayed green. `plan.md` §Phase 22 forbids closing this _by widening a budget instead of adding the assertion_: the assertion is added and the budget is **narrowed**. **One fact the rows carry that the pass did not know**: `kernel.js` does not pull `kernel/errors.js` either, so the two roots are **disjoint** rather than nested and D-48's _neither tier should have to import the other_ holds in both directions. Each mutation was built, measured, reverted and rebuilt; **no production code changed** |
| F-78 | `kernel/dev.ts` strips author-facing assertions on a premise Revision 2.1 voided, and its own stated revisit condition has fired unnoticed | **Closed by D-108, implemented 2026-08-22**: all four checks un-gated, `kernel/dev.ts` deleted, cost 282–305 B against the pre-slice tree and the budgets re-based. The packed tarball now carries the four assertion messages and `consumer.node.test.ts` reads them, which is where the gate was invisible before. **Found in tier B. Found by the D-107 optimization sweep, while testing whether two unguarded validators should be `__DEV__`-gated — the answer came back inverted.** `dev.ts` documents the trade as costless: _these assertions check behavior authoring … and behavior authoring is not on the public surface (contract 03 §The public/internal boundary). **The day it is, this needs revisiting**: either a dual build under an `exports` condition, or an inline `process.env.NODE_ENV` comparison at each site rather than a shared constant._ **Behavior authoring has been on the public surface since Revision 2.1** — D-61 published `sortable/feature.js` as the ladder's second rung, D-70 added `free-drag/feature.js`, and D-68 rebuilt `kernel.js` to publish `BehaviorFactory`'s structural closure (33 values, 35 types) for the express purpose of third-party behavior authoring; 03 §The public/internal boundary now reads _three tiers over three entry roots_. **So a third-party behavior author gets no frame-shape, reset-exhaustiveness or staged-value assertions in any build they can produce**, and cannot turn them on, which is precisely the cost `dev.ts` said would arrive with a public authoring surface. **Two consequences, and the second is why this is not a size finding.** It settles D-107's A1/A2 declines — gating more author-facing validation would deepen the gap rather than close it. And the remedies are `dev.ts`'s own two, both of which **cost the production bundle nothing**; choosing between them is an `exports`-map decision, so it belongs to Phase 22's **API** deliverable rather than to bundle structure. **Not resolved here on purpose**: this pass was scoped to bundle cost, and the fix adds a build condition rather than removing bytes |
| F-79 | `just clean-build` never cleans two directories the tarball ships, and the failure has already fired once | **Closed by D-111, implemented 2026-08-22**: `files.json`'s `clean.extras` gains `kernel`, `shared` and the two `landing-runner` orphans, and `packaging.node.test.ts` derives the pathspecs and asserts they cover every path `files` ships — held without running `git clean`, so it fails on a tree that was never built. **Recorded because the evidence is a live artifact rather than an argument.** `clean-build` derives its pathspecs from `files.json`, adding a **directory** pathspec only when an entrypoint contains a `/`. `sortable/*` and `free-drag/*` entries exist so those two are cleaned; there is no `kernel/x` entry and `shared` is never named at all, so **`kernel/` (42 files) and `shared/` (4 files) survive a clean-build** — both shipped by `package.json` `files`. Verified by running it: `sortable/` and `free-drag/` are removed and the other two are not. **So a module deleted from `src/kernel/` or `src/shared/` leaves its `.js`, `.js.map` and `.d.ts` on disk inside the allowlist, and the next `npm pack` publishes them.** **The proof it has already happened is sitting in the package**: a root `landing-runner.js` and `.js.map`, orphaned when the source moved from ~~`src/landing-runner.ts`~~ to `src/shared/landing-runner.ts`, with the old path still in the map's `sources`. **They do not reach the tarball — and only because that file fell outside the allowlist.** The same accident one directory over publishes silently. `files.json`'s `clean.extras` currently holds only `_virtual` |
| F-80 | Four divergences between the two ordinary entries carry no recorded reason, where every other one carries a `D-nn` | **Open, tier B. Not decided here on purpose** — each has a defensible reading, none violates a written rule, and `axis` in particular has a real rename cost. (a) **`axis` names two unrelated things**: `axis: AxisInstaller` is **required** and is an installer (`y()`/`xy()`); `axis?: DragAxis \| AxisSource` is **optional** and is a motion lock. Aggravated by `AxisInstaller` beside `AxisSource` — both `Axis*`, both published, nothing in common — and the free-drag meaning is the conventional one. **D-75's rule does not reach this**, because that rule governs published type names and `axis` is a config key, which is exactly why this is a finding and D-109 is a decision. (b) **`threshold` is documented by deferral to a silent declaration**: free drag says _Same default and domain as the sortable's_ and the sortable's is `threshold?: number` with no comment, unit or default; the unit exists only at `kernel/spec.d.ts`, which the ordinary consumer is told not to import, and **neither ordinary entry states the default at all**. (c) **Two callback asymmetries**: free drag has `onMove` and the sortable has no per-sample callback, and the sortable's `onStart` receives only the item while free drag's receives a full `DragGeometry`, so a sortable consumer cannot observe pointer state at activation. (d) **`FailureStage`'s doc claims a consumer role D-64 removed** — `kernel/failures.ts` still says _It is **public**: a consumer receiving `onError` has to be able to discriminate it (D-30)_, while D-64 took `stage` out of the error context and neither ordinary entry publishes the type. The type's placement is right; **the sentence is what is stale**, which makes (d) an instance of F-81 rather than of this row |
| F-81 | A landed decision repeatedly leaves standing the prose that justified its predecessor, and a later reader acts on it | **Open, tier B. The class this API pass actually found, and it is not the one the brief anticipated.** Four live instances: `dev.ts` still says _behavior authoring is not on the public surface_ after D-61, D-68 and D-70 published it (F-78); 02 and 03 still said _33 types_ after D-85 and 07 §K-1 each added one; `failures.ts` still says a consumer must discriminate a `FailureStage` after D-64 stopped handing them one (F-80 (d)); and `kernel.ts:474` justifies using `guarded` rather than throwing on the ground that _`DEV` is true in an ordinary browser … so this is not a test-only path_, which is true in this repository and false in every consumer's. **None is a stale example or a typo. Each is a premise, and F-78 is the proof someone acts on them** — D-107 nearly gated two more validators on `dev.ts`'s four days ago. **The distinguishing feature is that the decisions all landed completely**: this is not deferred work, so the deferred-decision instrument cannot see it, and neither can a green suite. **Not decided here**, because the natural remedy is an instrument and this ledger already has the model — `tests/decisions.node.test.ts` exists because F-63 was this class inside the decision tables. Whether the same treatment is worth extending to justifying prose in source doc blocks is a real question with a real cost, and the API pass was not scoped to settle it. **Note the asymmetry that makes it tractable**: the count instance was caught because `tests/kernel/vocabulary.node.test.ts` checks its list _against the entries_ — the executable half tracked both additions silently and correctly, and only the prose total drifted **Disposition of the four, 2026-08-22 (maintainability entry).** Two closed with D-108 — `dev.ts` was deleted and `kernel.ts:474` now reads _the assertion runs in **every** build since D-108_. The count instance closed with the API entry. **`failures.ts:6` is still live**, still saying _a consumer receiving `onError` has to be able to discriminate it_ while `SortableErrorContext` is one field and `FailureStage` appears in no ordinary-tier declaration — and `src/kernel/errors.ts:3` states the opposite in the same tree. **The instrument question is answered and the answer is no general instrument.** The class splits: **references** — a citation, a path, a symbol — resolve or do not, and D-112 takes them; **prose restating a fact a declarative artifact already owns** needs no instrument but a rule, _cite the artifact rather than copying it_ (the 33-count, `tests/docs.node.test.ts`'s _eight public entries_ against a twelve-entry `typedoc.json` and its _17 known gaps_ against an empty `intentionallyNotExported`, and the figure lists in `bench/size/measure.ts` that produced API-01); and **load-bearing arguments whose premise expired** — `failures.ts` and D-113's orphaned block — admit no mechanical check at all, and their only lever is the landing discipline D-114 (b) states. **The scope that makes any of this affordable is the tense rule**, recorded at D-112. **Disposition, 2026-08-22 (the maintainability slice).** The reference half is **closed and instrumented**: `tests/references.node.test.ts` resolves every citation and every repository path in the normative tree, sixty-seven were repaired, and the class can no longer rot silently. The **published-prose** half is closed by D-113. `src/kernel/failures.ts:6` **is still live** and is F-80's, not this instrument's — it is a load-bearing argument whose premise expired, which admits no mechanical check at all; its only lever is the landing discipline D-114 (b) states, and it needs an owner's call rather than a repair. **The finding stays open on that one instance** |
| F-82 | Eight obligations are live, unwithdrawn and tracked by nothing, and one of them can no longer be discharged as written | **Open, tier B. Booked here so they are addressable before D-114's register exists, and none is decided.** **Five were orphaned by a destination that closed over them**: **Check D-56** (Phase R landed the deletion and nobody took the before/after numbers; the check's own wording forbids a later phase from recording it satisfied, so **the window is permanently gone and this needs an owner's decision rather than a repair** — withdraw the check on D-56's argument, or restate it against a tree that can answer); **D-107's Class B** machine-readable-reason clause, routed to the API deliverable, which closed the bundle entry instead; **D-81's** _whether a stage is a promise or a description_, routed to Checkpoint E, which closed on a different seam; **D-46/D-54's** owed touch measurement, which the deferred vocabulary cannot express and which D-95's Phase 21 sweep did not scope; and `bundle-structure.md` §Corrections to the record, item 3, routed to _the next pass over 05_ — closed here by D-112. **Three more are normative open questions in 05 whose answering event has occurred and which nobody re-read**: **Q-4**'s narrow half (_a question a second implemented behavior answers_ — free drag landed at Phase 19 with two action tags against the sortable's three, and 05 still reads _still open_ in the present tense while `plan.md` asserts Q-4 closed at Phase 16, which is a different clause); **Q-6** (_the test matrix should include a rejection after a collection change_ — 05's matrix carries rejection rows and collection rows and no row combining them); and 05's _a second engine is owed_ clause. **The ninth item is M-02's surviving half**: `kernel/kernel.ts` is 2 468 lines against the 1 971 that prompted the finding, its duplicated-SPI half is closed, and M-02's own disposition was that _mechanical splitting before those decisions is not warranted_ — the decisions it named have since been taken, so splitting is now a live judgment that buys navigation and risks nothing else. **Not made on the last day of agent ownership**, and it belongs to the human owner. **Disposition, 2026-08-22 (the maintainability slice).** All nine are now in one place — [`obligations.md`](../obligations.md), which D-114 (c) creates — and this row is superseded by it rather than duplicated: the register is the live list, and a row leaves it by being discharged, withdrawn or re-booked, never by being forgotten. **Three are discharged** (the `05` re-pointing by D-112, API-03 by D-114 (a), Q-4's narrow half as bookkeeping), **one is withdrawn unsatisfied** (Check D-56, by the owner), and **seven stay live** — including Q-6, which was re-read and **recorded rather than closed**, because what its missing matrix row would assert is a semantic question and not bookkeeping. **The finding stays open** until the register's _Live_ table is empty: this row is the census that found them, and it closes when the last of them is discharged, withdrawn or re-booked there — not when the register exists |
| F-83 | Three of the four decision rows the maintainability slice added carry a fourth cell against a three-column header, so their _closes …_ clauses do not render | **Closed 2026-08-22 by the D-116 remediation pass**, which folded each dropped cell into the end of the one before it and repaired MNT-08's truncation in F-82's row at the same boundary; none of the wording was re-decided. What follows is the finding as found. **Was open, tier C — an editing defect with a rendering consequence, found while adding D-116 to the same table.** The Revision 2.1 decisions table declares three columns — _Decision_, _What and why_, _Supersedes_ — and **D-112, D-114 and D-115 each supply four cells**; GFM discards a row's excess cells, so _closes `bundle-structure.md` §Corrections to the record, item 3 and the `05 §What would reopen this` half of F-82_, _records F-82; makes API-03's remedy general_ and _closes the M-05 half of F-82 and Checkpoint E's E-08 residue_ are present in the source and **absent from the rendered ledger**. D-113 and D-116 are three-cell rows and are unaffected. **This is D-115's own shape in the record rather than in an instrument**: content that exists where it is authored and vanishes where it is read, with nothing failing. The repair is to fold each dropped cell into the end of the preceding one; the wording is already written and none of it is re-decided. Adjacent to MNT-08, which is the same table family — F-82's cell is truncated mid-sentence at _until the live rows are_ — and both belong to the same pass |

## What would falsify this model

- a feature needs a frame part _and_ the prepare-phase pipeline it requires cannot be expressed without giving features a path that both mutates the draft and performs effects;
- ~~a behavior needs to request a kernel _lifecycle_ transition rather than an action — the expected case is keyboard sorting~~ — **discharged, and it did not falsify the model.** Probe 13a produced the case; the answer (D-32) is a second admission member, not a lifecycle-request protocol. A behavior still cannot ask the kernel for a transition, and D-32 added no `KernelHost` member;
- **a discrete operation needs to stay `ACTIVE` across several discrete events.** D-32 mints a one-slot command because that is the shipped keyboard's semantics and the ledger's retained behavior. A _multi-press_ keyboard drag — pick up, move with several arrows, drop — is not expressible: there is no producer of a release other than the kernel's own, and no route for a behavior to end an operation with a proposal. Phase 16's accessibility review against [`accessibility.md`](../../../../.agents/docs/accessibility.md) is where that case would come from, and it would reopen the contract;
- the prepare/effect split cannot express a seam without a third phase;
- `__DEV__` shape assertions catch a real monomorphism break in ordinary use, meaning the frame factory should have stayed kernel-owned (F-2);
- M-1 shows the generic frame copy is measurable on the move path, or a supported lift mode cannot reach a scalar projection;
- a spring landing runner needs something from the kernel — **still live, and now the only tier where the question can be asked** (D-63): the sortable no longer accepts one, so a falsifier has to come from a kernel-tier behavior author;
- ~~a consumer legitimately unmounts or re-keys the dragged item during a drop, breaking the anchor constraint D-16 depends on (Q-12)~~ — **discharged by probe C1, and it did not falsify the model.** It falsified the _scope_: re-keying the dragged item is now out of contract (D-42), and the two supported commit strategies hold the anchor. The answer is a stated integration boundary plus a two-read precondition check, not a recovery protocol.

Added at Revision 2:

- **a consumer needs a supported commit strategy that destroys item identity.** D-42 draws the line at "moves existing nodes"; C1 shows `ReorderRequest.before`/`after` survive connected and mutually sibling in four of five strategies, so recovery is _buildable_. If a mainstream framework's committed output cannot be expressed as node movement, the line is in the wrong place and D-27's cross-container refusal has to be reopened for the post-commit path;
- **a consumer legitimately needs to drag from inside an interactive or editable descendant.** D-46 declines those by default. A drag handle _inside_ a text field, or a sortable whose rows are entirely `contenteditable`, would need admission to distinguish intent from interaction — which probe E deliberately did not attempt, and which the modifier convention only covers for non-interactive regions;
- ~~**the variadic merge in D-45 cannot express required-slot completeness as a type error.** The runtime throw for a missing axis stays regardless; if a tuple-merge type also cannot make the common literal call a compile error, D-45's merge loses the half that paid for keeping the merge in the library rather than in consumer spread syntax;~~ **discharged by D-77, and the model was not falsified — the question was.** Both halves of the bullet assumed the only candidate was a type that folds the argument tuple, and both of its bounds are real for that candidate. A **required first parameter** folds nothing and refuses a `Partial` value, so the completeness the bullet doubted is expressible after all, and the runtime throw does **not** stay. What survives is the bullet's real test: the merge still earns its place, because `plugins` concatenation is the thing consumer spread syntax cannot express, and that is now the merge's whole justification;
- **a behavior needs to observe its own teardown from inside the deferral window.** (continued below) D-38 forbids reading physical state for a liveness answer, and D-36 makes that window one stack frame wide. A behavior that genuinely needs to know _teardown has physically happened_ — rather than _the controller is closed_ — has no signal, and inventing one would re-couple the two events D-36 separated. Added at Revision 2.1:

- **the middle tier's published closure is the whole internal surface.** D-61 publishes `SortableContribution` and `FeatureContext`, and this table's own rule — export what a public type structurally depends on — then publishes what they name. If the closure turns out to reach the behavior's runtime views, its frame part or the kernel's seam types, then the middle rung is not a rung: it is the kernel tier with a different address, and the ladder has two steps rather than three;
- **the four fault classes cannot express a decision a consumer actually makes.** D-64 replaces 14 stages with a coarse code chosen for _fault attribution_. If a real integration needs to branch on something the code does not carry — retry this drop, re-render, disable dragging — the answer is a **richer error object**, not the stages back: the stages were granular on the pipeline axis, which is the axis review 3 §12 rejected;
- **a first-party or product need requires settle-time landing timing that the surviving thunk cannot express.** Review 3 §10 names accepted-vs-return-home travel distance, and says explicitly that it does **not** reopen `run` ownership. It would reopen `duration({ distance, from, to })`, which this contract does not have;
- ~~**`onEnd`'s four arms cannot honestly describe an operation that failed consequentially after its authored commit.**~~ **Answered by D-66, and the answer is that they can**: the frame's own result is published and `onError` carries the fault. What remains falsifiable is narrower and worth watching — **a consumer cannot distinguish _the drop committed and something else broke_ from _the drop committed cleanly_ without reading `onError`.** If that turns out to be a decision consumers must make from `onEnd` alone, the arm count is back under pressure and D-40 is what gives.