/**
 * Public entrypoint for the sortable behavior.
 *
 * Populated in phase 6 (the behavior), phase 7 (`SortableFeature`) and phase 9
 * (the public type surface). See `.agents/docs/drag/plan.md`.
 */
export {};
